window.HELP_DATA = {"title":"UniServer Script API","sections":[{"id":"01-osnovnye-funktsii","title":"Основные функции","intro":"Раздел объединяет базовые утилиты скриптового API, не привязанные к конкретной подсистеме сервера: безопасные преобразования Variant , проверку пустых значений, компактные GUID, короткую паузу Sleep и диагностический вывод DebugLog в лог плагина ( EventScript.log ). В UniServer почти все обменные значения (поля сообщений, параметры страниц, документы журналов, ответы веб-обработчиков) проходят чер","html":"<h1>Основные функции</h1>\n<p>Раздел объединяет базовые утилиты скриптового API, не привязанные к конкретной подсистеме сервера: безопасные преобразования <code>Variant</code>, проверку пустых значений, компактные GUID, короткую паузу <code>Sleep</code> и диагностический вывод <code>DebugLog</code> в лог плагина (<code>EventScript.log</code>). В UniServer почти все обменные значения (поля сообщений, параметры страниц, документы журналов, ответы веб-обработчиков) проходят через <code>Variant</code>, поэтому скрипт постоянно приводит данные к строке, числу или проверяет «пустоту» перед записью в журнал или отправкой сообщения. Функция <code>_ToStr</code> особенно важна для JSON-объектов и массивов (текстовое представление) и для дат (ISO 8601). <code>IsEmpty</code> / <code>VarIsEmptyOrNull</code> задают единый критерий «нет данных» для разных типов. <code>NewPackedGuid</code> / <code>IsPackedGuid</code> дают компактные идентификаторы записей и связей без ручной генерации.</p>\n<p><a id=\"tostr\"></a></p>\n<h3><code>_ToStr</code></h3>\n<p>*** <code>_ToStr</code> — Преобразование значения в строку ****</p>\n<p><code>function _ToStr(V: Variant; Format: Boolean = False): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — JSON-объект / JSON-массив или иное значение для преобразования в строку (по EventScript_desc.odt и <code>fsCommon.pas</code>)<br>\n- <code>Format: Boolean = False</code> — выполнять ли форматирование (переводы строк, отступы) текста JSON при переводе в строку (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>строковое представление значения; для JSON — JSON-текст (по EventScript_desc.odt / <code>fsCommon.pas</code>)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для JSON-объекта или JSON-массива возвращает его текстовое представление.</li>\n<li>При <code>Format = True</code> применяет <code>JsonReformat</code>.</li>\n<li>Для даты формирует ISO 8601 с разделителем <code>T</code> и миллисекундами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // JSON-объект\n  s: String;                 // строковый результат\nbegin\n  x := _Json('{&quot;ID&quot;:10}');   // разобрать JSON-строку в объект\n  s := _ToStr(x);            // преобразовать объект в строку\n  s := _ToStr(x, True);      // то же с форматированием JSON\n  DebugLog(s);               // записать результат в лог\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"todouble\"></a></p>\n<h3><code>ToDouble</code></h3>\n<p>*** <code>ToDouble</code> — Преобразование в <code>Double</code> ****</p>\n<p><code>function ToDouble(V: Variant): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Строковое значение преобразуется после удаления пробелов; остальные значения передаются стандартному преобразованию Variant.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  d: Double;                 // числовой результат\nbegin\n  d := ToDouble('12.5');     // преобразовать строку в Double\n  DebugLog(_ToStr(d));       // вывести результат\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"newpackedguid\"></a></p>\n<h3><code>NewPackedGuid</code></h3>\n<p>*** <code>NewPackedGuid</code> — Создание packed GUID ****</p>\n<p><code>function NewPackedGuid: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Генерирует компактный идентификатор GUID.</li>\n<li>Генерирует компактный GUID; в Scripts* — <code>Result_DocId</code> / CODE операции до <code>SetRecord</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  Result_DocId := NewPackedGuid; // ScriptsCraneScale sme.Empty_OnExit\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code>; <code>Материалы для документации/Скрипты/ScriptsBunkerScale</code>; <code>Материалы для документации/Скрипты/ScriptsCraneScale</code></p>\n<hr>\n<p><a id=\"ispackedguid\"></a></p>\n<h3><code>IsPackedGuid</code></h3>\n<p>*** <code>IsPackedGuid</code> — Проверка packed GUID ****</p>\n<p><code>function IsPackedGuid(Guid: String): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Guid: String</code> — проверяемая строка</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Проверяет, что строка соответствует формату packed GUID.</li>\n<li>Проверка формата packed GUID; в ScriptsJournal — идиома «есть живая ссылка» перед <code>GetRecord</code> / <code>SetLink*</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  if IsPackedGuid(NewDoc.Doc_Link) then\n    AsyncExecProc(JournalName, 'UpdateSlaveDoc',\n      _ObjEx(['MasterDoc', NewDoc, 'SaveRecord', True]));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code>; <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"sleep\"></a></p>\n<h3><code>Sleep</code></h3>\n<p>*** <code>Sleep</code> — Приостановка выполнения ****</p>\n<p><code>procedure Sleep(Milliseconds: Integer)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Milliseconds: Integer</code> — длительность паузы в миллисекундах; по <code>fsCommon.pas</code> ограничивается диапазоном 0…10000</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Значение задержки ограничивается диапазоном от 0 до 10000 мс.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  Sleep(500);                // пауза 500 мс (в реализации ограничено 0…10000)\n  DebugLog('готово');        // продолжение после паузы\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"isempty\"></a></p>\n<h3><code>IsEmpty</code></h3>\n<p>*** <code>IsEmpty</code> — Проверка пустого значения ****</p>\n<p><code>function IsEmpty(V: Variant): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для JSON-массива или объекта проверяется <code>Count = 0</code>.</li>\n<li>Для строк проверяется <code>Length = 0</code>; для остальных значений используется <code>VarIsEmptyOrNull</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой массив\n  if IsEmpty(x) then         // проверить пустоту\n    DebugLog('пусто');       // ветка для пустого значения\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"varisemptyornull\"></a></p>\n<h3><code>VarIsEmptyOrNull</code></h3>\n<p>*** <code>VarIsEmptyOrNull</code> — Проверка Empty или Null ****</p>\n<p><code>function VarIsEmptyOrNull(V: Variant): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вызывает <code>VarIsEmptyOrNull</code> для переданного Variant.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Variant;                // Variant без присвоенного значения\nbegin\n  if VarIsEmptyOrNull(v) then // проверка Empty/Null\n    DebugLog('Empty или Null');\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"debuglog\"></a></p>\n<h3><code>DebugLog</code></h3>\n<p>*** <code>DebugLog</code> — Вывод строки в лог файл плагина ****</p>\n<p><code>procedure DebugLog(V: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — параметр для вывода в лог файл (по примеру в <code>functions.txt</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Процедура ничего не возвращает. (по примеру в <code>functions.txt</code> блок «Возвращает:» пуст)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Записывает указанное значение в <code>EventScript.log</code>.</li>\n<li>Пишет значение в лог плагина (<code>EventScript.log</code>).</li>\n<li>В Scripts* часто: <code>DebugLog('...' + _ToStr(ExceptionMessage))</code> в <code>except</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: String;                 // строка для вывода\nbegin\n  x := '{&quot;ID&quot;:10}';          // подготовить текст\n  DebugLog(x);               // записать значение в лог файл плагина\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/functions.txt</code>; <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Основные функции Раздел объединяет базовые утилиты скриптового API, не привязанные к конкретной подсистеме сервера: безопасные преобразования Variant , проверку пустых значений, компактные GUID, короткую паузу Sleep и диагностический вывод DebugLog в лог плагина ( EventScript.log ). В UniServer почти все обменные значения (поля сообщений, параметры страниц, документы журналов, ответы веб-обработчиков) проходят через Variant , поэтому скрипт постоянно приводит данные к строке, числу или проверяет «пустоту» перед записью в журнал или отправкой сообщения. Функция _ToStr особенно важна для JSON-объектов и массивов (текстовое представление) и для дат (ISO 8601). IsEmpty / VarIsEmptyOrNull задают единый критерий «нет данных» для разных типов. NewPackedGuid / IsPackedGuid дают компактные идентификаторы записей и связей без ручной генерации. _ToStr *** _ToStr — Преобразование значения в строку **** function _ToStr(V: Variant; Format: Boolean = False): String Входные параметры: - V: Variant — JSON-объект / JSON-массив или иное значение для преобразования в строку (по EventScript_desc.odt и fsCommon.pas ) - Format: Boolean = False — выполнять ли форматирование (переводы строк, отступы) текста JSON при переводе в строку (по EventScript_desc.odt) Возвращает: строковое представление значения; для JSON — JSON-текст (по EventScript_desc.odt / fsCommon.pas ) Сведения из исходников / ODT: Для JSON-объекта или JSON-массива возвращает его текстовое представление. При Format = True применяет JsonReformat . Для даты формирует ISO 8601 с разделителем T и миллисекундами. Пример вызова: var\n  x: Variant;                // JSON-объект\n  s: String;                 // строковый результат\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку в объект\n  s := _ToStr(x);            // преобразовать объект в строку\n  s := _ToStr(x, True);      // то же с форматированием JSON\n  DebugLog(s);               // записать результат в лог\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas ToDouble *** ToDouble — Преобразование в Double **** function ToDouble(V: Variant): Double Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Строковое значение преобразуется после удаления пробелов; остальные значения передаются стандартному преобразованию Variant. Пример вызова: var\n  d: Double;                 // числовой результат\nbegin\n  d := ToDouble('12.5');     // преобразовать строку в Double\n  DebugLog(_ToStr(d));       // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas NewPackedGuid *** NewPackedGuid — Создание packed GUID **** function NewPackedGuid: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Генерирует компактный идентификатор GUID. Генерирует компактный GUID; в Scripts* — Result_DocId / CODE операции до SetRecord . Пример вызова: begin\n  Result_DocId := NewPackedGuid; // ScriptsCraneScale sme.Empty_OnExit\nend Источник сведений: Материалы для документации/source/fsCommon.pas ; Материалы для документации/Скрипты/ScriptsBunkerScale ; Материалы для документации/Скрипты/ScriptsCraneScale IsPackedGuid *** IsPackedGuid — Проверка packed GUID **** function IsPackedGuid(Guid: String): Boolean Входные параметры: - Guid: String — проверяемая строка Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Проверяет, что строка соответствует формату packed GUID. Проверка формата packed GUID; в ScriptsJournal — идиома «есть живая ссылка» перед GetRecord / SetLink* . Пример вызова: begin\n  if IsPackedGuid(NewDoc.Doc_Link) then\n    AsyncExecProc(JournalName, 'UpdateSlaveDoc',\n      _ObjEx(['MasterDoc', NewDoc, 'SaveRecord', True]));\nend Источник сведений: Материалы для документации/source/fsCommon.pas ; Материалы для документации/Скрипты/ScriptsJournal Sleep *** Sleep — Приостановка выполнения **** procedure Sleep(Milliseconds: Integer) Входные параметры: - Milliseconds: Integer — длительность паузы в миллисекундах; по fsCommon.pas ограничивается диапазоном 0…10000 Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Значение задержки ограничивается диапазоном от 0 до 10000 мс. Пример вызова: begin\n  Sleep(500);                // пауза 500 мс (в реализации ограничено 0…10000)\n  DebugLog('готово');        // продолжение после паузы\nend Источник сведений: Материалы для документации/source/fsCommon.pas IsEmpty *** IsEmpty — Проверка пустого значения **** function IsEmpty(V: Variant): Boolean Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (ти","entryCount":8},{"id":"02-json-obekty","title":"JSON объекты","intro":"JSON — основной структурный формат данных в скриптах UniServer. Сообщения несут Value типа Variant , часто как JSON-объект; фильтры GetRecords , документы SetRecord , параметры ReturnPage и ответы страниц данных собираются через _Obj / _Arr / _Json . Функции _ArrEx / _ObjEx создают структуру сразу со значениями; _Copy копирует JSON через строковое представление. QuotedStrJSON и EscapeJSON готовят ","html":"<h1>JSON объекты</h1>\n<p>JSON — основной структурный формат данных в скриптах UniServer. Сообщения несут <code>Value</code> типа <code>Variant</code>, часто как JSON-объект; фильтры <code>GetRecords</code>, документы <code>SetRecord</code>, параметры <code>ReturnPage</code> и ответы страниц данных собираются через <code>_Obj</code> / <code>_Arr</code> / <code>_Json</code>. Функции <code>_ArrEx</code> / <code>_ObjEx</code> создают структуру сразу со значениями; <code>_Copy</code> копирует JSON через строковое представление. <code>QuotedStrJSON</code> и <code>EscapeJSON</code> готовят безопасные фрагменты для ручной сборки JSON-текста. После создания объекта доступны свойства и методы (<code>_Kind</code>, <code>_Count</code>, <code>AddValue</code>, <code>Exists</code> и др.). Правило платформы: сначала структура (<code>_Json</code> / <code>_Obj</code>), затем поля; обратно в текст — <code>_ToStr</code> или <code>ToString</code>.</p>\n<p><a id=\"quotedstrjson\"></a></p>\n<h3><code>QuotedStrJSON</code></h3>\n<p>*** <code>QuotedStrJSON</code> — Строка в кавычках JSON ****</p>\n<p><code>function QuotedStrJSON(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Заключает строку в JSON-кавычки.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(QuotedStrJSON('a&quot;b')); // строка в JSON-кавычках с экранированием\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"escapejson\"></a></p>\n<h3><code>EscapeJSON</code></h3>\n<p>*** <code>EscapeJSON</code> — Экранирование JSON ****</p>\n<p><code>function EscapeJSON(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Экранирует спецсимволы для JSON-строки.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(EscapeJSON('a&quot;b')); // экранирование спецсимволов JSON без внешних кавычек\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"arr\"></a></p>\n<h3><code>_Arr</code></h3>\n<p>*** <code>_Arr</code> — Создание JSON-массива ****</p>\n<p><code>function _Arr: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>переменная для хранения JSON-массива (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт переменную для хранения JSON-массива.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой JSON-массив\n  x.Add(10);                 // добавить элемент\n  DebugLog(_ToStr(x));       // вывести массив как строку\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"arrex\"></a></p>\n<h3><code>_ArrEx</code></h3>\n<p>*** <code>_ArrEx</code> — Создание JSON-массива со значениями ****</p>\n<p><code>function _ArrEx(A: array of Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>A: array of Variant</code> — массив начальных значений для JSON-массива (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>JSON-массив с добавленными значениями (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт JSON-массив и добавляет переданные значения.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _ArrEx([1, 2, 3]);     // создать массив с начальными значениями\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"obj\"></a></p>\n<h3><code>_Obj</code></h3>\n<p>*** <code>_Obj</code> — Создание JSON-объекта ****</p>\n<p><code>function _Obj: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>переменная для хранения JSON-объекта (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт переменную для хранения JSON-объекта.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _Obj();               // создать пустой JSON-объект\n  x.AddValue('ID', 10);      // добавить поле\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"objex\"></a></p>\n<h3><code>_ObjEx</code></h3>\n<p>*** <code>_ObjEx</code> — Создание JSON-объекта с парами ****</p>\n<p><code>function _ObjEx(A: array of Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>A: array of Variant</code> — пары имя–значение для JSON-объекта (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>JSON-объект с переданными парами имя–значение (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт JSON-объект из переданных пар имя—значение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _ObjEx(['ID', 10, 'Name', 'X']); // объект из пар имя/значение\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"json\"></a></p>\n<h3><code>_Json</code></h3>\n<p>*** <code>_Json</code> — Разбор JSON-строки ****</p>\n<p><code>function _Json(S: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — строка описания массива или объекта JSON (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>JSON-объект или JSON-массив, созданный из строки (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт JSON-объект или JSON-массив из строки описания.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _Json('{&quot;ID&quot;:10}');   // разобрать JSON-строку\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"copy\"></a></p>\n<h3><code>_Copy</code></h3>\n<p>*** <code>_Copy</code> — Копирование значения ****</p>\n<p><code>function _Copy(V: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>JSON-объект или JSON-массив копируется через его строковое представление; прочие значения возвращаются без такого копирования.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  a, b: Variant;\nbegin\n  a := _Json('{&quot;ID&quot;:10}');   // исходный объект\n  b := _Copy(a);             // копия JSON-значения\n  DebugLog(_ToStr(b));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<h2><code>JSON-объект / JSON-массив</code>: свойства и методы</h2>\n<p><a id=\"json-json-kind\"></a></p>\n<h3><code>JSON-объект / JSON-массив._Kind</code></h3>\n<p>*** <code>JSON-объект / JSON-массив._Kind</code> — Тип JSON-переменной ****</p>\n<p><code>property _Kind: TDocVariantKind</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDocVariantKind</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает <code>dvObject</code> для объекта или <code>dvArray</code> для массива.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Свойство `_Kind` доступно у соответствующего объекта интерфейса.\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-count\"></a></p>\n<h3><code>JSON-объект / JSON-массив._Count</code></h3>\n<p>*** <code>JSON-объект / JSON-массив._Count</code> — Количество элементов ****</p>\n<p><code>property _Count: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает количество элементов массива или объекта.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Свойство `_Count` доступно у соответствующего объекта интерфейса.\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-name\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Name</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Name</code> — Имя элемента объекта ****</p>\n<p><code>function Name(Idx: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Idx: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает имя значения в объекте по индексу.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Name(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-value\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Value</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Value</code> — Значение элемента ****</p>\n<p><code>function Value(Idx: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Idx: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает или задаёт значение в массиве либо объекте.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Value(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-exists\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Exists</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Exists</code> — Проверка существования ****</p>\n<p><code>function Exists(Name: String): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Проверяет имя в объекте или значение в массиве.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Exists(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-add\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Add</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Add</code> — Добавление в массив ****</p>\n<p><code>function Add(V: Variant): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Добавляет значение в массив и возвращает его индекс.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Add(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-addvalue\"></a></p>\n<h3><code>JSON-объект / JSON-массив.AddValue</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.AddValue</code> — Добавление в объект ****</p>\n<p><code>function AddValue(Name: String; V: Variant): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Добавляет имя и значение в объект и возвращает индекс.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := AddValue(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-delete\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Delete</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Delete</code> — Удаление элемента ****</p>\n<p><code>procedure Delete(Idx: Integer)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Idx: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Удаляет элемент из массива или объекта.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Delete(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-tostring\"></a></p>\n<h3><code>JSON-объект / JSON-массив.ToString</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.ToString</code> — Преобразование в строку ****</p>\n<p><code>function ToString: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует массив или объект в строку.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := ToString(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>","text":"JSON объекты JSON — основной структурный формат данных в скриптах UniServer. Сообщения несут Value типа Variant , часто как JSON-объект; фильтры GetRecords , документы SetRecord , параметры ReturnPage и ответы страниц данных собираются через _Obj / _Arr / _Json . Функции _ArrEx / _ObjEx создают структуру сразу со значениями; _Copy копирует JSON через строковое представление. QuotedStrJSON и EscapeJSON готовят безопасные фрагменты для ручной сборки JSON-текста. После создания объекта доступны свойства и методы ( _Kind , _Count , AddValue , Exists и др.). Правило платформы: сначала структура ( _Json / _Obj ), затем поля; обратно в текст — _ToStr или ToString . QuotedStrJSON *** QuotedStrJSON — Строка в кавычках JSON **** function QuotedStrJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Заключает строку в JSON-кавычки. Пример вызова: begin\n  DebugLog(QuotedStrJSON('a\"b')); // строка в JSON-кавычках с экранированием\nend Источник сведений: Материалы для документации/source/fsCommon.pas EscapeJSON *** EscapeJSON — Экранирование JSON **** function EscapeJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Экранирует спецсимволы для JSON-строки. Пример вызова: begin\n  DebugLog(EscapeJSON('a\"b')); // экранирование спецсимволов JSON без внешних кавычек\nend Источник сведений: Материалы для документации/source/fsCommon.pas _Arr *** _Arr — Создание JSON-массива **** function _Arr: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-массива (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-массива. Пример вызова: var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой JSON-массив\n  x.Add(10);                 // добавить элемент\n  DebugLog(_ToStr(x));       // вывести массив как строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _ArrEx *** _ArrEx — Создание JSON-массива со значениями **** function _ArrEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — массив начальных значений для JSON-массива (по EventScript_desc.odt) Возвращает: JSON-массив с добавленными значениями (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-массив и добавляет переданные значения. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ArrEx([1, 2, 3]);     // создать массив с начальными значениями\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _Obj *** _Obj — Создание JSON-объекта **** function _Obj: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-объекта (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-объекта. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Obj();               // создать пустой JSON-объект\n  x.AddValue('ID', 10);      // добавить поле\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _ObjEx *** _ObjEx — Создание JSON-объекта с парами **** function _ObjEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — пары имя–значение для JSON-объекта (по EventScript_desc.odt) Возвращает: JSON-объект с переданными парами имя–значение (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект из переданных пар имя—значение. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ObjEx(['ID', 10, 'Name', 'X']); // объект из пар имя/значение\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _Json *** _Json — Разбор JSON-строки **** function _Json(S: String): Variant Входные параметры: - S: String — строка описания массива или объекта JSON (по EventScript_desc.odt) Возвращает: JSON-объект или JSON-массив, созданный из строки (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект или JSON-массив из строки описания. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _Copy *** _Copy — Копирование значения **** function _Copy(V: Variant): Variant Входные параметры: - V: Variant — > ⚠ ТРЕБ","entryCount":17},{"id":"03-stroki-i-formatirovanie","title":"Строки и форматирование","intro":"Строковый слой API закрывает повседневную обработку текста в PascalScript: регистр, длина, поиск, замена и форматирование. Поиск Pos и замена StringReplace выполняются без учёта регистра — это нужно учитывать при сравнении кодов, номеров ТС и имён полей. Семейство Format / FormatUtf8 / FormatJson / FormatFloat собирает строки для логов, HTTP-ответов и JSON. Строковые функции не «знают» о сервере —","html":"<h1>Строки и форматирование</h1>\n<p>Строковый слой API закрывает повседневную обработку текста в PascalScript: регистр, длина, поиск, замена и форматирование. Поиск <code>Pos</code> и замена <code>StringReplace</code> выполняются без учёта регистра — это нужно учитывать при сравнении кодов, номеров ТС и имён полей. Семейство <code>Format</code> / <code>FormatUtf8</code> / <code>FormatJson</code> / <code>FormatFloat</code> собирает строки для логов, HTTP-ответов и JSON. Строковые функции не «знают» о сервере — они готовят данные, которые затем уходят в <code>ReturnText</code>, сообщение или поле журнала.</p>\n<p><a id=\"quotedstr\"></a></p>\n<h3><code>QuotedStr</code></h3>\n<p>*** <code>QuotedStr</code> — Заключение строки в кавычки ****</p>\n<p><code>function QuotedStr(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает строку в кавычках Pascal.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  s: String;                 // результат с кавычками\nbegin\n  s := QuotedStr('abc');     // обернуть строку в кавычки\n  DebugLog(s);               // вывести результат\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"uppercase\"></a></p>\n<h3><code>UpperCase</code></h3>\n<p>*** <code>UpperCase</code> — Верхний регистр ****</p>\n<p><code>function UpperCase(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Переводит строку в верхний регистр.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(UpperCase('abc')); // преобразовать к верхнему регистру\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"lowercase\"></a></p>\n<h3><code>LowerCase</code></h3>\n<p>*** <code>LowerCase</code> — Нижний регистр ****</p>\n<p><code>function LowerCase(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Переводит строку в нижний регистр.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(LowerCase('ABC')); // преобразовать к нижнему регистру\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"length\"></a></p>\n<h3><code>Length</code></h3>\n<p>*** <code>Length</code> — Длина строки ****</p>\n<p><code>function Length(S: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает число символов в строке.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(Length('abc'))); // длина строки\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"pos\"></a></p>\n<h3><code>Pos</code></h3>\n<p>*** <code>Pos</code> — Поиск подстроки ****</p>\n<p><code>function Pos(SubStr: String; Str: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SubStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Str: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Используется регистронезависимый поиск <code>PosI</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(Pos('b', 'abc'))); // позиция подстроки (поиск без учёта регистра)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"stringreplace\"></a></p>\n<h3><code>StringReplace</code></h3>\n<p>*** <code>StringReplace</code> — Замена подстрок ****</p>\n<p><code>function StringReplace(const S, OldPattern, NewPattern: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>OldPattern: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NewPattern: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Используются флаги <code>rfReplaceAll</code> и <code>rfIgnoreCase</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(StringReplace('a-a', 'a', 'x')); // заменить все вхождения без учёта регистра\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"format\"></a></p>\n<h3><code>Format</code></h3>\n<p>*** <code>Format</code> — Форматирование строки ****</p>\n<p><code>function Format(Fmt: String; Args: array of Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: array of Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Собирает строку по шаблону и аргументам.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(Format('%s-%d', ['A', 1])); // сформировать строку по шаблону Format\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"formatutf8\"></a></p>\n<h3><code>FormatUtf8</code></h3>\n<p>*** <code>FormatUtf8</code> — Форматирование UTF-8 ****</p>\n<p><code>function FormatUtf8(Fmt: String; Args: array of Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: array of Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Форматирует строку с учётом UTF-8.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(FormatUtf8('%s', ['тест'])); // форматирование через FormatUtf8\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"formatjson\"></a></p>\n<h3><code>FormatJson</code></h3>\n<p>*** <code>FormatJson</code> — Форматирование для JSON ****</p>\n<p><code>function FormatJson(Fmt: String; Args: array of Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: array of Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Форматирует строку для использования в JSON-контексте.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(FormatJson('{&quot;n&quot;:?}', ['x'])); // форматирование с JSON-экранированием аргументов\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"formatfloat\"></a></p>\n<h3><code>FormatFloat</code></h3>\n<p>*** <code>FormatFloat</code> — Форматирование вещественного числа ****</p>\n<p><code>function FormatFloat(Fmt: String; Value: Double): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Value: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Форматирует <code>Double</code> по заданному шаблону.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(FormatFloat('0.00', 1.5)); // формат вещественного числа\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Строки и форматирование Строковый слой API закрывает повседневную обработку текста в PascalScript: регистр, длина, поиск, замена и форматирование. Поиск Pos и замена StringReplace выполняются без учёта регистра — это нужно учитывать при сравнении кодов, номеров ТС и имён полей. Семейство Format / FormatUtf8 / FormatJson / FormatFloat собирает строки для логов, HTTP-ответов и JSON. Строковые функции не «знают» о сервере — они готовят данные, которые затем уходят в ReturnText , сообщение или поле журнала. QuotedStr *** QuotedStr — Заключение строки в кавычки **** function QuotedStr(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает строку в кавычках Pascal. Пример вызова: var\n  s: String;                 // результат с кавычками\nbegin\n  s := QuotedStr('abc');     // обернуть строку в кавычки\n  DebugLog(s);               // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas UpperCase *** UpperCase — Верхний регистр **** function UpperCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в верхний регистр. Пример вызова: begin\n  DebugLog(UpperCase('abc')); // преобразовать к верхнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas LowerCase *** LowerCase — Нижний регистр **** function LowerCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в нижний регистр. Пример вызова: begin\n  DebugLog(LowerCase('ABC')); // преобразовать к нижнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas Length *** Length — Длина строки **** function Length(S: String): Integer Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает число символов в строке. Пример вызова: begin\n  DebugLog(_ToStr(Length('abc'))); // длина строки\nend Источник сведений: Материалы для документации/source/fsCommon.pas Pos *** Pos — Поиск подстроки **** function Pos(SubStr: String; Str: String): Integer Входные параметры: - SubStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Str: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используется регистронезависимый поиск PosI . Пример вызова: begin\n  DebugLog(_ToStr(Pos('b', 'abc'))); // позиция подстроки (поиск без учёта регистра)\nend Источник сведений: Материалы для документации/source/fsCommon.pas StringReplace *** StringReplace — Замена подстрок **** function StringReplace(const S, OldPattern, NewPattern: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - OldPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NewPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используются флаги rfReplaceAll и rfIgnoreCase . Пример вызова: begin\n  DebugLog(StringReplace('a-a', 'a', 'x')); // заменить все вхождения без учёта регистра\nend Источник сведений: Материалы для документации/source/fsCommon.pas Format *** Format — Форматирование строки **** function Format(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Собирает строку по шаблону и аргументам. Пример вызова: begin\n  DebugLog(Format('%s-%d', ['A', 1])); // сформировать строку по шаблону Format\nend Источник сведений: Материалы для документации/source/fsCommon.pas FormatU","entryCount":10},{"id":"04-format-veschestvennyh-chisel","title":"Формат вещественных чисел","intro":"Вещественные значения в весовой системе нельзя произвольно округлять: погрешность и дискрет весов задают допустимое представление массы. SetFormatPrecision округляет по числу знаков после запятой; SetFormatRoundFromDiscret сначала приводит значение к сетке дискрета, затем — к заданной точности. В ScriptsBunkerScale / ScriptsCraneScale любое значение массы перед записью в Result_* или журнал пропус","html":"<h1>Формат вещественных чисел</h1>\n<p>Вещественные значения в весовой системе нельзя произвольно округлять: погрешность и дискрет весов задают допустимое представление массы. <code>SetFormatPrecision</code> округляет по числу знаков после запятой; <code>SetFormatRoundFromDiscret</code> сначала приводит значение к сетке дискрета, затем — к заданной точности. В ScriptsBunkerScale / ScriptsCraneScale любое значение массы перед записью в <code>Result_*</code> или журнал пропускают через <code>SetFormatPrecision(..., Massa_Precision)</code>.</p>\n<p><a id=\"setformatprecision\"></a></p>\n<h3><code>SetFormatPrecision</code></h3>\n<p>*** <code>SetFormatPrecision</code> — Округление по точности ****</p>\n<p><code>function SetFormatPrecision(Value: Double; Precision: Integer): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Precision: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вычисляет <code>round(Value * 10^Precision) / 10^Precision</code>.</li>\n<li>Округление <code>round(Value * 10^Precision) / 10^Precision</code> (fsCommon).</li>\n<li>В весовых Scripts<em> любое значение массы перед записью в Result_</em> / журнал пропускают через <code>SetFormatPrecision(..., Massa_Precision)</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  m: Double;\nbegin\n  // ScriptsBunkerScale / CraneScale\n  m := SetFormatPrecision(Result_CURRENT_MASSA, Massa_Precision);\n  Result_OP_MASSA := m;\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code>; <code>Материалы для документации/Скрипты/ScriptsBunkerScale</code>; <code>Материалы для документации/Скрипты/ScriptsCraneScale</code></p>\n<hr>\n<p><a id=\"setformatroundfromdiscret\"></a></p>\n<h3><code>SetFormatRoundFromDiscret</code></h3>\n<p>*** <code>SetFormatRoundFromDiscret</code> — Округление по дискрету ****</p>\n<p><code>function SetFormatRoundFromDiscret(Value: double; Precision: integer; Discret: double): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Precision: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Discret: double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Если <code>Discret &lt; 0.00000001</code>, он заменяется на <code>0.00000001</code>.</li>\n<li>Вычисляет <code>SetFormatPrecision(round(Value / Discret) * Discret, Precision)</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(SetFormatRoundFromDiscret(1.23, 2, 0.05))); // округление к дискрету\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Формат вещественных чисел Вещественные значения в весовой системе нельзя произвольно округлять: погрешность и дискрет весов задают допустимое представление массы. SetFormatPrecision округляет по числу знаков после запятой; SetFormatRoundFromDiscret сначала приводит значение к сетке дискрета, затем — к заданной точности. В ScriptsBunkerScale / ScriptsCraneScale любое значение массы перед записью в Result_* или журнал пропускают через SetFormatPrecision(..., Massa_Precision) . SetFormatPrecision *** SetFormatPrecision — Округление по точности **** function SetFormatPrecision(Value: Double; Precision: Integer): Double Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вычисляет round(Value * 10^Precision) / 10^Precision . Округление round(Value * 10^Precision) / 10^Precision (fsCommon). В весовых Scripts любое значение массы перед записью в Result_ / журнал пропускают через SetFormatPrecision(..., Massa_Precision) . Пример вызова: var\n  m: Double;\nbegin\n  // ScriptsBunkerScale / CraneScale\n  m := SetFormatPrecision(Result_CURRENT_MASSA, Massa_Precision);\n  Result_OP_MASSA := m;\nend Источник сведений: Материалы для документации/source/fsCommon.pas ; Материалы для документации/Скрипты/ScriptsBunkerScale ; Материалы для документации/Скрипты/ScriptsCraneScale SetFormatRoundFromDiscret *** SetFormatRoundFromDiscret — Округление по дискрету **** function SetFormatRoundFromDiscret(Value: double; Precision: integer; Discret: double): Double Входные параметры: - Value: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Discret: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Если Discret < 0.00000001 , он заменяется на 0.00000001 . Вычисляет SetFormatPrecision(round(Value / Discret) * Discret, Precision) . Пример вызова: begin\n  DebugLog(_ToStr(SetFormatRoundFromDiscret(1.23, 2, 0.05))); // округление к дискрету\nend Источник сведений: Материалы для документации/source/fsCommon.pas","entryCount":2},{"id":"05-data-i-vremya","title":"Дата и время","intro":"Время в UniServer — сквозной контракт между плагинами, журналами и веб-страницами. Обменный текстовый формат — ISO 8601 ( Iso8601ToDateTime / DateTimeToIso8601 ). Функции интервалов ( SecondsBetween , MillisecondsBetween и др.) измеряют давность события — типичный приём при таймаутах (в ScriptsCraneScale: MillisecondsBetween(GetVarModTime('CurrentState'), Now()) ). NowPrecise / NowUTCPrecise дают ","html":"<h1>Дата и время</h1>\n<p>Время в UniServer — сквозной контракт между плагинами, журналами и веб-страницами. Обменный текстовый формат — ISO 8601 (<code>Iso8601ToDateTime</code> / <code>DateTimeToIso8601</code>). Функции интервалов (<code>SecondsBetween</code>, <code>MillisecondsBetween</code> и др.) измеряют давность события — типичный приём при таймаутах (в ScriptsCraneScale: <code>MillisecondsBetween(GetVarModTime('CurrentState'), Now())</code>). <code>NowPrecise</code> / <code>NowUTCPrecise</code> дают текущее время с высокой точностью; семейство <code>Inc*</code> сдвигает метку на заданный шаг.</p>\n<p><a id=\"iso8601todatetime\"></a></p>\n<h3><code>Iso8601ToDateTime</code></h3>\n<p>*** <code>Iso8601ToDateTime</code> — Разбор ISO 8601 ****</p>\n<p><code>function Iso8601ToDateTime(S: String): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует строку даты и времени формата ISO 8601 в <code>TDateTime</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;              // дата/время\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать ISO8601\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"datetimetoiso8601\"></a></p>\n<h3><code>DateTimeToIso8601</code></h3>\n<p>*** <code>DateTimeToIso8601</code> — Форматирование ISO 8601 ****</p>\n<p><code>function DateTimeToIso8601(D: TDateTime): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>D: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует <code>TDateTime</code> в строку ISO 8601.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;              // дата/время\n  s: String;                 // строка ISO8601\nbegin\n  x := NowPrecise;           // текущее время\n  s := DateTimeToIso8601(x); // преобразовать в ISO8601\n  DebugLog(s);               // вывести строку\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"secondsbetween\"></a></p>\n<h3><code>SecondsBetween</code></h3>\n<p>*** <code>SecondsBetween</code> — Разница в секундах ****</p>\n<p><code>function SecondsBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество секунд между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;              // опорная дата\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать дату\n  DebugLog(_ToStr(SecondsBetween(NowPrecise, x))); // разница в секундах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"millisecondsbetween\"></a></p>\n<h3><code>MillisecondsBetween</code></h3>\n<p>*** <code>MillisecondsBetween</code> — Разница в миллисекундах ****</p>\n<p><code>function MillisecondsBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество миллисекунд между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MillisecondsBetween(NowPrecise, x))); // разница в миллисекундах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"minutesbetween\"></a></p>\n<h3><code>MinutesBetween</code></h3>\n<p>*** <code>MinutesBetween</code> — Разница в минутах ****</p>\n<p><code>function MinutesBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество минут между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MinutesBetween(NowPrecise, x))); // разница в минутах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hoursbetween\"></a></p>\n<h3><code>HoursBetween</code></h3>\n<p>*** <code>HoursBetween</code> — Разница в часах ****</p>\n<p><code>function HoursBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество часов между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(HoursBetween(NowPrecise, x))); // разница в часах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"nowprecise\"></a></p>\n<h3><code>NowPrecise</code></h3>\n<p>*** <code>NowPrecise</code> — Текущее локальное время ****</p>\n<p><code>function NowPrecise: TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает текущее локальное время с высокой точностью.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(NowPrecise)); // текущее локальное время\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"nowutcprecise\"></a></p>\n<h3><code>NowUTCPrecise</code></h3>\n<p>*** <code>NowUTCPrecise</code> — Текущее время UTC ****</p>\n<p><code>function NowUTCPrecise: TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает текущее время UTC с высокой точностью.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(NowUTCPrecise)); // текущее UTC-время\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"inchour\"></a></p>\n<h3><code>IncHour</code></h3>\n<p>*** <code>IncHour</code> — Увеличение часов ****</p>\n<p><code>function IncHour(const Value: TDateTime; const NumberOfHours: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfHours: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Сдвигает <code>TDateTime</code> на заданное число часов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncHour(NowPrecise, 1))); // сдвиг на 1 час\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incday\"></a></p>\n<h3><code>IncDay</code></h3>\n<p>*** <code>IncDay</code> — Увеличение дней ****</p>\n<p><code>function IncDay(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Сдвигает <code>TDateTime</code> на заданное число дней.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncDay(NowPrecise, 1))); // сдвиг на 1 день\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incminute\"></a></p>\n<h3><code>IncMinute</code></h3>\n<p>*** <code>IncMinute</code> — Увеличение минут ****</p>\n<p><code>function IncMinute(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Несмотря на имя RTTI-параметра <code>NumberOfDays</code>, реализация вызывает <code>DateUtils.IncMinute</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncMinute(NowPrecise, 1))); // сдвиг на 1 минуту\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incsecond\"></a></p>\n<h3><code>IncSecond</code></h3>\n<p>*** <code>IncSecond</code> — Увеличение секунд ****</p>\n<p><code>function IncSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Несмотря на имя RTTI-параметра <code>NumberOfDays</code>, реализация вызывает <code>DateUtils.IncSecond</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncSecond(NowPrecise, 1))); // сдвиг на 1 секунду\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incmillisecond\"></a></p>\n<h3><code>IncMilliSecond</code></h3>\n<p>*** <code>IncMilliSecond</code> — Увеличение миллисекунд ****</p>\n<p><code>function IncMilliSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Несмотря на имя RTTI-параметра <code>NumberOfDays</code>, реализация вызывает <code>DateUtils.IncMilliSecond</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncMilliSecond(NowPrecise, 100))); // сдвиг на 100 мс\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"ticksperiodtotext\"></a></p>\n<h3><code>TicksPeriodToText</code></h3>\n<p>*** <code>TicksPeriodToText</code> — Период тиков в текст ****</p>\n<p><code>function TicksPeriodToText(const ATicks: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>ATicks: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует период в тиках в текстовое представление.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(TicksPeriodToText(1500)); // текстовое представление периода в тиках\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Дата и время Время в UniServer — сквозной контракт между плагинами, журналами и веб-страницами. Обменный текстовый формат — ISO 8601 ( Iso8601ToDateTime / DateTimeToIso8601 ). Функции интервалов ( SecondsBetween , MillisecondsBetween и др.) измеряют давность события — типичный приём при таймаутах (в ScriptsCraneScale: MillisecondsBetween(GetVarModTime('CurrentState'), Now()) ). NowPrecise / NowUTCPrecise дают текущее время с высокой точностью; семейство Inc* сдвигает метку на заданный шаг. Iso8601ToDateTime *** Iso8601ToDateTime — Разбор ISO 8601 **** function Iso8601ToDateTime(S: String): TDateTime Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует строку даты и времени формата ISO 8601 в TDateTime . Пример вызова: var\n  x: TDateTime;              // дата/время\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать ISO8601\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas DateTimeToIso8601 *** DateTimeToIso8601 — Форматирование ISO 8601 **** function DateTimeToIso8601(D: TDateTime): String Входные параметры: - D: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует TDateTime в строку ISO 8601. Пример вызова: var\n  x: TDateTime;              // дата/время\n  s: String;                 // строка ISO8601\nbegin\n  x := NowPrecise;           // текущее время\n  s := DateTimeToIso8601(x); // преобразовать в ISO8601\n  DebugLog(s);               // вывести строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas SecondsBetween *** SecondsBetween — Разница в секундах **** function SecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество секунд между двумя датами. Пример вызова: var\n  x: TDateTime;              // опорная дата\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать дату\n  DebugLog(_ToStr(SecondsBetween(NowPrecise, x))); // разница в секундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas MillisecondsBetween *** MillisecondsBetween — Разница в миллисекундах **** function MillisecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество миллисекунд между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MillisecondsBetween(NowPrecise, x))); // разница в миллисекундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas MinutesBetween *** MinutesBetween — Разница в минутах **** function MinutesBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество минут между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MinutesBetween(NowPrecise, x))); // разница в минутах\nend Источник сведений: Материалы для документации/source/fsCommon.pas HoursBetween *** HoursBetween — Разница в часах **** function HoursBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (","entryCount":14},{"id":"06-predstavlenie-hex","title":"Представление HEX чисел","intro":"Hex-преобразования обслуживают обмен с оборудованием и двоичными протоколами: вес, пакеты весов, кадры устройств часто приходят или уходят как hex-строки фиксированной ширины ( ByteToHex , WordToHex , IntToHex и обратные HexTo* ). StrToHex / HexToStr учитывают кодовую страницу через TSynAnsiConvert . Раздел тесно связан с CRC: контрольные суммы считаются по hex-представлению буфера.","html":"<h1>Представление HEX чисел</h1>\n<p>Hex-преобразования обслуживают обмен с оборудованием и двоичными протоколами: вес, пакеты весов, кадры устройств часто приходят или уходят как hex-строки фиксированной ширины (<code>ByteToHex</code>, <code>WordToHex</code>, <code>IntToHex</code> и обратные <code>HexTo*</code>). <code>StrToHex</code> / <code>HexToStr</code> учитывают кодовую страницу через <code>TSynAnsiConvert</code>. Раздел тесно связан с CRC: контрольные суммы считаются по hex-представлению буфера.</p>\n<p><a id=\"bytetohex\"></a></p>\n<h3><code>ByteToHex</code></h3>\n<p>*** <code>ByteToHex</code> — Byte в hex ****</p>\n<p><code>function ByteToHex(Value: Byte): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Byte</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 2 символа.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(ByteToHex(255)); // Byte -&gt; hex (ширина 2)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"wordtohex\"></a></p>\n<h3><code>WordToHex</code></h3>\n<p>*** <code>WordToHex</code> — Word в hex ****</p>\n<p><code>function WordToHex(Value: Word): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 4 символа.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(WordToHex(65535)); // Word -&gt; hex (ширина 4)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"inttohex\"></a></p>\n<h3><code>IntToHex</code></h3>\n<p>*** <code>IntToHex</code> — Integer в hex ****</p>\n<p><code>function IntToHex(Value: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 8 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(IntToHex(1)); // Integer -&gt; hex (ширина 8)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"int64tohex\"></a></p>\n<h3><code>Int64ToHex</code></h3>\n<p>*** <code>Int64ToHex</code> — Int64 в hex ****</p>\n<p><code>function Int64ToHex(Value: Int64): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Int64</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 16 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(Int64ToHex(1)); // Int64 -&gt; hex (ширина 16)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"doubletohex\"></a></p>\n<h3><code>DoubleToHex</code></h3>\n<p>*** <code>DoubleToHex</code> — Double в hex ****</p>\n<p><code>function DoubleToHex(Value: Double): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует <code>Double</code> в hex-строку.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DoubleToHex(1.0)); // Double -&gt; hex\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"singletohex\"></a></p>\n<h3><code>SingleToHex</code></h3>\n<p>*** <code>SingleToHex</code> — Single в hex ****</p>\n<p><code>function SingleToHex(Value: Single): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Single</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует <code>Single</code> в hex-строку.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(SingleToHex(1.0)); // Single -&gt; hex\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextobyte\"></a></p>\n<h3><code>HexToByte</code></h3>\n<p>*** <code>HexToByte</code> — Hex в Byte ****</p>\n<p><code>function HexToByte(Value: String): byte</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>byte</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Byte</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToByte('FF'))); // hex -&gt; Byte\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextoword\"></a></p>\n<h3><code>HexToWord</code></h3>\n<p>*** <code>HexToWord</code> — Hex в Word ****</p>\n<p><code>function HexToWord(Value: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в целое (Word/Integer по RTTI).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToWord('FFFF'))); // hex -&gt; Word\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextolongword\"></a></p>\n<h3><code>HexToLongWord</code></h3>\n<p>*** <code>HexToLongWord</code> — Hex в LongWord ****</p>\n<p><code>function HexToLongWord(Value: String): Word</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Word</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в LongWord (сигнатура RTTI: Word).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToLongWord('00000001'))); // hex -&gt; LongWord\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextoint\"></a></p>\n<h3><code>HexToInt</code></h3>\n<p>*** <code>HexToInt</code> — Hex в Integer ****</p>\n<p><code>function HexToInt(Value: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Integer</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToInt('00000001'))); // hex -&gt; Integer\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextoint64\"></a></p>\n<h3><code>HexToInt64</code></h3>\n<p>*** <code>HexToInt64</code> — Hex в Int64 ****</p>\n<p><code>function HexToInt64(Value: String): Int64</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Int64</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Int64</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToInt64('0000000000000001'))); // hex -&gt; Int64\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextodouble\"></a></p>\n<h3><code>HexToDouble</code></h3>\n<p>*** <code>HexToDouble</code> — Hex в Double ****</p>\n<p><code>function HexToDouble(Value: String): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Double</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToDouble(DoubleToHex(1.5)))); // hex -&gt; Double\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextosingle\"></a></p>\n<h3><code>HexToSingle</code></h3>\n<p>*** <code>HexToSingle</code> — Hex в Single ****</p>\n<p><code>function HexToSingle(Value: String): Single</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Single</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Single</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToSingle(SingleToHex(1.5)))); // hex -&gt; Single\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"strtohex\"></a></p>\n<h3><code>StrToHex</code></h3>\n<p>*** <code>StrToHex</code> — Преобразование строки в hex ****</p>\n<p><code>function StrToHex(S: String; CodePage: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>CodePage: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для заданной кодовой страницы используется <code>TSynAnsiConvert</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(StrToHex('A', 1251)); // строка -&gt; hex с кодовой страницей\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextostr\"></a></p>\n<h3><code>HexToStr</code></h3>\n<p>*** <code>HexToStr</code> — Преобразование hex в строку ****</p>\n<p><code>function HexToStr(Value: String; CodePage: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>CodePage: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для заданной кодовой страницы используется <code>TSynAnsiConvert</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(HexToStr(StrToHex('A', 1251), 1251)); // hex -&gt; строка с кодовой страницей\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Представление HEX чисел Hex-преобразования обслуживают обмен с оборудованием и двоичными протоколами: вес, пакеты весов, кадры устройств часто приходят или уходят как hex-строки фиксированной ширины ( ByteToHex , WordToHex , IntToHex и обратные HexTo* ). StrToHex / HexToStr учитывают кодовую страницу через TSynAnsiConvert . Раздел тесно связан с CRC: контрольные суммы считаются по hex-представлению буфера. ByteToHex *** ByteToHex — Byte в hex **** function ByteToHex(Value: Byte): String Входные параметры: - Value: Byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 2 символа. Пример вызова: begin\n  DebugLog(ByteToHex(255)); // Byte -> hex (ширина 2)\nend Источник сведений: Материалы для документации/source/fsCommon.pas WordToHex *** WordToHex — Word в hex **** function WordToHex(Value: Word): String Входные параметры: - Value: Word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 4 символа. Пример вызова: begin\n  DebugLog(WordToHex(65535)); // Word -> hex (ширина 4)\nend Источник сведений: Материалы для документации/source/fsCommon.pas IntToHex *** IntToHex — Integer в hex **** function IntToHex(Value: Integer): String Входные параметры: - Value: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 8 символов. Пример вызова: begin\n  DebugLog(IntToHex(1)); // Integer -> hex (ширина 8)\nend Источник сведений: Материалы для документации/source/fsCommon.pas Int64ToHex *** Int64ToHex — Int64 в hex **** function Int64ToHex(Value: Int64): String Входные параметры: - Value: Int64 — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 16 символов. Пример вызова: begin\n  DebugLog(Int64ToHex(1)); // Int64 -> hex (ширина 16)\nend Источник сведений: Материалы для документации/source/fsCommon.pas DoubleToHex *** DoubleToHex — Double в hex **** function DoubleToHex(Value: Double): String Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Double в hex-строку. Пример вызова: begin\n  DebugLog(DoubleToHex(1.0)); // Double -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas SingleToHex *** SingleToHex — Single в hex **** function SingleToHex(Value: Single): String Входные параметры: - Value: Single — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Single в hex-строку. Пример вызова: begin\n  DebugLog(SingleToHex(1.0)); // Single -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToByte *** HexToByte — Hex в Byte **** function HexToByte(Value: String): byte Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа byte (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Byte . Пример вызова: begin\n  DebugLog(_ToStr(HexToByte('FF'))); // hex -> Byte\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToWord *** HexToWord — Hex в Word **** function HexToWord(Value: String): Integer Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в целое (Word/Integer по RTTI). Пример вызова: begin\n  DebugLog(_ToStr(HexToWord('FFFF'))); // hex -> Word\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToLongWord *** HexToLongWord — Hex в LongWord **** function HexToLongWord(Value: String): Word Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Word (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в ","entryCount":15},{"id":"07-kontrolnye-summy-crc","title":"Контрольные суммы CRC8 и CRC16","intro":"Контрольные суммы CRC8/CRC16 входят в проверку целостности пакетов устройств: скрипт получает данные как hex-строку, считает сумму по выбранному алгоритму и сравнивает с полем контрольной суммы в кадре. Результаты возвращаются снова в hex (2 символа для CRC8, 4 для CRC16). Раздел описывает только вычислительный слой; параметры полинома задаёт документация устройства.","html":"<h1>Контрольные суммы CRC8 и CRC16</h1>\n<p>Контрольные суммы CRC8/CRC16 входят в проверку целостности пакетов устройств: скрипт получает данные как hex-строку, считает сумму по выбранному алгоритму и сравнивает с полем контрольной суммы в кадре. Результаты возвращаются снова в hex (2 символа для CRC8, 4 для CRC16). Раздел описывает только вычислительный слой; параметры полинома задаёт документация устройства.</p>\n<p><a id=\"hextocrc8summod2\"></a></p>\n<h3><code>HexToCRC8SumMod2</code></h3>\n<p>*** <code>HexToCRC8SumMod2</code> — CRC8 в hex ****</p>\n<p><code>function HexToCRC8SumMod2(SourceStr: String; AIndex, Count: integer; FirstByte: byte): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Count: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>FirstByte: byte</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC8 представлен hex-строкой из 2 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(HexToCRC8SumMod2('0102', 0, 2, 0)); // CRC8 SumMod2 по hex-данным\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextocrc8sum\"></a></p>\n<h3><code>HexToCRC8Sum</code></h3>\n<p>*** <code>HexToCRC8Sum</code> — CRC8 в hex ****</p>\n<p><code>function HexToCRC8Sum(SourceStr: String; AIndex, ACount: integer; FirstByte: byte): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ACount: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>FirstByte: byte</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC8 представлен hex-строкой из 2 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(HexToCRC8Sum('0102', 0, 2, 0)); // CRC8 Sum по hex-данным\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextocrc8polinom\"></a></p>\n<h3><code>HexToCRC8Polinom</code></h3>\n<p>*** <code>HexToCRC8Polinom</code> — CRC8 по полиному ****</p>\n<p><code>function HexToCRC8Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ACount: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Poly: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Init: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ReflIn: boolean</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>XorOut: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC8 представлен hex-строкой из 2 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC8Polinom('0102', 0, 2, 0, 0, False, 0));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextocrc16polinom\"></a></p>\n<h3><code>HexToCRC16Polinom</code></h3>\n<p>*** <code>HexToCRC16Polinom</code> — CRC16 по полиному ****</p>\n<p><code>function HexToCRC16Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ACount: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Poly: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Init: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ReflIn: boolean</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>XorOut: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC16 представлен hex-строкой из 4 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC16Polinom('0102', 0, 2, 0, 0, False, 0));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Контрольные суммы CRC8 и CRC16 Контрольные суммы CRC8/CRC16 входят в проверку целостности пакетов устройств: скрипт получает данные как hex-строку, считает сумму по выбранному алгоритму и сравнивает с полем контрольной суммы в кадре. Результаты возвращаются снова в hex (2 символа для CRC8, 4 для CRC16). Раздел описывает только вычислительный слой; параметры полинома задаёт документация устройства. HexToCRC8SumMod2 *** HexToCRC8SumMod2 — CRC8 в hex **** function HexToCRC8SumMod2(SourceStr: String; AIndex, Count: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Count: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8SumMod2('0102', 0, 2, 0)); // CRC8 SumMod2 по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToCRC8Sum *** HexToCRC8Sum — CRC8 в hex **** function HexToCRC8Sum(SourceStr: String; AIndex, ACount: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8Sum('0102', 0, 2, 0)); // CRC8 Sum по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToCRC8Polinom *** HexToCRC8Polinom — CRC8 по полиному **** function HexToCRC8Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ReflIn: boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - XorOut: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC8Polinom('0102', 0, 2, 0, 0, False, 0));\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToCRC16Polinom *** HexToCRC16Polinom — CRC16 по полиному **** function HexToCRC16Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ReflIn: boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого парамет","entryCount":4},{"id":"08-fayly","title":"Файлы","intro":"Файловые функции позволяют скрипту читать и записывать текст относительно каталога плагина ( MyPath из конфигурации ядра): относительный путь разрешается именно к нему. Это не замена универсальным журналам: для весовых и учётных записей платформа использует журналы и сообщения. Кодировка веб-страниц и шаблонов в UniServer — UTF-8.","html":"<h1>Файлы</h1>\n<p>Файловые функции позволяют скрипту читать и записывать текст относительно каталога плагина (<code>MyPath</code> из конфигурации ядра): относительный путь разрешается именно к нему. Это не замена универсальным журналам: для весовых и учётных записей платформа использует журналы и сообщения. Кодировка веб-страниц и шаблонов в UniServer — UTF-8.</p>\n<p><a id=\"stringfromfile\"></a></p>\n<h3><code>StringFromFile</code></h3>\n<p>*** <code>StringFromFile</code> — Чтение текста из файла ****</p>\n<p><code>function StringFromFile(FileName: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>FileName: String</code> — путь к файлу; относительный путь резолвится от MyPath (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Относительный путь разрешается относительно <code>MyPath</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  s: String;                 // содержимое файла\nbegin\n  s := StringFromFile('data.txt'); // прочитать файл\n  DebugLog(s);                     // вывести содержимое\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"filefromstring\"></a></p>\n<h3><code>FileFromString</code></h3>\n<p>*** <code>FileFromString</code> — Запись текста в файл ****</p>\n<p><code>procedure FileFromString(Content: String; FileName: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Content: String</code> — текст для записи в файл<br>\n- <code>FileName: String</code> — путь к файлу; относительный путь резолвится от MyPath (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Относительный путь разрешается относительно <code>MyPath</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  FileFromString('text', 'out.txt'); // записать строку в файл\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Файлы Файловые функции позволяют скрипту читать и записывать текст относительно каталога плагина ( MyPath из конфигурации ядра): относительный путь разрешается именно к нему. Это не замена универсальным журналам: для весовых и учётных записей платформа использует журналы и сообщения. Кодировка веб-страниц и шаблонов в UniServer — UTF-8. StringFromFile *** StringFromFile — Чтение текста из файла **** function StringFromFile(FileName: String): String Входные параметры: - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: var\n  s: String;                 // содержимое файла\nbegin\n  s := StringFromFile('data.txt'); // прочитать файл\n  DebugLog(s);                     // вывести содержимое\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas FileFromString *** FileFromString — Запись текста в файл **** procedure FileFromString(Content: String; FileName: String) Входные параметры: - Content: String — текст для записи в файл - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: begin\n  FileFromString('text', 'out.txt'); // записать строку в файл\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas","entryCount":2},{"id":"09-funktsii-globalnyh-peremennyh","title":"Функции глобальных переменных","intro":"Метаданные изменений глобальных переменных конструктора: время последнего изменения ( GetVarModTime ), счётчик ( GetVarModCount ) и частота ( GetVarModFreq ). В ScriptsCraneScale / BunkerScale их используют для таймаутов авторежима и мониторинга канала массы. При отсутствии переменной с указанным именем функции вызывают исключение. Чтение/запись значений — в разделе Конструктора ( GetVarValue / Se","html":"<h1>Функции глобальных переменных.</h1>\n<p>Метаданные изменений глобальных переменных конструктора: время последнего изменения (<code>GetVarModTime</code>), счётчик (<code>GetVarModCount</code>) и частота (<code>GetVarModFreq</code>). В ScriptsCraneScale / BunkerScale их используют для таймаутов авторежима и мониторинга канала массы. При отсутствии переменной с указанным именем функции вызывают исключение. Чтение/запись значений — в разделе Конструктора (<code>GetVarValue</code> / <code>SetVarValue</code>).</p>\n<p><a id=\"getvarmodtime\"></a></p>\n<h3><code>GetVarModTime</code></h3>\n<p>*** <code>GetVarModTime</code> — Время изменения глобальной переменной ****</p>\n<p><code>function GetVarModTime(Name: String): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя глобальной переменной Конструктора (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n<li>Время последнего изменения глобальной переменной; при отсутствии имени — исключение.</li>\n<li>В ScriptsCraneScale: <code>MillisecondsBetween(GetVarModTime('CurrentState'), Now())</code> для таймаута авторежима.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LTimeOut, LLimit: Integer;\nbegin\n  // ScriptsCraneScale wrk.AutoWeighingControl\n  if modeAuto and (CurrentState = 'ReadyWeighing') then begin\n    LTimeOut := MillisecondsBetween(GetVarModTime('CurrentState'), Now());\n    LLimit := GetConfigIntDef(PluginName + '.AutoModeTimeOut', 5) * 1000;\n    if LTimeOut &gt; LLimit then\n      SendMsg(NewMessage(PluginName + '.StartWeighing', ''), 2000);\n  end;\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code>; <code>Материалы для документации/Скрипты/ScriptsCraneScale</code></p>\n<hr>\n<p><a id=\"getvarmodcount\"></a></p>\n<h3><code>GetVarModCount</code></h3>\n<p>*** <code>GetVarModCount</code> — Счётчик изменений глобальной переменной ****</p>\n<p><code>function GetVarModCount(Name: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя глобальной переменной Конструктора (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetVarModCount('Counter'))); // счётчик изменений\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getvarmodfreq\"></a></p>\n<h3><code>GetVarModFreq</code></h3>\n<p>*** <code>GetVarModFreq</code> — Частота изменений глобальной переменной ****</p>\n<p><code>function GetVarModFreq(Name: String): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя глобальной переменной Конструктора (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n<li>Частота изменений глобальной переменной; в ScriptsBunkerScale — мониторинг канала массы.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // ScriptsBunkerScale: частота обновления канала массы\n  DebugLog(_ToStr(GetVarModFreq('Result_CURRENT_MASSA')));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code>; <code>Материалы для документации/Скрипты/ScriptsBunkerScale</code></p>\n<hr>","text":"Функции глобальных переменных. Метаданные изменений глобальных переменных конструктора: время последнего изменения ( GetVarModTime ), счётчик ( GetVarModCount ) и частота ( GetVarModFreq ). В ScriptsCraneScale / BunkerScale их используют для таймаутов авторежима и мониторинга канала массы. При отсутствии переменной с указанным именем функции вызывают исключение. Чтение/запись значений — в разделе Конструктора ( GetVarValue / SetVarValue ). GetVarModTime *** GetVarModTime — Время изменения глобальной переменной **** function GetVarModTime(Name: String): TDateTime Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Время последнего изменения глобальной переменной; при отсутствии имени — исключение. В ScriptsCraneScale: MillisecondsBetween(GetVarModTime('CurrentState'), Now()) для таймаута авторежима. Пример вызова: var\n  LTimeOut, LLimit: Integer;\nbegin\n  // ScriptsCraneScale wrk.AutoWeighingControl\n  if modeAuto and (CurrentState = 'ReadyWeighing') then begin\n    LTimeOut := MillisecondsBetween(GetVarModTime('CurrentState'), Now());\n    LLimit := GetConfigIntDef(PluginName + '.AutoModeTimeOut', 5) * 1000;\n    if LTimeOut > LLimit then\n      SendMsg(NewMessage(PluginName + '.StartWeighing', ''), 2000);\n  end;\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsCraneScale GetVarModCount *** GetVarModCount — Счётчик изменений глобальной переменной **** function GetVarModCount(Name: String): Integer Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(_ToStr(GetVarModCount('Counter'))); // счётчик изменений\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetVarModFreq *** GetVarModFreq — Частота изменений глобальной переменной **** function GetVarModFreq(Name: String): Double Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Частота изменений глобальной переменной; в ScriptsBunkerScale — мониторинг канала массы. Пример вызова: begin\n  // ScriptsBunkerScale: частота обновления канала массы\n  DebugLog(_ToStr(GetVarModFreq('Result_CURRENT_MASSA')));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsBunkerScale","entryCount":3},{"id":"10-server-parametry-nastroek","title":"Сервер. Параметры настроек","intro":"GetConfigValue и типизированные GetConfig*Def читают параметры сервера по имени (например Core.ServerID , PluginName + '.ObjectName' ). В регистрации RTTI эти методы помечены как старые, сохранённые для совместимости; в production-скриптах они по-прежнему широко используются. Запись настроек в скриптах Конструктора часто идёт через CoreConfig.Values[...] (см. раздел Конструктора).","html":"<h1>Сервер. Параметры настроек</h1>\n<p><code>GetConfigValue</code> и типизированные <code>GetConfig*Def</code> читают параметры сервера по имени (например <code>Core.ServerID</code>, <code>PluginName + '.ObjectName'</code>). В регистрации RTTI эти методы помечены как старые, сохранённые для совместимости; в production-скриптах они по-прежнему широко используются. Запись настроек в скриптах Конструктора часто идёт через <code>CoreConfig.Values[...]</code> (см. раздел Конструктора).</p>\n<p><a id=\"getconfigvalue\"></a></p>\n<h3><code>GetConfigValue</code></h3>\n<p>*** <code>GetConfigValue</code> — Прочитать значение параметра настройки по наименованию ****</p>\n<p><code>function GetConfigValue(Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — строка с именем параметра настройки (по примеру в <code>functions.txt</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>значение параметра настройки (по примеру в <code>functions.txt</code>)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает конфигурацию по имени.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // значение настройки\nbegin\n  x := GetConfigValue('Core.ServerID'); // прочитать параметр по имени\n  DebugLog(x);                          // вывести прочитанное значение в лог\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/functions.txt</code>; <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getconfigfloatdef\"></a></p>\n<h3><code>GetConfigFloatDef</code></h3>\n<p>*** <code>GetConfigFloatDef</code> — Чтение <code>Double</code> с значением по умолчанию ****</p>\n<p><code>function GetConfigFloatDef(Name: String; ADefValue: Double): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ADefValue: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает <code>CoreConfig.Values</code> и применяет преобразование с <code>ADefValue</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Double;\nbegin\n  v := GetConfigFloatDef('SomeFloat', 0.0); // читать Double с значением по умолчанию\n  DebugLog(_ToStr(v));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getconfigintdef\"></a></p>\n<h3><code>GetConfigIntDef</code></h3>\n<p>*** <code>GetConfigIntDef</code> — Чтение <code>Integer</code> с значением по умолчанию ****</p>\n<p><code>function GetConfigIntDef(Name: String; ADefValue: Integer): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ADefValue: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает <code>CoreConfig.Values</code> и применяет преобразование с <code>ADefValue</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Integer;\nbegin\n  v := GetConfigIntDef('SomeInt', 0); // читать Integer с значением по умолчанию\n  DebugLog(_ToStr(v));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getconfigbooldef\"></a></p>\n<h3><code>GetConfigBoolDef</code></h3>\n<p>*** <code>GetConfigBoolDef</code> — Чтение <code>Boolean</code> с значением по умолчанию ****</p>\n<p><code>function GetConfigBoolDef(Name: String; ADefValue: Boolean): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ADefValue: Boolean</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает <code>CoreConfig.Values</code> и применяет преобразование с <code>ADefValue</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Boolean;\nbegin\n  v := GetConfigBoolDef('SomeBool', False); // читать Boolean с значением по умолчанию\n  DebugLog(_ToStr(v));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Сервер. Параметры настроек GetConfigValue и типизированные GetConfig*Def читают параметры сервера по имени (например Core.ServerID , PluginName + '.ObjectName' ). В регистрации RTTI эти методы помечены как старые, сохранённые для совместимости; в production-скриптах они по-прежнему широко используются. Запись настроек в скриптах Конструктора часто идёт через CoreConfig.Values[...] (см. раздел Конструктора). GetConfigValue *** GetConfigValue — Прочитать значение параметра настройки по наименованию **** function GetConfigValue(Name: String): Variant Входные параметры: - Name: String — строка с именем параметра настройки (по примеру в functions.txt ) Возвращает: значение параметра настройки (по примеру в functions.txt ) Сведения из исходников / ODT: Читает конфигурацию по имени. Пример вызова: var\n  x: Variant;                // значение настройки\nbegin\n  x := GetConfigValue('Core.ServerID'); // прочитать параметр по имени\n  DebugLog(x);                          // вывести прочитанное значение в лог\nend Источник сведений: Материалы для документации/functions.txt ; Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetConfigFloatDef *** GetConfigFloatDef — Чтение Double с значением по умолчанию **** function GetConfigFloatDef(Name: String; ADefValue: Double): Double Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Double;\nbegin\n  v := GetConfigFloatDef('SomeFloat', 0.0); // читать Double с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetConfigIntDef *** GetConfigIntDef — Чтение Integer с значением по умолчанию **** function GetConfigIntDef(Name: String; ADefValue: Integer): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Integer;\nbegin\n  v := GetConfigIntDef('SomeInt', 0); // читать Integer с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetConfigBoolDef *** GetConfigBoolDef — Чтение Boolean с значением по умолчанию **** function GetConfigBoolDef(Name: String; ADefValue: Boolean): Boolean Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Boolean;\nbegin\n  v := GetConfigBoolDef('SomeBool', False); // читать Boolean с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas","entryCount":4},{"id":"11-server-otpravka-soobscheniy","title":"Сервер. Отправка сообщений","intro":"Сообщения — шина взаимодействия плагинов и ядра UniServer. Имя имеет вид ИмяПлагина.ИмяСообщения ; в Scripts* команду почти всегда собирают как PluginName + '.Command' . У сообщения есть Value ( Variant ), опциональный Blob , метка времени и признак устаревания. NewMessage / NewMessageEx создают объект; PostMsg ставит в очередь асинхронно; SendMsg ждёт обработки до Timeout мс и возвращает поле Res","html":"<h1>Сервер. Отправка сообщений</h1>\n<p>Сообщения — шина взаимодействия плагинов и ядра UniServer. Имя имеет вид <code>ИмяПлагина.ИмяСообщения</code>; в Scripts* команду почти всегда собирают как <code>PluginName + '.Command'</code>. У сообщения есть <code>Value</code> (<code>Variant</code>), опциональный <code>Blob</code>, метка времени и признак устаревания. <code>NewMessage</code> / <code>NewMessageEx</code> создают объект; <code>PostMsg</code> ставит в очередь асинхронно; <code>SendMsg</code> ждёт обработки до <code>Timeout</code> мс и возвращает поле <code>Result</code>. В cmd-скриптах ответ кладут в <code>Msg.Result</code>. Через эту же шину уходят операции журналов (<code>Journal.Operation</code>).</p>\n<p><a id=\"newmessage\"></a></p>\n<h3><code>NewMessage</code></h3>\n<p>*** <code>NewMessage</code> — Создание сообщения ****</p>\n<p><code>function NewMessage(MsgName: String; Value: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>MsgName: String</code> — имя сообщения вида <code>ИмяПлагина.ИмяСообщения</code> (по PluginAPI_desc.odt)<br>\n- <code>Value: Variant</code> — полезная нагрузка сообщения (тип Variant по RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Имя сообщения имеет вид <code>ИмяПлагина.ИмяСообщения</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;             // сообщение\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // создать сообщение по имени\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"newmessageex\"></a></p>\n<h3><code>NewMessageEx</code></h3>\n<p>*** <code>NewMessageEx</code> — Создание расширенного сообщения ****</p>\n<p><code>function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>MsgName: String</code> — имя сообщения вида <code>ИмяПлагина.ИмяСообщения</code> (по PluginAPI_desc.odt)<br>\n- <code>Value: Variant</code> — полезная нагрузка сообщения (<code>Variant</code>, по PluginAPI_desc.odt / RTTI)<br>\n- <code>Blob: Variant</code> — двоичные данные сообщения (<code>IBlob</code> / <code>TIBlob</code>, по PluginAPI_desc.odt)<br>\n- <code>CurrentUser: Variant</code> — пользователь, от имени которого создаётся сообщение (<code>IUser</code> / <code>TIUser</code>, по TLB)<br>\n- <code>Timeout: Integer</code> — таймаут сообщения в миллисекундах (по RTTI / TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Имя сообщения имеет вид <code>ИмяПлагина.ИмяСообщения</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"postmsg\"></a></p>\n<h3><code>PostMsg</code></h3>\n<p>*** <code>PostMsg</code> — Асинхронная отправка сообщения ****</p>\n<p><code>function PostMsg(Msg: Variant): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Msg: Variant</code> — сообщение для асинхронной постановки в очередь (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>по PluginAPI_desc.odt для интерфейса ядра PostMsg возвращает WordBool/False при отсутствии обработчиков; в глобальной RTTI-процедуре <code>procedure PostMsg</code> возвращаемого значения нет</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Постановка в очередь выполняется асинхронно; возвращает <code>False</code>, если обработчиков нет.</li>\n<li>Асинхронная постановка в очередь; в Scripts* — FixPhoto, SaveRecord, Enable (fire-and-forget).</li>\n<li>Глобальная RTTI-процедура <code>procedure PostMsg</code> не возвращает значение (в отличие от <code>TICoreMessages.PostMsg</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  PostMsg(NewMessage(PluginName + '.FixPhoto', '')); // fire-and-forget\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/Скрипты/ScriptsAutoControl</code></p>\n<hr>\n<p><a id=\"sendmsg\"></a></p>\n<h3><code>SendMsg</code></h3>\n<p>*** <code>SendMsg</code> — Синхронная отправка сообщения ****</p>\n<p><code>function SendMsg(Msg: Variant; Timeout: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Msg: Variant</code> — сообщение для синхронной отправки (по PluginAPI_desc.odt)<br>\n- <code>Timeout: Integer</code> — таймаут ожидания в миллисекундах; <code>-1</code> — бесконечное ожидание (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>результат обработки (поле Result сообщения) после ожидания до Timeout (по PluginAPI_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ожидает обработку до <code>Timeout</code> мс; <code>-1</code> означает бесконечное ожидание.</li>\n<li>Результат берётся из поля <code>Result</code> сообщения.</li>\n<li>Синхронная отправка; результат — поле <code>Result</code> сообщения после ожидания.</li>\n<li>В cmd-скриптах ответ кладут в <code>Msg.Result</code>; снаружи читают значение <code>SendMsg(...)</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg, LRes: Variant;\nbegin\n  // ScriptsAutoControl: имя = PluginName + '.Command'\n  LMsg := NewMessage(PluginName + '.FindPassDoc', _ToStr(_ObjEx(['Method', 'RFID', 'Label', RFID])));\n  LRes := SendMsg(LMsg, 2000);\n  DebugLog(_ToStr(LRes));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/Скрипты/ScriptsAutoControl</code></p>\n<hr>","text":"Сервер. Отправка сообщений Сообщения — шина взаимодействия плагинов и ядра UniServer. Имя имеет вид ИмяПлагина.ИмяСообщения ; в Scripts* команду почти всегда собирают как PluginName + '.Command' . У сообщения есть Value ( Variant ), опциональный Blob , метка времени и признак устаревания. NewMessage / NewMessageEx создают объект; PostMsg ставит в очередь асинхронно; SendMsg ждёт обработки до Timeout мс и возвращает поле Result . В cmd-скриптах ответ кладут в Msg.Result . Через эту же шину уходят операции журналов ( Journal.Operation ). NewMessage *** NewMessage — Создание сообщения **** function NewMessage(MsgName: String; Value: Variant): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения (тип Variant по RTTI) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;             // сообщение\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // создать сообщение по имени\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas NewMessageEx *** NewMessageEx — Создание расширенного сообщения **** function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения ( Variant , по PluginAPI_desc.odt / RTTI) - Blob: Variant — двоичные данные сообщения ( IBlob / TIBlob , по PluginAPI_desc.odt) - CurrentUser: Variant — пользователь, от имени которого создаётся сообщение ( IUser / TIUser , по TLB) - Timeout: Integer — таймаут сообщения в миллисекундах (по RTTI / TLB) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas PostMsg *** PostMsg — Асинхронная отправка сообщения **** function PostMsg(Msg: Variant): Boolean Входные параметры: - Msg: Variant — сообщение для асинхронной постановки в очередь (по PluginAPI_desc.odt) Возвращает: по PluginAPI_desc.odt для интерфейса ядра PostMsg возвращает WordBool/False при отсутствии обработчиков; в глобальной RTTI-процедуре procedure PostMsg возвращаемого значения нет Сведения из исходников / ODT: Постановка в очередь выполняется асинхронно; возвращает False , если обработчиков нет. Асинхронная постановка в очередь; в Scripts* — FixPhoto, SaveRecord, Enable (fire-and-forget). Глобальная RTTI-процедура procedure PostMsg не возвращает значение (в отличие от TICoreMessages.PostMsg ). Пример вызова: begin\n  PostMsg(NewMessage(PluginName + '.FixPhoto', '')); // fire-and-forget\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/Скрипты/ScriptsAutoControl SendMsg *** SendMsg — Синхронная отправка сообщения **** function SendMsg(Msg: Variant; Timeout: Integer): Variant Входные параметры: - Msg: Variant — сообщение для синхронной отправки (по PluginAPI_desc.odt) - Timeout: Integer — таймаут ожидания в миллисекундах; -1 — бесконечное ожидание (по PluginAPI_desc.odt) Возвращает: результат обработки (поле Result сообщения) после ожидания до Timeout (по PluginAPI_desc.odt) Сведения из исходников / ODT: Ожидает обработку до Timeout мс; -1 означает бесконечное ожидание. Результат берётся из поля Result сообщения. Синхронная отправка; результат — поле Result сообщения после ожидания. В cmd-скриптах ответ кладут в Msg.Result ; снаружи читают значение SendMsg(...) . Пример вызова: var\n  LMsg, LRes: Variant;\nbegin\n  // ScriptsAutoControl: имя = PluginName + '.Command'\n  LMsg := NewMessage(PluginName + '.FindPassDoc', _ToStr(_ObjEx(['Method', 'RFID', 'Label', RFID])));\n  LRes := SendMsg(LMsg, 2000);\n  DebugLog(_ToStr(LRes));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/Скрипты/ScriptsAutoControl","entryCount":4},{"id":"12-server-zhurnaly","title":"Сервер. Универсальные журналы/справочники","intro":"Универсальные журналы — постоянное хранилище бизнес-данных UniServer (взвешивания, справочники, связанные документы). Скрипт обращается к журналу по имени: GetRecord / GetRecords читают, SetRecord сохраняет документ- Variant . Варианты *NT выполняют изменение без запуска триггеров (антирекурсия в ScriptsJournal); Async* ставят операцию в очередь без ожидания. GetQuery — SQL к SQLite-таблице Journa","html":"<h1>Сервер. Универсальные журналы/справочники</h1>\n<p>Универсальные журналы — постоянное хранилище бизнес-данных UniServer (взвешивания, справочники, связанные документы). Скрипт обращается к журналу по имени: <code>GetRecord</code> / <code>GetRecords</code> читают, <code>SetRecord</code> сохраняет документ-<code>Variant</code>. Варианты <code>*NT</code> выполняют изменение без запуска триггеров (антирекурсия в ScriptsJournal); <code>Async*</code> ставят операцию в очередь без ожидания. <code>GetQuery</code> — SQL к SQLite-таблице <code>Journal</code>; <code>GetQueryEx</code> — к внешней БД. Связи (<code>SetLink*</code> / <code>DeleteLinks*</code>, типы SLAVE/MASTER/LINK) и вложения (<code>SetBlob</code>) дополняют документ. Триггеры <code>On*</code> и процедуры <code>Proc_*</code> (вызов через <code>ExecProc</code>) задают бизнес-логику журнала.</p>\n<h2>Паттерны журналов (ScriptsJournal)</h2>\n<p>Контекст триггеров и процедур журнала (production):</p>\n<table>\n<thead>\n<tr>\n<th>Идентификатор</th>\n<th>Роль</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>JournalName</code></td>\n<td>имя текущего журнала</td>\n</tr>\n<tr>\n<td><code>NewDoc</code> / <code>OldDoc</code></td>\n<td>документ до/после изменения</td>\n</tr>\n<tr>\n<td><code>UpdatedProps</code></td>\n<td>набор изменённых полей; проверка <code>UpdatedProps.Exists('FIELD')</code></td>\n</tr>\n<tr>\n<td><code>Args</code></td>\n<td>аргументы хранимой процедуры</td>\n</tr>\n<tr>\n<td><code>Result</code></td>\n<td>возвращаемое значение триггера/процедуры (<code>OnBefore*</code> обычно <code>Result := True</code>)</td>\n</tr>\n</tbody>\n</table>\n<p><strong>Sync vs Async / NT</strong></p>\n<ul>\n<li><code>*NT</code> — без триггеров (антирекурсия при каскадах).</li>\n<li><code>Async*</code> — не блокировать <code>OnAfterUpdate</code>.</li>\n<li><code>AsyncSetRecord</code> (без NT) — когда нужно, чтобы сработали триггеры (уведомление AutoScale о <code>Doc_Link</code>).</li>\n</ul>\n<p><strong>Связи:</strong> <code>SLAVE</code> (отвес→документ), <code>MASTER</code> (документ→отвес), <code>LINK</code> (отвес↔отвес).</p>\n<p><strong>Хранимые процедуры:</strong> файл <code>Proc_Name.pas</code> → вызов <code>ExecProc(JournalName, 'Name', Args)</code>.</p>\n<p><a id=\"getquery\"></a></p>\n<h3><code>GetQuery</code></h3>\n<p>*** <code>GetQuery</code> — SQL-запрос к журналу ****</p>\n<p><code>function GetQuery(Journal, SQL: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>SQL: String</code> — SQL-запрос; синтаксис SQLite; имя таблицы всегда Journal (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Использует SQLite; имя таблицы журнала всегда <code>Journal</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetQuery('WeighingJournal', 'SELECT MAX(RowID) FROM Journal'))); // SQL к журналу\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getqueryex\"></a></p>\n<h3><code>GetQueryEx</code></h3>\n<p>*** <code>GetQueryEx</code> — SQL-запрос к внешней БД ****</p>\n<p><code>function GetQueryEx(Journal, SQL: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>SQL: String</code> — SQL-запрос к внешней БД; синтаксис внешней БД (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Выполняет запрос к внешней базе данных.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetQueryEx('WeighingJournal', 'SELECT MAX(ID) FROM Weighing'))); // SQL к внешней БД\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getrecord\"></a></p>\n<h3><code>GetRecord</code></h3>\n<p>*** <code>GetRecord</code> — Чтение записи журнала ****</p>\n<p><code>function GetRecord(Journal, Code: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (EventScript ODT; ScriptsJournal)<br>\n- <code>Code: String</code> — идентификатор записи CODE (EventScript ODT; ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Получает запись по идентификатору <code>Code</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  Doc: Variant;\nbegin\n  Doc := GetRecord('WeighingJournal', 'D307EB...205027'); // запись по CODE\n  DebugLog(Doc.NUMB_TS);                                  // поле записи\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getrecords\"></a></p>\n<h3><code>GetRecords</code></h3>\n<p>*** <code>GetRecords</code> — Поиск записей журнала ****</p>\n<p><code>function GetRecords(Journal: String; Args: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (EventScript ODT)<br>\n- <code>Args: Variant</code> — объект: Filter (поля и операторы NotEqual/Range/OR), SortField, SortDesc, FirstRow, MaxRows (ScriptsJournal / ScriptsAutoControl)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>В <code>Args</code> используются поля <code>Filter</code>, <code>SortField</code>, <code>SortDesc</code>, <code>MaxRows</code>.</li>\n<li>В <code>Args</code> используются <code>Filter</code>, <code>SortField</code>, <code>SortDesc</code>, <code>FirstRow</code>, <code>MaxRows</code> (ScriptsJournal / EventScript ODT).</li>\n<li>В <code>Filter</code> поддерживаются равенство полей, операторы <code>NotEqual</code>, <code>Range</code> (с <code>_ArrEx([from, to])</code>), вложенный <code>OR</code> через <code>_ObjEx</code> (ScriptsJournal, ScriptsAutoControl).</li>\n<li>Пустой результат удобно проверять как <code>_ToStr(Docs) &lt;&gt; '[]'</code> и/или <code>Docs._Count &gt; 0</code> (ScriptsJournal).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  Args, Docs, Doc: Variant;\nbegin\n  // ScriptsJournal: NotEqual + Range + FirstRow/MaxRows\n  Args := _ObjEx([\n    'Filter', _ObjEx([\n      'NUMB_TS', 'A999AB45',\n      'CODE', _ObjEx(['NotEqual', 'D307EB...']),\n      'DATETIME_CREATE', _ObjEx(['Range', _ArrEx([\n        DateTimeToIso8601(IncHour(Now(), -48)),\n        DateTimeToIso8601(Now())\n      ])]),\n      'DELETED', 0\n    ]),\n    'SortField', 'DATETIME_CREATE',\n    'SortDesc', True,\n    'FirstRow', 0,\n    'MaxRows', 1\n  ]);\n  Docs := GetRecords(JournalName, Args);\n  if (_ToStr(Docs) &lt;&gt; '[]') and (Docs._Count &gt; 0) then\n    Doc := _Json(_ToStr(Docs.Value(0)));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/Скрипты/ScriptsJournal</code>; <code>Материалы для документации/Скрипты/ScriptsAutoControl</code></p>\n<hr>\n<p><a id=\"setrecord\"></a></p>\n<h3><code>SetRecord</code></h3>\n<p>*** <code>SetRecord</code> — Сохранение записи журнала ****</p>\n<p><code>function SetRecord(Journal: String; Doc: Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (EventScript ODT; ScriptsAutoControl)<br>\n- <code>Doc: Variant</code> — документ записи (JSON-объект); часто содержит CODE (EventScript ODT; ScriptsAutoControl)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Перед отправкой удаляет поле <code>DBID</code> из документа.</li>\n<li>Операция передаётся сообщением <code>Journal.Operation</code> через <code>SendMsg</code>.</li>\n<li>Возвращает код записи; в ScriptsAutoControl результат кладут в <code>Result_CODE</code> / <code>Msg.Result</code>.</li>\n<li>После сохранения часто вызывают <code>SetBlob(Journal, Code, Field, Blob)</code> для фото (ScriptsAutoControl / ScriptsCraneScale).</li>\n<li>Операция в реализации уходит через <code>Journal.Operation</code> / <code>SendMsg</code> (fsCoreScript).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LNewDoc: Variant;\n  LCode: String;\nbegin\n  LNewDoc := _Obj();\n  LNewDoc.CODE := Result_DocId;\n  LNewDoc.NUMB_TS := Result_NUMB_TS;\n  LCode := SetRecord(TrafficJournalName, LNewDoc); // ScriptsAutoControl\n  if not IsEmpty(Result_PHOTO1) then\n    SetBlob(TrafficJournalName, Result_DocId, 'PHOTO1', Result_PHOTO1);\n  Msg.Result := True;\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code>; <code>Материалы для документации/Скрипты/ScriptsAutoControl</code>; <code>Материалы для документации/Скрипты/ScriptsCraneScale</code></p>\n<hr>\n<p><a id=\"setrecordnt\"></a></p>\n<h3><code>SetRecordNT</code></h3>\n<p>*** <code>SetRecordNT</code> — Сохранение без триггеров ****</p>\n<p><code>function SetRecordNT(Journal: String; Doc: Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (EventScript ODT; ScriptsJournal)<br>\n- <code>Doc: Variant</code> — документ записи без запуска триггеров (ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p>результат операции записи; изменение выполняется без запуска триггеров (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Выполняет изменение записи без запуска триггеров.</li>\n<li>Операция передаётся сообщением <code>Journal.Operation</code> через <code>SendMsg</code>.</li>\n<li>Без запуска триггеров журнала — для антирекурсии при каскадных правках (ScriptsJournal).</li>\n<li>Операция передаётся сообщением <code>Journal.Operation</code> через <code>SendMsg</code> (fsCoreScript).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LDocID: String;\nbegin\n  // ScriptsJournal Proc_SetSlaveDoc — без триггеров DocsJournal\n  LDocID := SetRecordNT('DocsJournal', LNewDocument);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"getview\"></a></p>\n<h3><code>GetView</code></h3>\n<p>*** <code>GetView</code> — Данные представления ****</p>\n<p><code>function GetView(Journal, Name: String; Params, Filter: Variant; SortField: String; SortDesc: Boolean; FirstRow, MaxRows: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (EventScript ODT / RTTI)<br>\n- <code>Name: String</code> — имя представления (EventScript ODT / RTTI)<br>\n- <code>Params: Variant</code> — параметры представления (RTTI; уточнение семантики — по конфигурации журнала)<br>\n- <code>Filter: Variant</code> — фильтр представления (RTTI)<br>\n- <code>SortField: String</code> — поле сортировки (RTTI; как в GetRecords)<br>\n- <code>SortDesc: Boolean</code> — сортировка по убыванию (RTTI)<br>\n- <code>FirstRow: Integer</code> — смещение первой строки (RTTI; ScriptsJournal для GetRecords)<br>\n- <code>MaxRows: Integer</code> — максимум строк (RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Получает данные именованного представления журнала.</li>\n<li>В RTTI сигнатура: <code>Params</code>, <code>Filter</code>, <code>SortField</code>, <code>SortDesc</code>, <code>FirstRow</code>, <code>MaxRows</code> (functions.txt).</li>\n<li>В EventScript ODT пример со старой сигнатурой <code>(Args, Params)</code> — ориентироваться на RTTI.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // EventScript ODT (сигнатура в RTTI шире: Params, Filter, SortField, SortDesc, FirstRow, MaxRows)\n  DebugLog(_ToStr(GetView('WeighingJournal', 'Test',\n    _ArrEx(['CODE', '']), Null, '', False, 0, 100)));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code>; <code>Материалы для документации/functions.txt</code></p>\n<hr>\n<p><a id=\"execproc\"></a></p>\n<h3><code>ExecProc</code></h3>\n<p>*** <code>ExecProc</code> — Хранимая процедура журнала ****</p>\n<p><code>function ExecProc(Journal, Name: String; Args: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (ScriptsJournal; EventScript ODT)<br>\n- <code>Name: String</code> — имя процедуры без префикса Proc_ (ScriptsJournal: 'SetSlaveDoc')<br>\n- <code>Args: Variant</code> — аргументы; в ScriptsJournal — объект <code>_ObjEx([...])</code></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Выполняет хранимую процедуру журнала.</li>\n<li>Вызывает хранимую процедуру журнала; имя без префикса файла <code>Proc_</code> (файл <code>Proc_SetSlaveDoc</code> → <code>'SetSlaveDoc'</code>).</li>\n<li>В ScriptsJournal аргументы передают как <code>_ObjEx([...])</code>, не позиционный <code>_ArrEx</code> (пример ODT устарел относительно практики).</li>\n<li>Синхронный вызов из триггеров <code>OnBefore*</code> / процедур; для фона после update — <code>AsyncExecProc</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LDocID: String;\nbegin\n  // ScriptsJournal: имя без Proc_; аргументы — объект\n  LDocID := ExecProc(JournalName, 'SetSlaveDoc',\n    _ObjEx(['CODE', Args.CODE, 'NAME', 'DOC1', 'CAPTION', 'Документ']));\n  DebugLog(LDocID);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"getblobs\"></a></p>\n<h3><code>GetBlobs</code></h3>\n<p>*** <code>GetBlobs</code> — Список вложений записи ****</p>\n<p><code>function GetBlobs(Journal, Code: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (RTTI / Scripts<em>)<br>\n- <code>Code: String</code> — CODE записи (RTTI / Scripts</em>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает вложения (blob) записи журнала.</li>\n<li>Возвращает набор вложений записи журнала по CODE.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetBlobs(TrafficJournalName, Result_DocId))); // все вложения записи\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getblob\"></a></p>\n<h3><code>GetBlob</code></h3>\n<p>*** <code>GetBlob</code> — Чтение вложения ****</p>\n<p><code>function GetBlob(Journal, Code, Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (ScriptsAutoControl / RTTI)<br>\n- <code>Code: String</code> — CODE записи (ScriptsAutoControl / RTTI)<br>\n- <code>Name: String</code> — имя поля вложения, например PHOTO1 (ScriptsAutoControl)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает именованное вложение записи.</li>\n<li>Читает именованное вложение записи по журналу, CODE и имени поля (зеркало <code>SetBlob</code> в ScriptsAutoControl).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // зеркало SetBlob из ScriptsAutoControl\n  Result_PHOTO1 := GetBlob(TrafficJournalName, Result_DocId, 'PHOTO1');\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code>; <code>Материалы для документации/Скрипты/ScriptsAutoControl</code></p>\n<hr>\n<p><a id=\"setblob\"></a></p>\n<h3><code>SetBlob</code></h3>\n<p>*** <code>SetBlob</code> — Запись вложения ****</p>\n<p><code>procedure SetBlob(Journal, Code, Name: String; Blob: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (ScriptsAutoControl)<br>\n- <code>Code: String</code> — CODE записи (ScriptsAutoControl)<br>\n- <code>Name: String</code> — имя поля вложения, например PHOTO1 (ScriptsAutoControl)<br>\n- <code>Blob: Variant</code> — данные вложения; писать после проверки not IsEmpty (ScriptsAutoControl)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Сохраняет именованное вложение записи.</li>\n<li>Пишет именованное вложение записи после <code>SetRecord</code>; перед вызовом проверяют <code>not IsEmpty(Blob)</code> (ScriptsAutoControl).</li>\n<li>Аргументы: журнал, CODE записи, имя поля (например <code>PHOTO1</code>), данные blob.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // ScriptsAutoControl после SetRecord\n  if not IsEmpty(Result_PHOTO1) then\n    SetBlob(TrafficJournalName, Result_DocId, 'PHOTO1', Result_PHOTO1);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsAutoControl</code>; <code>Материалы для документации/Скрипты/ScriptsCraneScale</code></p>\n<hr>\n<p><a id=\"setlink\"></a></p>\n<h3><code>SetLink</code></h3>\n<p>*** <code>SetLink</code> — Создание связи ****</p>\n<p><code>function SetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — журнал исходной записи (ScriptsJournal)<br>\n- <code>Code: String</code> — CODE исходной записи (ScriptsJournal)<br>\n- <code>Name: String</code> — имя/ключ связи, например DOC1 (ScriptsJournal)<br>\n- <code>Caption: String</code> — отображаемое имя связи, например «Документ» (ScriptsJournal)<br>\n- <code>LinkJournal: String</code> — журнал связанной записи (ScriptsJournal)<br>\n- <code>LinkType: String</code> — тип связи: SLAVE / MASTER / LINK (ScriptsJournal)<br>\n- <code>Link: String</code> — CODE связанной записи (ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт связь записи с другой записью/журналом.</li>\n<li>Создаёт связь записи с записью другого (или того же) журнала <strong>с</strong> запуском триггеров.</li>\n<li>В ScriptsJournal чаще <code>SetLinkNT</code>; аргументы те же: <code>LinkType</code> = <code>SLAVE</code> / <code>MASTER</code> / <code>LINK</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // как SetLinkNT (ScriptsJournal), но с запуском триггеров\n  SetLink(JournalName, NewDoc.CODE, 'DOC1', 'Документ',\n    'DocsJournal', 'SLAVE', NewDoc.Doc_Link);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setlinknt\"></a></p>\n<h3><code>SetLinkNT</code></h3>\n<p>*** <code>SetLinkNT</code> — Создание связи без триггеров ****</p>\n<p><code>function SetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — журнал исходной записи (ScriptsJournal)<br>\n- <code>Code: String</code> — CODE исходной записи (ScriptsJournal)<br>\n- <code>Name: String</code> — имя/ключ связи (ScriptsJournal)<br>\n- <code>Caption: String</code> — подпись связи (ScriptsJournal)<br>\n- <code>LinkJournal: String</code> — журнал связанной записи (ScriptsJournal)<br>\n- <code>LinkType: String</code> — SLAVE / MASTER / LINK (ScriptsJournal)<br>\n- <code>Link: String</code> — CODE связанной записи (ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт связь без запуска триггеров.</li>\n<li>Как <code>SetLink</code>, но без запуска триггеров — типичный вызов из <code>OnAfterUpdate</code> (ScriptsJournal).</li>\n<li>Пример: <code>SetLinkNT(JournalName, CODE, 'DOC1', 'Документ', 'DocsJournal', 'SLAVE', Doc_Link)</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // ScriptsJournal: отвес → DocsJournal (SLAVE) и обратно (MASTER)\n  SetLinkNT(JournalName, NewDoc.CODE, 'DOC1', 'Документ',\n    'DocsJournal', 'SLAVE', NewDoc.Doc_Link);\n  SetLinkNT('DocsJournal', NewDoc.Doc_Link,\n    'Weighing' + NewDoc.TYPMASSA, NewDoc.TYPMASSACAPTION,\n    JournalName, 'MASTER', NewDoc.CODE);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"getlink\"></a></p>\n<h3><code>GetLink</code></h3>\n<p>*** <code>GetLink</code> — Чтение связи ****</p>\n<p><code>function GetLink(Journal, Code, Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (RTTI)<br>\n- <code>Code: String</code> — CODE записи (RTTI)<br>\n- <code>Name: String</code> — имя связи (RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает данные связи по имени.</li>\n<li>Возвращает данные связи по имени (<code>Name</code>, как в <code>SetLink*</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetLink(JournalName, NewDoc.CODE, 'DOC1'))); // связь по имени\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code>; <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"getlink-link\"></a></p>\n<h3><code>GetLink_Link</code></h3>\n<p>*** <code>GetLink_Link</code> — Идентификатор связанной записи ****</p>\n<p><code>function GetLink_Link(Journal, Code, Name: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (RTTI)<br>\n- <code>Code: String</code> — CODE записи (RTTI)<br>\n- <code>Name: String</code> — имя связи (RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает строковый идентификатор связи.</li>\n<li>Возвращает строковый CODE связанной записи по имени связи.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(GetLink_Link(JournalName, NewDoc.CODE, 'DOC1')); // CODE связанной записи\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code>; <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"getlinks\"></a></p>\n<h3><code>GetLinks</code></h3>\n<p>*** <code>GetLinks</code> — Список связей ****</p>\n<p><code>function GetLinks(Journal, Code: String; MaxRows: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (RTTI)<br>\n- <code>Code: String</code> — CODE записи (RTTI)<br>\n- <code>MaxRows: Integer</code> — ограничение числа связей (RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает связи записи.</li>\n<li>Возвращает список связей записи (<code>MaxRows</code> ограничивает выборку).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetLinks(JournalName, NewDoc.CODE, 100))); // список связей\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"deletelinks\"></a></p>\n<h3><code>DeleteLinks</code></h3>\n<p>*** <code>DeleteLinks</code> — Удаление связей ****</p>\n<p><code>procedure DeleteLinks(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (RTTI)<br>\n- <code>Code: String</code> — CODE записи или пусто для обратных (ScriptsJournal для NT)<br>\n- <code>Link: String</code> — CODE связанной записи или пусто для всех исходящих (ScriptsJournal для NT)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Удаляет связи записи.</li>\n<li>Удаляет связи записи <strong>с</strong> триггерами; семантика пустых Code/Link — как у <code>DeleteLinksNT</code> (ScriptsJournal).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // как DeleteLinksNT (ScriptsJournal), но с триггерами\n  DeleteLinks(JournalName, NewDoc.CODE, '');\n  DeleteLinks(JournalName, '', NewDoc.CODE);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"undeletelinks\"></a></p>\n<h3><code>UnDeleteLinks</code></h3>\n<p>*** <code>UnDeleteLinks</code> — Восстановление связей ****</p>\n<p><code>procedure UnDeleteLinks(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (RTTI)<br>\n- <code>Code: String</code> — CODE записи (RTTI)<br>\n- <code>Link: String</code> — CODE связанной записи (RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Восстанавливает удалённые связи.</li>\n<li>Восстанавливает удалённые связи записи (с триггерами).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  UnDeleteLinks(JournalName, NewDoc.CODE, ''); // восстановить связи записи\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"deletelinksnt\"></a></p>\n<h3><code>DeleteLinksNT</code></h3>\n<p>*** <code>DeleteLinksNT</code> — Удаление связей без триггеров ****</p>\n<p><code>procedure DeleteLinksNT(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (ScriptsJournal)<br>\n- <code>Code: String</code> — CODE записи; пустая строка — режим «все входящие на Link» (ScriptsJournal)<br>\n- <code>Link: String</code> — CODE связанной записи; пустая строка — все исходящие с Code (ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Удаляет связи без запуска триггеров.</li>\n<li>Удаление связей без триггеров (ScriptsJournal).</li>\n<li><code>DeleteLinksNT(J, CODE, '')</code> — все исходящие с записи; <code>DeleteLinksNT(J, '', CODE)</code> — все входящие на запись; оба аргумента заданы — одна пара.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // ScriptsJournal при пометке DELETED\n  DeleteLinksNT(JournalName, NewDoc.CODE, ''); // все исходящие\n  DeleteLinksNT(JournalName, '', NewDoc.CODE); // все входящие\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"undeletelinksnt\"></a></p>\n<h3><code>UnDeleteLinksNT</code></h3>\n<p>*** <code>UnDeleteLinksNT</code> — Восстановление связей без триггеров ****</p>\n<p><code>procedure UnDeleteLinksNT(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (RTTI)<br>\n- <code>Code: String</code> — CODE записи (RTTI)<br>\n- <code>Link: String</code> — CODE связанной записи (RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Восстанавливает связи без запуска триггеров.</li>\n<li>Восстановление связей без запуска триггеров.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  UnDeleteLinksNT(JournalName, NewDoc.CODE, ''); // без триггеров\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"asyncsetrecord\"></a></p>\n<h3><code>AsyncSetRecord</code></h3>\n<p>*** <code>AsyncSetRecord</code> — Асинхронное сохранение записи ****</p>\n<p><code>procedure AsyncSetRecord(Journal: String; Doc: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (ScriptsJournal)<br>\n- <code>Doc: Variant</code> — документ для асинхронного сохранения с триггерами (ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ставит сохранение записи в очередь сообщений.</li>\n<li>Асинхронное сохранение <strong>с</strong> триггерами — чтобы уведомить связанные плагины (ScriptsJournal <code>SetSlaveDoc</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // ScriptsJournal SetSlaveDoc — с триггерами (уведомить AutoScale)\n  AsyncSetRecord(JournalName, _ObjEx([\n    'CODE', Args.CODE, 'Doc_Link', LDocID, 'Doc_Numb', LSlaveDOCUMENT_NUMBER\n  ]));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"asyncsetrecordnt\"></a></p>\n<h3><code>AsyncSetRecordNT</code></h3>\n<p>*** <code>AsyncSetRecordNT</code> — Асинхронное сохранение без триггеров ****</p>\n<p><code>procedure AsyncSetRecordNT(Journal: String; Doc: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (ScriptsJournal)<br>\n- <code>Doc: Variant</code> — документ для асинхронного сохранения без триггеров (ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Асинхронное сохранение без запуска триггеров.</li>\n<li>Асинхронное сохранение без триггеров — массовые правки пары отвесов без рекурсии (ScriptsJournal).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  AsyncSetRecordNT(JournalName, _ObjEx([\n    'CODE', LMasterLinkDoc.CODE, 'Doc_Link', LDocID, 'Doc_Numb', LSlaveDOCUMENT_NUMBER\n  ]));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"asyncexecproc\"></a></p>\n<h3><code>AsyncExecProc</code></h3>\n<p>*** <code>AsyncExecProc</code> — Асинхронный вызов процедуры ****</p>\n<p><code>procedure AsyncExecProc(Journal, Name: String; Args: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (ScriptsJournal)<br>\n- <code>Name: String</code> — имя процедуры без префикса Proc_ (ScriptsJournal)<br>\n- <code>Args: Variant</code> — аргументы процедуры (обычно <code>_ObjEx</code> / документ, ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ставит выполнение хранимой процедуры в очередь.</li>\n<li>Асинхронный вызов хранимой процедуры (не блокирует <code>OnAfterUpdate</code>, ScriptsJournal).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // ScriptsJournal OnAfterUpdate — не блокировать сохранение\n  if IsPackedGuid(NewDoc.RECORD_LINK) then\n    AsyncExecProc(JournalName, 'UpdateWeighing2', NewDoc);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>\n<p><a id=\"asyncsetlink\"></a></p>\n<h3><code>AsyncSetLink</code></h3>\n<p>*** <code>AsyncSetLink</code> — Асинхронное создание связи ****</p>\n<p><code>procedure AsyncSetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — журнал исходной записи (RTTI / ScriptsJournal)<br>\n- <code>Code: String</code> — CODE исходной записи<br>\n- <code>Name: String</code> — имя связи<br>\n- <code>Caption: String</code> — подпись связи<br>\n- <code>LinkJournal: String</code> — журнал связанной записи<br>\n- <code>LinkType: String</code> — SLAVE / MASTER / LINK (ScriptsJournal)<br>\n- <code>Link: String</code> — CODE связанной записи</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ставит создание связи в очередь сообщений.</li>\n<li>Асинхронное создание связи с триггерами; аргументы как у <code>AsyncSetLinkNT</code> (ScriptsJournal).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // как AsyncSetLinkNT (ScriptsJournal), но с триггерами\n  AsyncSetLink(JournalName, Args.CODE, Args.NAME, Args.CAPTION,\n    'DocsJournal', 'SLAVE', LDocID);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"asyncsetlinknt\"></a></p>\n<h3><code>AsyncSetLinkNT</code></h3>\n<p>*** <code>AsyncSetLinkNT</code> — Асинхронная связь без триггеров ****</p>\n<p><code>procedure AsyncSetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — журнал исходной записи (ScriptsJournal)<br>\n- <code>Code: String</code> — CODE исходной записи (ScriptsJournal)<br>\n- <code>Name: String</code> — имя связи (ScriptsJournal)<br>\n- <code>Caption: String</code> — подпись связи (ScriptsJournal)<br>\n- <code>LinkJournal: String</code> — журнал связанной записи (ScriptsJournal)<br>\n- <code>LinkType: String</code> — SLAVE / MASTER / LINK (ScriptsJournal)<br>\n- <code>Link: String</code> — CODE связанной записи (ScriptsJournal)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Асинхронное создание связи без триггеров.</li>\n<li>Асинхронное создание связи без триггеров (ScriptsJournal <code>Proc_SetSlaveDoc</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  AsyncSetLinkNT(JournalName, Args.CODE, Args.NAME, Args.CAPTION,\n    'DocsJournal', 'SLAVE', LDocID);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/Скрипты/ScriptsJournal</code></p>\n<hr>","text":"Сервер. Универсальные журналы/справочники Универсальные журналы — постоянное хранилище бизнес-данных UniServer (взвешивания, справочники, связанные документы). Скрипт обращается к журналу по имени: GetRecord / GetRecords читают, SetRecord сохраняет документ- Variant . Варианты *NT выполняют изменение без запуска триггеров (антирекурсия в ScriptsJournal); Async* ставят операцию в очередь без ожидания. GetQuery — SQL к SQLite-таблице Journal ; GetQueryEx — к внешней БД. Связи ( SetLink* / DeleteLinks* , типы SLAVE/MASTER/LINK) и вложения ( SetBlob ) дополняют документ. Триггеры On* и процедуры Proc_* (вызов через ExecProc ) задают бизнес-логику журнала. Паттерны журналов (ScriptsJournal) Контекст триггеров и процедур журнала (production): Идентификатор Роль JournalName имя текущего журнала NewDoc / OldDoc документ до/после изменения UpdatedProps набор изменённых полей; проверка UpdatedProps.Exists('FIELD') Args аргументы хранимой процедуры Result возвращаемое значение триггера/процедуры ( OnBefore* обычно Result := True ) Sync vs Async / NT *NT — без триггеров (антирекурсия при каскадах). Async* — не блокировать OnAfterUpdate . AsyncSetRecord (без NT) — когда нужно, чтобы сработали триггеры (уведомление AutoScale о Doc_Link ). Связи: SLAVE (отвес→документ), MASTER (документ→отвес), LINK (отвес↔отвес). Хранимые процедуры: файл Proc_Name.pas → вызов ExecProc(JournalName, 'Name', Args) . GetQuery *** GetQuery — SQL-запрос к журналу **** function GetQuery(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос; синтаксис SQLite; имя таблицы всегда Journal (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Использует SQLite; имя таблицы журнала всегда Journal . Пример вызова: begin\n  DebugLog(_ToStr(GetQuery('WeighingJournal', 'SELECT MAX(RowID) FROM Journal'))); // SQL к журналу\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetQueryEx *** GetQueryEx — SQL-запрос к внешней БД **** function GetQueryEx(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос к внешней БД; синтаксис внешней БД (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Выполняет запрос к внешней базе данных. Пример вызова: begin\n  DebugLog(_ToStr(GetQueryEx('WeighingJournal', 'SELECT MAX(ID) FROM Weighing'))); // SQL к внешней БД\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetRecord *** GetRecord — Чтение записи журнала **** function GetRecord(Journal, Code: String): Variant Входные параметры: - Journal: String — имя журнала (EventScript ODT; ScriptsJournal) - Code: String — идентификатор записи CODE (EventScript ODT; ScriptsJournal) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Получает запись по идентификатору Code . Пример вызова: var\n  Doc: Variant;\nbegin\n  Doc := GetRecord('WeighingJournal', 'D307EB...205027'); // запись по CODE\n  DebugLog(Doc.NUMB_TS);                                  // поле записи\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetRecords *** GetRecords — Поиск записей журнала **** function GetRecords(Journal: String; Args: Variant): Variant Входные параметры: - Journal: String — имя журнала (EventScript ODT) - Args: Variant — объект: Filter (поля и операторы NotEqual/Range/OR), SortField, SortDesc, FirstRow, MaxRows (ScriptsJournal / ScriptsAutoControl) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: В Args используются поля Filter , SortField , SortDesc , MaxRows . В Args используются Filter , SortField , SortDesc , FirstRow , MaxRows (ScriptsJournal / EventScript ODT). В Filter поддерживаются равенство полей, операторы NotEqual , Range (с _ArrEx([from, to]) ), вложенный OR через _ObjEx (ScriptsJournal, ScriptsAutoControl). Пустой результат удобно проверять как _ToStr(Docs) <> '[]' и/или Docs._Count > 0 (ScriptsJournal). Пример вызова: var\n  Args, Docs, Doc: Variant;\nbegin\n  // ScriptsJournal: NotEqual + Range + FirstRow/MaxRows\n  Args := _ObjEx([\n    'Filter', _ObjEx([\n      'NUMB_TS', 'A999AB45',\n      'CODE', _ObjEx(['NotEqual', 'D307EB...']),\n      'DATETIME_CREATE', _ObjEx(['Range', _ArrEx([\n        DateTimeToIso8601(IncHour(Now(), -48)),\n        DateTimeToIso8601(Now())\n      ])]),\n      'DELETED', 0\n    ]),\n    'SortField', 'DATETIME_CREATE',\n    'SortDesc', True,\n    'FirstRow', 0,\n    'MaxRows', 1\n  ]);\n  Docs := GetRecords(JournalName, Args);\n  if (_ToStr(Docs) <> '[]') and (Docs._Count > 0) then\n    Doc := _Json(_To","entryCount":25},{"id":"13-konstruktor-sreda","title":"Конструктор. Переменные среды","intro":"Среда Конструктора внедряет объекты и константы экземпляра плагина ( CoreConfig , Request , Response , PluginName , CurrentState и др. — см. ниже): их не объявляют в списке глобалов SMC. Пользовательские глобальные переменные сценария читают и пишут через GetVarValue / SetVarValue (в production часто обращаются по имени напрямую: Result_CURRENT_MASSA , modeAuto ). FSM: переходы smt.* только вычисл","html":"<h1>Конструктор. Переменные среды, дополнительные функции</h1>\n<p>Среда Конструктора внедряет объекты и константы экземпляра плагина (<code>CoreConfig</code>, <code>Request</code>, <code>Response</code>, <code>PluginName</code>, <code>CurrentState</code> и др. — см. ниже): их не объявляют в списке глобалов SMC. Пользовательские глобальные переменные сценария читают и пишут через <code>GetVarValue</code> / <code>SetVarValue</code> (в production часто обращаются по имени напрямую: <code>Result_CURRENT_MASSA</code>, <code>modeAuto</code>). FSM: переходы <code>smt.*</code> только вычисляют следующее состояние (<code>Result := …</code>); эффекты — в <code>sme.*</code> / <code>cmd.*</code>. Метаданные изменений переменных — в разделе «Функции глобальных переменных».</p>\n<h2>Глобальные переменные среды Конструктора</h2>\n<p>По уточнению тимлида, в скриптах Конструктора среда автоматически предоставляет объекты и константы экземпляра плагина. Их <strong>не объявляют</strong> в списке пользовательских глобалов SMC — они уже есть в контексте выполнения.</p>\n<table>\n<thead>\n<tr>\n<th>Имя</th>\n<th>Тип / вид</th>\n<th>Назначение</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>CoreConfig</code></td>\n<td><code>TICoreConfig</code></td>\n<td>Конфигурация ядра: пути, <code>Values[...]</code>, метаданные сообщений</td>\n</tr>\n<tr>\n<td><code>Request</code></td>\n<td><code>TIWebServerRequest</code></td>\n<td>Текущий HTTP-запрос (в web-скриптах страниц)</td>\n</tr>\n<tr>\n<td><code>Response</code></td>\n<td><code>TIWebServerResponse</code></td>\n<td>Формирование HTTP-ответа (<code>ReturnPage</code>, <code>ReturnText</code>, …)</td>\n</tr>\n<tr>\n<td><code>PluginIndex</code></td>\n<td>индекс экземпляра</td>\n<td>Номер/индекс плагина в среде Конструктора</td>\n</tr>\n<tr>\n<td><code>PluginName</code></td>\n<td>имя экземпляра</td>\n<td>Имя плагина; основа для имён сообщений (<code>PluginName + '.Command'</code>) и ключей конфига</td>\n</tr>\n<tr>\n<td><code>PluginCaption</code></td>\n<td>отображаемое имя</td>\n<td>Подпись плагина (в т.ч. для scope шаблонов)</td>\n</tr>\n<tr>\n<td><code>CurrentState</code></td>\n<td>состояние FSM</td>\n<td>Текущее состояние машины состояний сценария</td>\n</tr>\n<tr>\n<td><code>CurrentStateCaption</code></td>\n<td>подпись состояния</td>\n<td>Отображаемое имя текущего состояния</td>\n</tr>\n</tbody>\n</table>\n<p>Подробности методов <code>CoreConfig</code> / <code>Request</code> / <code>Response</code> в отдельном справочнике интерфейсов пока не публикуются (раздел на рассмотрении); в скриптах используют свойства и методы объектов среды напрямую.</p>\n<p><strong>Примеры из production-скриптов:</strong></p>\n<pre><code class=\"language-pascal\">// Имя команды и конфиг от экземпляра плагина\nResult_OBJECT_NAME := GetConfigValue(PluginName + '.ObjectName');\nLMsg := NewMessage(PluginName + '.GetParameters', Null);\n\n// Запись настройки через объект среды\nCoreConfig.Values[LOpts.Value(i).FullName] := LNewValue;\n\n// Веб-страница: scope + ответ\nLScope.PluginIndex := PluginIndex;\nLScope.PluginName := PluginName;\nLScope.PluginCaption := PluginCaption;\nResponse.ReturnPage('Control', LScope);\n\n// Условие по состоянию FSM\nif CurrentState = 'ReadyWeighing' then\n  ...\n</code></pre>\n<p><em>Источник:</em> уточнение тимлида (глобальные переменные среды Конструктора); подтверждение использования — пакеты <code>ScriptsAutoControl</code>, <code>ScriptsBunkerScale</code>, <code>ScriptsCraneScale</code>.</p>\n<p><a id=\"getvarvalue\"></a></p>\n<h3><code>GetVarValue</code></h3>\n<p>*** <code>GetVarValue</code> — Чтение глобальной переменной ****</p>\n<p><code>function GetVarValue(Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя глобальной переменной Конструктора (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Variant;\nbegin\n  v := GetVarValue('Counter'); // прочитать глобальную переменную плагина\n  DebugLog(v);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setvarvalue\"></a></p>\n<h3><code>SetVarValue</code></h3>\n<p>*** <code>SetVarValue</code> — Запись глобальной переменной ****</p>\n<p><code>procedure SetVarValue(Name: String; Value: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя глобальной переменной Конструктора (по <code>fsCoreScript.pas</code>)<br>\n- <code>Value: Variant</code> — новое значение переменной (по RTTI / <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  SetVarValue('Counter', 1); // записать значение в глобальную переменную\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Конструктор. Переменные среды, дополнительные функции Среда Конструктора внедряет объекты и константы экземпляра плагина ( CoreConfig , Request , Response , PluginName , CurrentState и др. — см. ниже): их не объявляют в списке глобалов SMC. Пользовательские глобальные переменные сценария читают и пишут через GetVarValue / SetVarValue (в production часто обращаются по имени напрямую: Result_CURRENT_MASSA , modeAuto ). FSM: переходы smt.* только вычисляют следующее состояние ( Result := … ); эффекты — в sme.* / cmd.* . Метаданные изменений переменных — в разделе «Функции глобальных переменных». Глобальные переменные среды Конструктора По уточнению тимлида, в скриптах Конструктора среда автоматически предоставляет объекты и константы экземпляра плагина. Их не объявляют в списке пользовательских глобалов SMC — они уже есть в контексте выполнения. Имя Тип / вид Назначение CoreConfig TICoreConfig Конфигурация ядра: пути, Values[...] , метаданные сообщений Request TIWebServerRequest Текущий HTTP-запрос (в web-скриптах страниц) Response TIWebServerResponse Формирование HTTP-ответа ( ReturnPage , ReturnText , …) PluginIndex индекс экземпляра Номер/индекс плагина в среде Конструктора PluginName имя экземпляра Имя плагина; основа для имён сообщений ( PluginName + '.Command' ) и ключей конфига PluginCaption отображаемое имя Подпись плагина (в т.ч. для scope шаблонов) CurrentState состояние FSM Текущее состояние машины состояний сценария CurrentStateCaption подпись состояния Отображаемое имя текущего состояния Подробности методов CoreConfig / Request / Response в отдельном справочнике интерфейсов пока не публикуются (раздел на рассмотрении); в скриптах используют свойства и методы объектов среды напрямую. Примеры из production-скриптов: // Имя команды и конфиг от экземпляра плагина\nResult_OBJECT_NAME := GetConfigValue(PluginName + '.ObjectName');\nLMsg := NewMessage(PluginName + '.GetParameters', Null);\n\n// Запись настройки через объект среды\nCoreConfig.Values[LOpts.Value(i).FullName] := LNewValue;\n\n// Веб-страница: scope + ответ\nLScope.PluginIndex := PluginIndex;\nLScope.PluginName := PluginName;\nLScope.PluginCaption := PluginCaption;\nResponse.ReturnPage('Control', LScope);\n\n// Условие по состоянию FSM\nif CurrentState = 'ReadyWeighing' then\n  ... Источник: уточнение тимлида (глобальные переменные среды Конструктора); подтверждение использования — пакеты ScriptsAutoControl , ScriptsBunkerScale , ScriptsCraneScale . GetVarValue *** GetVarValue — Чтение глобальной переменной **** function GetVarValue(Name: String): Variant Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: var\n  v: Variant;\nbegin\n  v := GetVarValue('Counter'); // прочитать глобальную переменную плагина\n  DebugLog(v);\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas SetVarValue *** SetVarValue — Запись глобальной переменной **** procedure SetVarValue(Name: String; Value: Variant) Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) - Value: Variant — новое значение переменной (по RTTI / fsCoreScript.pas ) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  SetVarValue('Counter', 1); // записать значение в глобальную переменную\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas","entryCount":2}],"entries":[{"id":"tostr","sectionId":"01-osnovnye-funktsii","title":"_ToStr","text":"_ToStr *** _ToStr — Преобразование значения в строку **** function _ToStr(V: Variant; Format: Boolean = False): String Входные параметры: - V: Variant — JSON-объект / JSON-массив или иное значение для преобразования в строку (по EventScript_desc.odt и fsCommon.pas ) - Format: Boolean = False — выполнять ли форматирование (переводы строк, отступы) текста JSON при переводе в строку (по EventScript_desc.odt) Возвращает: строковое представление значения; для JSON — JSON-текст (по EventScript_desc.odt / fsCommon.pas ) Сведения из исходников / ODT: Для JSON-объекта или JSON-массива возвращает его текстовое представление. При Format = True применяет JsonReformat . Для даты формирует ISO 8601 с разделителем T и миллисекундами. Пример вызова: var\n  x: Variant;                // JSON-объект\n  s: String;                 // строковый результат\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку в объект\n  s := _ToStr(x);            // преобразовать объект в строку\n  s := _ToStr(x, True);      // то же с форматированием JSON\n  DebugLog(s);               // записать результат в лог\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"todouble","sectionId":"01-osnovnye-funktsii","title":"ToDouble","text":"ToDouble *** ToDouble — Преобразование в Double **** function ToDouble(V: Variant): Double Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Строковое значение преобразуется после удаления пробелов; остальные значения передаются стандартному преобразованию Variant. Пример вызова: var\n  d: Double;                 // числовой результат\nbegin\n  d := ToDouble('12.5');     // преобразовать строку в Double\n  DebugLog(_ToStr(d));       // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"newpackedguid","sectionId":"01-osnovnye-funktsii","title":"NewPackedGuid","text":"NewPackedGuid *** NewPackedGuid — Создание packed GUID **** function NewPackedGuid: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Генерирует компактный идентификатор GUID. Генерирует компактный GUID; в Scripts* — Result_DocId / CODE операции до SetRecord . Пример вызова: begin\n  Result_DocId := NewPackedGuid; // ScriptsCraneScale sme.Empty_OnExit\nend Источник сведений: Материалы для документации/source/fsCommon.pas ; Материалы для документации/Скрипты/ScriptsBunkerScale ; Материалы для документации/Скрипты/ScriptsCraneScale"},{"id":"ispackedguid","sectionId":"01-osnovnye-funktsii","title":"IsPackedGuid","text":"IsPackedGuid *** IsPackedGuid — Проверка packed GUID **** function IsPackedGuid(Guid: String): Boolean Входные параметры: - Guid: String — проверяемая строка Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Проверяет, что строка соответствует формату packed GUID. Проверка формата packed GUID; в ScriptsJournal — идиома «есть живая ссылка» перед GetRecord / SetLink* . Пример вызова: begin\n  if IsPackedGuid(NewDoc.Doc_Link) then\n    AsyncExecProc(JournalName, 'UpdateSlaveDoc',\n      _ObjEx(['MasterDoc', NewDoc, 'SaveRecord', True]));\nend Источник сведений: Материалы для документации/source/fsCommon.pas ; Материалы для документации/Скрипты/ScriptsJournal"},{"id":"sleep","sectionId":"01-osnovnye-funktsii","title":"Sleep","text":"Sleep *** Sleep — Приостановка выполнения **** procedure Sleep(Milliseconds: Integer) Входные параметры: - Milliseconds: Integer — длительность паузы в миллисекундах; по fsCommon.pas ограничивается диапазоном 0…10000 Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Значение задержки ограничивается диапазоном от 0 до 10000 мс. Пример вызова: begin\n  Sleep(500);                // пауза 500 мс (в реализации ограничено 0…10000)\n  DebugLog('готово');        // продолжение после паузы\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"isempty","sectionId":"01-osnovnye-funktsii","title":"IsEmpty","text":"IsEmpty *** IsEmpty — Проверка пустого значения **** function IsEmpty(V: Variant): Boolean Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Для JSON-массива или объекта проверяется Count = 0 . Для строк проверяется Length = 0 ; для остальных значений используется VarIsEmptyOrNull . Пример вызова: var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой массив\n  if IsEmpty(x) then         // проверить пустоту\n    DebugLog('пусто');       // ветка для пустого значения\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"varisemptyornull","sectionId":"01-osnovnye-funktsii","title":"VarIsEmptyOrNull","text":"VarIsEmptyOrNull *** VarIsEmptyOrNull — Проверка Empty или Null **** function VarIsEmptyOrNull(V: Variant): Boolean Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вызывает VarIsEmptyOrNull для переданного Variant. Пример вызова: var\n  v: Variant;                // Variant без присвоенного значения\nbegin\n  if VarIsEmptyOrNull(v) then // проверка Empty/Null\n    DebugLog('Empty или Null');\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"debuglog","sectionId":"01-osnovnye-funktsii","title":"DebugLog","text":"DebugLog *** DebugLog — Вывод строки в лог файл плагина **** procedure DebugLog(V: Variant) Входные параметры: - V: Variant — параметр для вывода в лог файл (по примеру в functions.txt ) Возвращает: Процедура ничего не возвращает. (по примеру в functions.txt блок «Возвращает:» пуст) Сведения из исходников / ODT: Записывает указанное значение в EventScript.log . Пишет значение в лог плагина ( EventScript.log ). В Scripts* часто: DebugLog('...' + _ToStr(ExceptionMessage)) в except . Пример вызова: var\n  x: String;                 // строка для вывода\nbegin\n  x := '{\"ID\":10}';          // подготовить текст\n  DebugLog(x);               // записать значение в лог файл плагина\nend Источник сведений: Материалы для документации/functions.txt ; Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"quotedstrjson","sectionId":"02-json-obekty","title":"QuotedStrJSON","text":"QuotedStrJSON *** QuotedStrJSON — Строка в кавычках JSON **** function QuotedStrJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Заключает строку в JSON-кавычки. Пример вызова: begin\n  DebugLog(QuotedStrJSON('a\"b')); // строка в JSON-кавычках с экранированием\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"escapejson","sectionId":"02-json-obekty","title":"EscapeJSON","text":"EscapeJSON *** EscapeJSON — Экранирование JSON **** function EscapeJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Экранирует спецсимволы для JSON-строки. Пример вызова: begin\n  DebugLog(EscapeJSON('a\"b')); // экранирование спецсимволов JSON без внешних кавычек\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"arr","sectionId":"02-json-obekty","title":"_Arr","text":"_Arr *** _Arr — Создание JSON-массива **** function _Arr: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-массива (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-массива. Пример вызова: var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой JSON-массив\n  x.Add(10);                 // добавить элемент\n  DebugLog(_ToStr(x));       // вывести массив как строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"arrex","sectionId":"02-json-obekty","title":"_ArrEx","text":"_ArrEx *** _ArrEx — Создание JSON-массива со значениями **** function _ArrEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — массив начальных значений для JSON-массива (по EventScript_desc.odt) Возвращает: JSON-массив с добавленными значениями (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-массив и добавляет переданные значения. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ArrEx([1, 2, 3]);     // создать массив с начальными значениями\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"obj","sectionId":"02-json-obekty","title":"_Obj","text":"_Obj *** _Obj — Создание JSON-объекта **** function _Obj: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-объекта (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-объекта. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Obj();               // создать пустой JSON-объект\n  x.AddValue('ID', 10);      // добавить поле\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"objex","sectionId":"02-json-obekty","title":"_ObjEx","text":"_ObjEx *** _ObjEx — Создание JSON-объекта с парами **** function _ObjEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — пары имя–значение для JSON-объекта (по EventScript_desc.odt) Возвращает: JSON-объект с переданными парами имя–значение (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект из переданных пар имя—значение. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ObjEx(['ID', 10, 'Name', 'X']); // объект из пар имя/значение\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"json","sectionId":"02-json-obekty","title":"_Json","text":"_Json *** _Json — Разбор JSON-строки **** function _Json(S: String): Variant Входные параметры: - S: String — строка описания массива или объекта JSON (по EventScript_desc.odt) Возвращает: JSON-объект или JSON-массив, созданный из строки (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект или JSON-массив из строки описания. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"copy","sectionId":"02-json-obekty","title":"_Copy","text":"_Copy *** _Copy — Копирование значения **** function _Copy(V: Variant): Variant Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: JSON-объект или JSON-массив копируется через его строковое представление; прочие значения возвращаются без такого копирования. Пример вызова: var\n  a, b: Variant;\nbegin\n  a := _Json('{\"ID\":10}');   // исходный объект\n  b := _Copy(a);             // копия JSON-значения\n  DebugLog(_ToStr(b));\nend Источник сведений: Материалы для документации/source/fsCommon.pas JSON-объект / JSON-массив : свойства и методы"},{"id":"json-json-kind","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив._Kind","text":"JSON-объект / JSON-массив._Kind *** JSON-объект / JSON-массив._Kind — Тип JSON-переменной **** property _Kind: TDocVariantKind Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TDocVariantKind (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает dvObject для объекта или dvArray для массива. Пример вызова: begin\n  // Свойство `_Kind` доступно у соответствующего объекта интерфейса.\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-count","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив._Count","text":"JSON-объект / JSON-массив._Count *** JSON-объект / JSON-массив._Count — Количество элементов **** property _Count: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает количество элементов массива или объекта. Пример вызова: begin\n  // Свойство `_Count` доступно у соответствующего объекта интерфейса.\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-name","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив.Name","text":"JSON-объект / JSON-массив.Name *** JSON-объект / JSON-массив.Name — Имя элемента объекта **** function Name(Idx: Integer): String Входные параметры: - Idx: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает имя значения в объекте по индексу. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Name(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-value","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив.Value","text":"JSON-объект / JSON-массив.Value *** JSON-объект / JSON-массив.Value — Значение элемента **** function Value(Idx: Integer): Variant Входные параметры: - Idx: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает или задаёт значение в массиве либо объекте. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Value(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-exists","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив.Exists","text":"JSON-объект / JSON-массив.Exists *** JSON-объект / JSON-массив.Exists — Проверка существования **** function Exists(Name: String): Boolean Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Проверяет имя в объекте или значение в массиве. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Exists(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-add","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив.Add","text":"JSON-объект / JSON-массив.Add *** JSON-объект / JSON-массив.Add — Добавление в массив **** function Add(V: Variant): Integer Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Добавляет значение в массив и возвращает его индекс. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Add(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-addvalue","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив.AddValue","text":"JSON-объект / JSON-массив.AddValue *** JSON-объект / JSON-массив.AddValue — Добавление в объект **** function AddValue(Name: String; V: Variant): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Добавляет имя и значение в объект и возвращает индекс. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := AddValue(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-delete","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив.Delete","text":"JSON-объект / JSON-массив.Delete *** JSON-объект / JSON-массив.Delete — Удаление элемента **** procedure Delete(Idx: Integer) Входные параметры: - Idx: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Удаляет элемент из массива или объекта. Пример вызова: begin\n  // Delete(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-tostring","sectionId":"02-json-obekty","title":"JSON-объект / JSON-массив.ToString","text":"JSON-объект / JSON-массив.ToString *** JSON-объект / JSON-массив.ToString — Преобразование в строку **** function ToString: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует массив или объект в строку. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := ToString(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"quotedstr","sectionId":"03-stroki-i-formatirovanie","title":"QuotedStr","text":"QuotedStr *** QuotedStr — Заключение строки в кавычки **** function QuotedStr(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает строку в кавычках Pascal. Пример вызова: var\n  s: String;                 // результат с кавычками\nbegin\n  s := QuotedStr('abc');     // обернуть строку в кавычки\n  DebugLog(s);               // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"uppercase","sectionId":"03-stroki-i-formatirovanie","title":"UpperCase","text":"UpperCase *** UpperCase — Верхний регистр **** function UpperCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в верхний регистр. Пример вызова: begin\n  DebugLog(UpperCase('abc')); // преобразовать к верхнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"lowercase","sectionId":"03-stroki-i-formatirovanie","title":"LowerCase","text":"LowerCase *** LowerCase — Нижний регистр **** function LowerCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в нижний регистр. Пример вызова: begin\n  DebugLog(LowerCase('ABC')); // преобразовать к нижнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"length","sectionId":"03-stroki-i-formatirovanie","title":"Length","text":"Length *** Length — Длина строки **** function Length(S: String): Integer Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает число символов в строке. Пример вызова: begin\n  DebugLog(_ToStr(Length('abc'))); // длина строки\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"pos","sectionId":"03-stroki-i-formatirovanie","title":"Pos","text":"Pos *** Pos — Поиск подстроки **** function Pos(SubStr: String; Str: String): Integer Входные параметры: - SubStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Str: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используется регистронезависимый поиск PosI . Пример вызова: begin\n  DebugLog(_ToStr(Pos('b', 'abc'))); // позиция подстроки (поиск без учёта регистра)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"stringreplace","sectionId":"03-stroki-i-formatirovanie","title":"StringReplace","text":"StringReplace *** StringReplace — Замена подстрок **** function StringReplace(const S, OldPattern, NewPattern: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - OldPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NewPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используются флаги rfReplaceAll и rfIgnoreCase . Пример вызова: begin\n  DebugLog(StringReplace('a-a', 'a', 'x')); // заменить все вхождения без учёта регистра\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"format","sectionId":"03-stroki-i-formatirovanie","title":"Format","text":"Format *** Format — Форматирование строки **** function Format(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Собирает строку по шаблону и аргументам. Пример вызова: begin\n  DebugLog(Format('%s-%d', ['A', 1])); // сформировать строку по шаблону Format\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"formatutf8","sectionId":"03-stroki-i-formatirovanie","title":"FormatUtf8","text":"FormatUtf8 *** FormatUtf8 — Форматирование UTF-8 **** function FormatUtf8(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Форматирует строку с учётом UTF-8. Пример вызова: begin\n  DebugLog(FormatUtf8('%s', ['тест'])); // форматирование через FormatUtf8\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"formatjson","sectionId":"03-stroki-i-formatirovanie","title":"FormatJson","text":"FormatJson *** FormatJson — Форматирование для JSON **** function FormatJson(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Форматирует строку для использования в JSON-контексте. Пример вызова: begin\n  DebugLog(FormatJson('{\"n\":?}', ['x'])); // форматирование с JSON-экранированием аргументов\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"formatfloat","sectionId":"03-stroki-i-formatirovanie","title":"FormatFloat","text":"FormatFloat *** FormatFloat — Форматирование вещественного числа **** function FormatFloat(Fmt: String; Value: Double): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Форматирует Double по заданному шаблону. Пример вызова: begin\n  DebugLog(FormatFloat('0.00', 1.5)); // формат вещественного числа\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"setformatprecision","sectionId":"04-format-veschestvennyh-chisel","title":"SetFormatPrecision","text":"SetFormatPrecision *** SetFormatPrecision — Округление по точности **** function SetFormatPrecision(Value: Double; Precision: Integer): Double Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вычисляет round(Value * 10^Precision) / 10^Precision . Округление round(Value * 10^Precision) / 10^Precision (fsCommon). В весовых Scripts любое значение массы перед записью в Result_ / журнал пропускают через SetFormatPrecision(..., Massa_Precision) . Пример вызова: var\n  m: Double;\nbegin\n  // ScriptsBunkerScale / CraneScale\n  m := SetFormatPrecision(Result_CURRENT_MASSA, Massa_Precision);\n  Result_OP_MASSA := m;\nend Источник сведений: Материалы для документации/source/fsCommon.pas ; Материалы для документации/Скрипты/ScriptsBunkerScale ; Материалы для документации/Скрипты/ScriptsCraneScale"},{"id":"setformatroundfromdiscret","sectionId":"04-format-veschestvennyh-chisel","title":"SetFormatRoundFromDiscret","text":"SetFormatRoundFromDiscret *** SetFormatRoundFromDiscret — Округление по дискрету **** function SetFormatRoundFromDiscret(Value: double; Precision: integer; Discret: double): Double Входные параметры: - Value: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Discret: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Если Discret < 0.00000001 , он заменяется на 0.00000001 . Вычисляет SetFormatPrecision(round(Value / Discret) * Discret, Precision) . Пример вызова: begin\n  DebugLog(_ToStr(SetFormatRoundFromDiscret(1.23, 2, 0.05))); // округление к дискрету\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"iso8601todatetime","sectionId":"05-data-i-vremya","title":"Iso8601ToDateTime","text":"Iso8601ToDateTime *** Iso8601ToDateTime — Разбор ISO 8601 **** function Iso8601ToDateTime(S: String): TDateTime Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует строку даты и времени формата ISO 8601 в TDateTime . Пример вызова: var\n  x: TDateTime;              // дата/время\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать ISO8601\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"datetimetoiso8601","sectionId":"05-data-i-vremya","title":"DateTimeToIso8601","text":"DateTimeToIso8601 *** DateTimeToIso8601 — Форматирование ISO 8601 **** function DateTimeToIso8601(D: TDateTime): String Входные параметры: - D: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует TDateTime в строку ISO 8601. Пример вызова: var\n  x: TDateTime;              // дата/время\n  s: String;                 // строка ISO8601\nbegin\n  x := NowPrecise;           // текущее время\n  s := DateTimeToIso8601(x); // преобразовать в ISO8601\n  DebugLog(s);               // вывести строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"secondsbetween","sectionId":"05-data-i-vremya","title":"SecondsBetween","text":"SecondsBetween *** SecondsBetween — Разница в секундах **** function SecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество секунд между двумя датами. Пример вызова: var\n  x: TDateTime;              // опорная дата\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать дату\n  DebugLog(_ToStr(SecondsBetween(NowPrecise, x))); // разница в секундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"millisecondsbetween","sectionId":"05-data-i-vremya","title":"MillisecondsBetween","text":"MillisecondsBetween *** MillisecondsBetween — Разница в миллисекундах **** function MillisecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество миллисекунд между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MillisecondsBetween(NowPrecise, x))); // разница в миллисекундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"minutesbetween","sectionId":"05-data-i-vremya","title":"MinutesBetween","text":"MinutesBetween *** MinutesBetween — Разница в минутах **** function MinutesBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество минут между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MinutesBetween(NowPrecise, x))); // разница в минутах\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hoursbetween","sectionId":"05-data-i-vremya","title":"HoursBetween","text":"HoursBetween *** HoursBetween — Разница в часах **** function HoursBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество часов между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(HoursBetween(NowPrecise, x))); // разница в часах\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"nowprecise","sectionId":"05-data-i-vremya","title":"NowPrecise","text":"NowPrecise *** NowPrecise — Текущее локальное время **** function NowPrecise: TDateTime Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает текущее локальное время с высокой точностью. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(NowPrecise)); // текущее локальное время\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"nowutcprecise","sectionId":"05-data-i-vremya","title":"NowUTCPrecise","text":"NowUTCPrecise *** NowUTCPrecise — Текущее время UTC **** function NowUTCPrecise: TDateTime Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает текущее время UTC с высокой точностью. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(NowUTCPrecise)); // текущее UTC-время\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"inchour","sectionId":"05-data-i-vremya","title":"IncHour","text":"IncHour *** IncHour — Увеличение часов **** function IncHour(const Value: TDateTime; const NumberOfHours: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfHours: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Сдвигает TDateTime на заданное число часов. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncHour(NowPrecise, 1))); // сдвиг на 1 час\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incday","sectionId":"05-data-i-vremya","title":"IncDay","text":"IncDay *** IncDay — Увеличение дней **** function IncDay(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Сдвигает TDateTime на заданное число дней. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncDay(NowPrecise, 1))); // сдвиг на 1 день\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incminute","sectionId":"05-data-i-vremya","title":"IncMinute","text":"IncMinute *** IncMinute — Увеличение минут **** function IncMinute(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Несмотря на имя RTTI-параметра NumberOfDays , реализация вызывает DateUtils.IncMinute . Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncMinute(NowPrecise, 1))); // сдвиг на 1 минуту\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incsecond","sectionId":"05-data-i-vremya","title":"IncSecond","text":"IncSecond *** IncSecond — Увеличение секунд **** function IncSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Несмотря на имя RTTI-параметра NumberOfDays , реализация вызывает DateUtils.IncSecond . Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncSecond(NowPrecise, 1))); // сдвиг на 1 секунду\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incmillisecond","sectionId":"05-data-i-vremya","title":"IncMilliSecond","text":"IncMilliSecond *** IncMilliSecond — Увеличение миллисекунд **** function IncMilliSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Несмотря на имя RTTI-параметра NumberOfDays , реализация вызывает DateUtils.IncMilliSecond . Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncMilliSecond(NowPrecise, 100))); // сдвиг на 100 мс\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"ticksperiodtotext","sectionId":"05-data-i-vremya","title":"TicksPeriodToText","text":"TicksPeriodToText *** TicksPeriodToText — Период тиков в текст **** function TicksPeriodToText(const ATicks: Integer): String Входные параметры: - ATicks: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует период в тиках в текстовое представление. Пример вызова: begin\n  DebugLog(TicksPeriodToText(1500)); // текстовое представление периода в тиках\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"bytetohex","sectionId":"06-predstavlenie-hex","title":"ByteToHex","text":"ByteToHex *** ByteToHex — Byte в hex **** function ByteToHex(Value: Byte): String Входные параметры: - Value: Byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 2 символа. Пример вызова: begin\n  DebugLog(ByteToHex(255)); // Byte -> hex (ширина 2)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"wordtohex","sectionId":"06-predstavlenie-hex","title":"WordToHex","text":"WordToHex *** WordToHex — Word в hex **** function WordToHex(Value: Word): String Входные параметры: - Value: Word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 4 символа. Пример вызова: begin\n  DebugLog(WordToHex(65535)); // Word -> hex (ширина 4)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"inttohex","sectionId":"06-predstavlenie-hex","title":"IntToHex","text":"IntToHex *** IntToHex — Integer в hex **** function IntToHex(Value: Integer): String Входные параметры: - Value: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 8 символов. Пример вызова: begin\n  DebugLog(IntToHex(1)); // Integer -> hex (ширина 8)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"int64tohex","sectionId":"06-predstavlenie-hex","title":"Int64ToHex","text":"Int64ToHex *** Int64ToHex — Int64 в hex **** function Int64ToHex(Value: Int64): String Входные параметры: - Value: Int64 — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 16 символов. Пример вызова: begin\n  DebugLog(Int64ToHex(1)); // Int64 -> hex (ширина 16)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"doubletohex","sectionId":"06-predstavlenie-hex","title":"DoubleToHex","text":"DoubleToHex *** DoubleToHex — Double в hex **** function DoubleToHex(Value: Double): String Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Double в hex-строку. Пример вызова: begin\n  DebugLog(DoubleToHex(1.0)); // Double -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"singletohex","sectionId":"06-predstavlenie-hex","title":"SingleToHex","text":"SingleToHex *** SingleToHex — Single в hex **** function SingleToHex(Value: Single): String Входные параметры: - Value: Single — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Single в hex-строку. Пример вызова: begin\n  DebugLog(SingleToHex(1.0)); // Single -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextobyte","sectionId":"06-predstavlenie-hex","title":"HexToByte","text":"HexToByte *** HexToByte — Hex в Byte **** function HexToByte(Value: String): byte Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа byte (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Byte . Пример вызова: begin\n  DebugLog(_ToStr(HexToByte('FF'))); // hex -> Byte\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextoword","sectionId":"06-predstavlenie-hex","title":"HexToWord","text":"HexToWord *** HexToWord — Hex в Word **** function HexToWord(Value: String): Integer Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в целое (Word/Integer по RTTI). Пример вызова: begin\n  DebugLog(_ToStr(HexToWord('FFFF'))); // hex -> Word\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextolongword","sectionId":"06-predstavlenie-hex","title":"HexToLongWord","text":"HexToLongWord *** HexToLongWord — Hex в LongWord **** function HexToLongWord(Value: String): Word Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Word (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в LongWord (сигнатура RTTI: Word). Пример вызова: begin\n  DebugLog(_ToStr(HexToLongWord('00000001'))); // hex -> LongWord\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextoint","sectionId":"06-predstavlenie-hex","title":"HexToInt","text":"HexToInt *** HexToInt — Hex в Integer **** function HexToInt(Value: String): Integer Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Integer . Пример вызова: begin\n  DebugLog(_ToStr(HexToInt('00000001'))); // hex -> Integer\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextoint64","sectionId":"06-predstavlenie-hex","title":"HexToInt64","text":"HexToInt64 *** HexToInt64 — Hex в Int64 **** function HexToInt64(Value: String): Int64 Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Int64 (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Int64 . Пример вызова: begin\n  DebugLog(_ToStr(HexToInt64('0000000000000001'))); // hex -> Int64\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextodouble","sectionId":"06-predstavlenie-hex","title":"HexToDouble","text":"HexToDouble *** HexToDouble — Hex в Double **** function HexToDouble(Value: String): Double Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Double . Пример вызова: begin\n  DebugLog(_ToStr(HexToDouble(DoubleToHex(1.5)))); // hex -> Double\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextosingle","sectionId":"06-predstavlenie-hex","title":"HexToSingle","text":"HexToSingle *** HexToSingle — Hex в Single **** function HexToSingle(Value: String): Single Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Single (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Single . Пример вызова: begin\n  DebugLog(_ToStr(HexToSingle(SingleToHex(1.5)))); // hex -> Single\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"strtohex","sectionId":"06-predstavlenie-hex","title":"StrToHex","text":"StrToHex *** StrToHex — Преобразование строки в hex **** function StrToHex(S: String; CodePage: Integer): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - CodePage: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Для заданной кодовой страницы используется TSynAnsiConvert . Пример вызова: begin\n  DebugLog(StrToHex('A', 1251)); // строка -> hex с кодовой страницей\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextostr","sectionId":"06-predstavlenie-hex","title":"HexToStr","text":"HexToStr *** HexToStr — Преобразование hex в строку **** function HexToStr(Value: String; CodePage: Integer): String Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - CodePage: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Для заданной кодовой страницы используется TSynAnsiConvert . Пример вызова: begin\n  DebugLog(HexToStr(StrToHex('A', 1251), 1251)); // hex -> строка с кодовой страницей\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc8summod2","sectionId":"07-kontrolnye-summy-crc","title":"HexToCRC8SumMod2","text":"HexToCRC8SumMod2 *** HexToCRC8SumMod2 — CRC8 в hex **** function HexToCRC8SumMod2(SourceStr: String; AIndex, Count: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Count: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8SumMod2('0102', 0, 2, 0)); // CRC8 SumMod2 по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc8sum","sectionId":"07-kontrolnye-summy-crc","title":"HexToCRC8Sum","text":"HexToCRC8Sum *** HexToCRC8Sum — CRC8 в hex **** function HexToCRC8Sum(SourceStr: String; AIndex, ACount: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8Sum('0102', 0, 2, 0)); // CRC8 Sum по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc8polinom","sectionId":"07-kontrolnye-summy-crc","title":"HexToCRC8Polinom","text":"HexToCRC8Polinom *** HexToCRC8Polinom — CRC8 по полиному **** function HexToCRC8Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ReflIn: boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - XorOut: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC8Polinom('0102', 0, 2, 0, 0, False, 0));\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc16polinom","sectionId":"07-kontrolnye-summy-crc","title":"HexToCRC16Polinom","text":"HexToCRC16Polinom *** HexToCRC16Polinom — CRC16 по полиному **** function HexToCRC16Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ReflIn: boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - XorOut: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC16 представлен hex-строкой из 4 символов. Пример вызова: begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC16Polinom('0102', 0, 2, 0, 0, False, 0));\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"stringfromfile","sectionId":"08-fayly","title":"StringFromFile","text":"StringFromFile *** StringFromFile — Чтение текста из файла **** function StringFromFile(FileName: String): String Входные параметры: - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: var\n  s: String;                 // содержимое файла\nbegin\n  s := StringFromFile('data.txt'); // прочитать файл\n  DebugLog(s);                     // вывести содержимое\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"filefromstring","sectionId":"08-fayly","title":"FileFromString","text":"FileFromString *** FileFromString — Запись текста в файл **** procedure FileFromString(Content: String; FileName: String) Входные параметры: - Content: String — текст для записи в файл - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: begin\n  FileFromString('text', 'out.txt'); // записать строку в файл\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getvarmodtime","sectionId":"09-funktsii-globalnyh-peremennyh","title":"GetVarModTime","text":"GetVarModTime *** GetVarModTime — Время изменения глобальной переменной **** function GetVarModTime(Name: String): TDateTime Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Время последнего изменения глобальной переменной; при отсутствии имени — исключение. В ScriptsCraneScale: MillisecondsBetween(GetVarModTime('CurrentState'), Now()) для таймаута авторежима. Пример вызова: var\n  LTimeOut, LLimit: Integer;\nbegin\n  // ScriptsCraneScale wrk.AutoWeighingControl\n  if modeAuto and (CurrentState = 'ReadyWeighing') then begin\n    LTimeOut := MillisecondsBetween(GetVarModTime('CurrentState'), Now());\n    LLimit := GetConfigIntDef(PluginName + '.AutoModeTimeOut', 5) * 1000;\n    if LTimeOut > LLimit then\n      SendMsg(NewMessage(PluginName + '.StartWeighing', ''), 2000);\n  end;\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsCraneScale"},{"id":"getvarmodcount","sectionId":"09-funktsii-globalnyh-peremennyh","title":"GetVarModCount","text":"GetVarModCount *** GetVarModCount — Счётчик изменений глобальной переменной **** function GetVarModCount(Name: String): Integer Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(_ToStr(GetVarModCount('Counter'))); // счётчик изменений\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getvarmodfreq","sectionId":"09-funktsii-globalnyh-peremennyh","title":"GetVarModFreq","text":"GetVarModFreq *** GetVarModFreq — Частота изменений глобальной переменной **** function GetVarModFreq(Name: String): Double Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Частота изменений глобальной переменной; в ScriptsBunkerScale — мониторинг канала массы. Пример вызова: begin\n  // ScriptsBunkerScale: частота обновления канала массы\n  DebugLog(_ToStr(GetVarModFreq('Result_CURRENT_MASSA')));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsBunkerScale"},{"id":"getconfigvalue","sectionId":"10-server-parametry-nastroek","title":"GetConfigValue","text":"GetConfigValue *** GetConfigValue — Прочитать значение параметра настройки по наименованию **** function GetConfigValue(Name: String): Variant Входные параметры: - Name: String — строка с именем параметра настройки (по примеру в functions.txt ) Возвращает: значение параметра настройки (по примеру в functions.txt ) Сведения из исходников / ODT: Читает конфигурацию по имени. Пример вызова: var\n  x: Variant;                // значение настройки\nbegin\n  x := GetConfigValue('Core.ServerID'); // прочитать параметр по имени\n  DebugLog(x);                          // вывести прочитанное значение в лог\nend Источник сведений: Материалы для документации/functions.txt ; Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getconfigfloatdef","sectionId":"10-server-parametry-nastroek","title":"GetConfigFloatDef","text":"GetConfigFloatDef *** GetConfigFloatDef — Чтение Double с значением по умолчанию **** function GetConfigFloatDef(Name: String; ADefValue: Double): Double Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Double;\nbegin\n  v := GetConfigFloatDef('SomeFloat', 0.0); // читать Double с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getconfigintdef","sectionId":"10-server-parametry-nastroek","title":"GetConfigIntDef","text":"GetConfigIntDef *** GetConfigIntDef — Чтение Integer с значением по умолчанию **** function GetConfigIntDef(Name: String; ADefValue: Integer): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Integer;\nbegin\n  v := GetConfigIntDef('SomeInt', 0); // читать Integer с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getconfigbooldef","sectionId":"10-server-parametry-nastroek","title":"GetConfigBoolDef","text":"GetConfigBoolDef *** GetConfigBoolDef — Чтение Boolean с значением по умолчанию **** function GetConfigBoolDef(Name: String; ADefValue: Boolean): Boolean Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Boolean;\nbegin\n  v := GetConfigBoolDef('SomeBool', False); // читать Boolean с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"newmessage","sectionId":"11-server-otpravka-soobscheniy","title":"NewMessage","text":"NewMessage *** NewMessage — Создание сообщения **** function NewMessage(MsgName: String; Value: Variant): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения (тип Variant по RTTI) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;             // сообщение\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // создать сообщение по имени\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"newmessageex","sectionId":"11-server-otpravka-soobscheniy","title":"NewMessageEx","text":"NewMessageEx *** NewMessageEx — Создание расширенного сообщения **** function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения ( Variant , по PluginAPI_desc.odt / RTTI) - Blob: Variant — двоичные данные сообщения ( IBlob / TIBlob , по PluginAPI_desc.odt) - CurrentUser: Variant — пользователь, от имени которого создаётся сообщение ( IUser / TIUser , по TLB) - Timeout: Integer — таймаут сообщения в миллисекундах (по RTTI / TLB) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"postmsg","sectionId":"11-server-otpravka-soobscheniy","title":"PostMsg","text":"PostMsg *** PostMsg — Асинхронная отправка сообщения **** function PostMsg(Msg: Variant): Boolean Входные параметры: - Msg: Variant — сообщение для асинхронной постановки в очередь (по PluginAPI_desc.odt) Возвращает: по PluginAPI_desc.odt для интерфейса ядра PostMsg возвращает WordBool/False при отсутствии обработчиков; в глобальной RTTI-процедуре procedure PostMsg возвращаемого значения нет Сведения из исходников / ODT: Постановка в очередь выполняется асинхронно; возвращает False , если обработчиков нет. Асинхронная постановка в очередь; в Scripts* — FixPhoto, SaveRecord, Enable (fire-and-forget). Глобальная RTTI-процедура procedure PostMsg не возвращает значение (в отличие от TICoreMessages.PostMsg ). Пример вызова: begin\n  PostMsg(NewMessage(PluginName + '.FixPhoto', '')); // fire-and-forget\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/Скрипты/ScriptsAutoControl"},{"id":"sendmsg","sectionId":"11-server-otpravka-soobscheniy","title":"SendMsg","text":"SendMsg *** SendMsg — Синхронная отправка сообщения **** function SendMsg(Msg: Variant; Timeout: Integer): Variant Входные параметры: - Msg: Variant — сообщение для синхронной отправки (по PluginAPI_desc.odt) - Timeout: Integer — таймаут ожидания в миллисекундах; -1 — бесконечное ожидание (по PluginAPI_desc.odt) Возвращает: результат обработки (поле Result сообщения) после ожидания до Timeout (по PluginAPI_desc.odt) Сведения из исходников / ODT: Ожидает обработку до Timeout мс; -1 означает бесконечное ожидание. Результат берётся из поля Result сообщения. Синхронная отправка; результат — поле Result сообщения после ожидания. В cmd-скриптах ответ кладут в Msg.Result ; снаружи читают значение SendMsg(...) . Пример вызова: var\n  LMsg, LRes: Variant;\nbegin\n  // ScriptsAutoControl: имя = PluginName + '.Command'\n  LMsg := NewMessage(PluginName + '.FindPassDoc', _ToStr(_ObjEx(['Method', 'RFID', 'Label', RFID])));\n  LRes := SendMsg(LMsg, 2000);\n  DebugLog(_ToStr(LRes));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/Скрипты/ScriptsAutoControl"},{"id":"getquery","sectionId":"12-server-zhurnaly","title":"GetQuery","text":"GetQuery *** GetQuery — SQL-запрос к журналу **** function GetQuery(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос; синтаксис SQLite; имя таблицы всегда Journal (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Использует SQLite; имя таблицы журнала всегда Journal . Пример вызова: begin\n  DebugLog(_ToStr(GetQuery('WeighingJournal', 'SELECT MAX(RowID) FROM Journal'))); // SQL к журналу\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getqueryex","sectionId":"12-server-zhurnaly","title":"GetQueryEx","text":"GetQueryEx *** GetQueryEx — SQL-запрос к внешней БД **** function GetQueryEx(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос к внешней БД; синтаксис внешней БД (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Выполняет запрос к внешней базе данных. Пример вызова: begin\n  DebugLog(_ToStr(GetQueryEx('WeighingJournal', 'SELECT MAX(ID) FROM Weighing'))); // SQL к внешней БД\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getrecord","sectionId":"12-server-zhurnaly","title":"GetRecord","text":"GetRecord *** GetRecord — Чтение записи журнала **** function GetRecord(Journal, Code: String): Variant Входные параметры: - Journal: String — имя журнала (EventScript ODT; ScriptsJournal) - Code: String — идентификатор записи CODE (EventScript ODT; ScriptsJournal) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Получает запись по идентификатору Code . Пример вызова: var\n  Doc: Variant;\nbegin\n  Doc := GetRecord('WeighingJournal', 'D307EB...205027'); // запись по CODE\n  DebugLog(Doc.NUMB_TS);                                  // поле записи\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getrecords","sectionId":"12-server-zhurnaly","title":"GetRecords","text":"GetRecords *** GetRecords — Поиск записей журнала **** function GetRecords(Journal: String; Args: Variant): Variant Входные параметры: - Journal: String — имя журнала (EventScript ODT) - Args: Variant — объект: Filter (поля и операторы NotEqual/Range/OR), SortField, SortDesc, FirstRow, MaxRows (ScriptsJournal / ScriptsAutoControl) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: В Args используются поля Filter , SortField , SortDesc , MaxRows . В Args используются Filter , SortField , SortDesc , FirstRow , MaxRows (ScriptsJournal / EventScript ODT). В Filter поддерживаются равенство полей, операторы NotEqual , Range (с _ArrEx([from, to]) ), вложенный OR через _ObjEx (ScriptsJournal, ScriptsAutoControl). Пустой результат удобно проверять как _ToStr(Docs) <> '[]' и/или Docs._Count > 0 (ScriptsJournal). Пример вызова: var\n  Args, Docs, Doc: Variant;\nbegin\n  // ScriptsJournal: NotEqual + Range + FirstRow/MaxRows\n  Args := _ObjEx([\n    'Filter', _ObjEx([\n      'NUMB_TS', 'A999AB45',\n      'CODE', _ObjEx(['NotEqual', 'D307EB...']),\n      'DATETIME_CREATE', _ObjEx(['Range', _ArrEx([\n        DateTimeToIso8601(IncHour(Now(), -48)),\n        DateTimeToIso8601(Now())\n      ])]),\n      'DELETED', 0\n    ]),\n    'SortField', 'DATETIME_CREATE',\n    'SortDesc', True,\n    'FirstRow', 0,\n    'MaxRows', 1\n  ]);\n  Docs := GetRecords(JournalName, Args);\n  if (_ToStr(Docs) <> '[]') and (Docs._Count > 0) then\n    Doc := _Json(_ToStr(Docs.Value(0)));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/Скрипты/ScriptsJournal ; Материалы для документации/Скрипты/ScriptsAutoControl"},{"id":"setrecord","sectionId":"12-server-zhurnaly","title":"SetRecord","text":"SetRecord *** SetRecord — Сохранение записи журнала **** function SetRecord(Journal: String; Doc: Variant): String Входные параметры: - Journal: String — имя журнала (EventScript ODT; ScriptsAutoControl) - Doc: Variant — документ записи (JSON-объект); часто содержит CODE (EventScript ODT; ScriptsAutoControl) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Перед отправкой удаляет поле DBID из документа. Операция передаётся сообщением Journal.Operation через SendMsg . Возвращает код записи; в ScriptsAutoControl результат кладут в Result_CODE / Msg.Result . После сохранения часто вызывают SetBlob(Journal, Code, Field, Blob) для фото (ScriptsAutoControl / ScriptsCraneScale). Операция в реализации уходит через Journal.Operation / SendMsg (fsCoreScript). Пример вызова: var\n  LNewDoc: Variant;\n  LCode: String;\nbegin\n  LNewDoc := _Obj();\n  LNewDoc.CODE := Result_DocId;\n  LNewDoc.NUMB_TS := Result_NUMB_TS;\n  LCode := SetRecord(TrafficJournalName, LNewDoc); // ScriptsAutoControl\n  if not IsEmpty(Result_PHOTO1) then\n    SetBlob(TrafficJournalName, Result_DocId, 'PHOTO1', Result_PHOTO1);\n  Msg.Result := True;\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsAutoControl ; Материалы для документации/Скрипты/ScriptsCraneScale"},{"id":"setrecordnt","sectionId":"12-server-zhurnaly","title":"SetRecordNT","text":"SetRecordNT *** SetRecordNT — Сохранение без триггеров **** function SetRecordNT(Journal: String; Doc: Variant): String Входные параметры: - Journal: String — имя журнала (EventScript ODT; ScriptsJournal) - Doc: Variant — документ записи без запуска триггеров (ScriptsJournal) Возвращает: результат операции записи; изменение выполняется без запуска триггеров (по EventScript_desc.odt) Сведения из исходников / ODT: Выполняет изменение записи без запуска триггеров. Операция передаётся сообщением Journal.Operation через SendMsg . Без запуска триггеров журнала — для антирекурсии при каскадных правках (ScriptsJournal). Операция передаётся сообщением Journal.Operation через SendMsg (fsCoreScript). Пример вызова: var\n  LDocID: String;\nbegin\n  // ScriptsJournal Proc_SetSlaveDoc — без триггеров DocsJournal\n  LDocID := SetRecordNT('DocsJournal', LNewDocument);\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/Скрипты/ScriptsJournal"},{"id":"getview","sectionId":"12-server-zhurnaly","title":"GetView","text":"GetView *** GetView — Данные представления **** function GetView(Journal, Name: String; Params, Filter: Variant; SortField: String; SortDesc: Boolean; FirstRow, MaxRows: Integer): Variant Входные параметры: - Journal: String — имя журнала (EventScript ODT / RTTI) - Name: String — имя представления (EventScript ODT / RTTI) - Params: Variant — параметры представления (RTTI; уточнение семантики — по конфигурации журнала) - Filter: Variant — фильтр представления (RTTI) - SortField: String — поле сортировки (RTTI; как в GetRecords) - SortDesc: Boolean — сортировка по убыванию (RTTI) - FirstRow: Integer — смещение первой строки (RTTI; ScriptsJournal для GetRecords) - MaxRows: Integer — максимум строк (RTTI) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Получает данные именованного представления журнала. В RTTI сигнатура: Params , Filter , SortField , SortDesc , FirstRow , MaxRows (functions.txt). В EventScript ODT пример со старой сигнатурой (Args, Params) — ориентироваться на RTTI. Пример вызова: begin\n  // EventScript ODT (сигнатура в RTTI шире: Params, Filter, SortField, SortDesc, FirstRow, MaxRows)\n  DebugLog(_ToStr(GetView('WeighingJournal', 'Test',\n    _ArrEx(['CODE', '']), Null, '', False, 0, 100)));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/functions.txt"},{"id":"execproc","sectionId":"12-server-zhurnaly","title":"ExecProc","text":"ExecProc *** ExecProc — Хранимая процедура журнала **** function ExecProc(Journal, Name: String; Args: Variant): Variant Входные параметры: - Journal: String — имя журнала (ScriptsJournal; EventScript ODT) - Name: String — имя процедуры без префикса Proc_ (ScriptsJournal: 'SetSlaveDoc') - Args: Variant — аргументы; в ScriptsJournal — объект _ObjEx([...]) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Выполняет хранимую процедуру журнала. Вызывает хранимую процедуру журнала; имя без префикса файла Proc_ (файл Proc_SetSlaveDoc → 'SetSlaveDoc' ). В ScriptsJournal аргументы передают как _ObjEx([...]) , не позиционный _ArrEx (пример ODT устарел относительно практики). Синхронный вызов из триггеров OnBefore* / процедур; для фона после update — AsyncExecProc . Пример вызова: var\n  LDocID: String;\nbegin\n  // ScriptsJournal: имя без Proc_; аргументы — объект\n  LDocID := ExecProc(JournalName, 'SetSlaveDoc',\n    _ObjEx(['CODE', Args.CODE, 'NAME', 'DOC1', 'CAPTION', 'Документ']));\n  DebugLog(LDocID);\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/Скрипты/ScriptsJournal"},{"id":"getblobs","sectionId":"12-server-zhurnaly","title":"GetBlobs","text":"GetBlobs *** GetBlobs — Список вложений записи **** function GetBlobs(Journal, Code: String): Variant Входные параметры: - Journal: String — имя журнала (RTTI / Scripts ) - Code: String — CODE записи (RTTI / Scripts ) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает вложения (blob) записи журнала. Возвращает набор вложений записи журнала по CODE. Пример вызова: begin\n  DebugLog(_ToStr(GetBlobs(TrafficJournalName, Result_DocId))); // все вложения записи\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getblob","sectionId":"12-server-zhurnaly","title":"GetBlob","text":"GetBlob *** GetBlob — Чтение вложения **** function GetBlob(Journal, Code, Name: String): Variant Входные параметры: - Journal: String — имя журнала (ScriptsAutoControl / RTTI) - Code: String — CODE записи (ScriptsAutoControl / RTTI) - Name: String — имя поля вложения, например PHOTO1 (ScriptsAutoControl) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает именованное вложение записи. Читает именованное вложение записи по журналу, CODE и имени поля (зеркало SetBlob в ScriptsAutoControl). Пример вызова: begin\n  // зеркало SetBlob из ScriptsAutoControl\n  Result_PHOTO1 := GetBlob(TrafficJournalName, Result_DocId, 'PHOTO1');\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsAutoControl"},{"id":"setblob","sectionId":"12-server-zhurnaly","title":"SetBlob","text":"SetBlob *** SetBlob — Запись вложения **** procedure SetBlob(Journal, Code, Name: String; Blob: Variant) Входные параметры: - Journal: String — имя журнала (ScriptsAutoControl) - Code: String — CODE записи (ScriptsAutoControl) - Name: String — имя поля вложения, например PHOTO1 (ScriptsAutoControl) - Blob: Variant — данные вложения; писать после проверки not IsEmpty (ScriptsAutoControl) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Сохраняет именованное вложение записи. Пишет именованное вложение записи после SetRecord ; перед вызовом проверяют not IsEmpty(Blob) (ScriptsAutoControl). Аргументы: журнал, CODE записи, имя поля (например PHOTO1 ), данные blob. Пример вызова: begin\n  // ScriptsAutoControl после SetRecord\n  if not IsEmpty(Result_PHOTO1) then\n    SetBlob(TrafficJournalName, Result_DocId, 'PHOTO1', Result_PHOTO1);\nend Источник сведений: Материалы для документации/Скрипты/ScriptsAutoControl ; Материалы для документации/Скрипты/ScriptsCraneScale"},{"id":"setlink","sectionId":"12-server-zhurnaly","title":"SetLink","text":"SetLink *** SetLink — Создание связи **** function SetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String Входные параметры: - Journal: String — журнал исходной записи (ScriptsJournal) - Code: String — CODE исходной записи (ScriptsJournal) - Name: String — имя/ключ связи, например DOC1 (ScriptsJournal) - Caption: String — отображаемое имя связи, например «Документ» (ScriptsJournal) - LinkJournal: String — журнал связанной записи (ScriptsJournal) - LinkType: String — тип связи: SLAVE / MASTER / LINK (ScriptsJournal) - Link: String — CODE связанной записи (ScriptsJournal) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Создаёт связь записи с другой записью/журналом. Создаёт связь записи с записью другого (или того же) журнала с запуском триггеров. В ScriptsJournal чаще SetLinkNT ; аргументы те же: LinkType = SLAVE / MASTER / LINK . Пример вызова: begin\n  // как SetLinkNT (ScriptsJournal), но с запуском триггеров\n  SetLink(JournalName, NewDoc.CODE, 'DOC1', 'Документ',\n    'DocsJournal', 'SLAVE', NewDoc.Doc_Link);\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal ; Материалы для документации/source/fsCoreScript.pas"},{"id":"setlinknt","sectionId":"12-server-zhurnaly","title":"SetLinkNT","text":"SetLinkNT *** SetLinkNT — Создание связи без триггеров **** function SetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String Входные параметры: - Journal: String — журнал исходной записи (ScriptsJournal) - Code: String — CODE исходной записи (ScriptsJournal) - Name: String — имя/ключ связи (ScriptsJournal) - Caption: String — подпись связи (ScriptsJournal) - LinkJournal: String — журнал связанной записи (ScriptsJournal) - LinkType: String — SLAVE / MASTER / LINK (ScriptsJournal) - Link: String — CODE связанной записи (ScriptsJournal) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Создаёт связь без запуска триггеров. Как SetLink , но без запуска триггеров — типичный вызов из OnAfterUpdate (ScriptsJournal). Пример: SetLinkNT(JournalName, CODE, 'DOC1', 'Документ', 'DocsJournal', 'SLAVE', Doc_Link) . Пример вызова: begin\n  // ScriptsJournal: отвес → DocsJournal (SLAVE) и обратно (MASTER)\n  SetLinkNT(JournalName, NewDoc.CODE, 'DOC1', 'Документ',\n    'DocsJournal', 'SLAVE', NewDoc.Doc_Link);\n  SetLinkNT('DocsJournal', NewDoc.Doc_Link,\n    'Weighing' + NewDoc.TYPMASSA, NewDoc.TYPMASSACAPTION,\n    JournalName, 'MASTER', NewDoc.CODE);\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal"},{"id":"getlink","sectionId":"12-server-zhurnaly","title":"GetLink","text":"GetLink *** GetLink — Чтение связи **** function GetLink(Journal, Code, Name: String): Variant Входные параметры: - Journal: String — имя журнала (RTTI) - Code: String — CODE записи (RTTI) - Name: String — имя связи (RTTI) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает данные связи по имени. Возвращает данные связи по имени ( Name , как в SetLink* ). Пример вызова: begin\n  DebugLog(_ToStr(GetLink(JournalName, NewDoc.CODE, 'DOC1'))); // связь по имени\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsJournal"},{"id":"getlink-link","sectionId":"12-server-zhurnaly","title":"GetLink_Link","text":"GetLink_Link *** GetLink_Link — Идентификатор связанной записи **** function GetLink_Link(Journal, Code, Name: String): String Входные параметры: - Journal: String — имя журнала (RTTI) - Code: String — CODE записи (RTTI) - Name: String — имя связи (RTTI) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает строковый идентификатор связи. Возвращает строковый CODE связанной записи по имени связи. Пример вызова: begin\n  DebugLog(GetLink_Link(JournalName, NewDoc.CODE, 'DOC1')); // CODE связанной записи\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas ; Материалы для документации/Скрипты/ScriptsJournal"},{"id":"getlinks","sectionId":"12-server-zhurnaly","title":"GetLinks","text":"GetLinks *** GetLinks — Список связей **** function GetLinks(Journal, Code: String; MaxRows: Integer): Variant Входные параметры: - Journal: String — имя журнала (RTTI) - Code: String — CODE записи (RTTI) - MaxRows: Integer — ограничение числа связей (RTTI) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает связи записи. Возвращает список связей записи ( MaxRows ограничивает выборку). Пример вызова: begin\n  DebugLog(_ToStr(GetLinks(JournalName, NewDoc.CODE, 100))); // список связей\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"deletelinks","sectionId":"12-server-zhurnaly","title":"DeleteLinks","text":"DeleteLinks *** DeleteLinks — Удаление связей **** procedure DeleteLinks(Journal, Code, Link: String) Входные параметры: - Journal: String — имя журнала (RTTI) - Code: String — CODE записи или пусто для обратных (ScriptsJournal для NT) - Link: String — CODE связанной записи или пусто для всех исходящих (ScriptsJournal для NT) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Удаляет связи записи. Удаляет связи записи с триггерами; семантика пустых Code/Link — как у DeleteLinksNT (ScriptsJournal). Пример вызова: begin\n  // как DeleteLinksNT (ScriptsJournal), но с триггерами\n  DeleteLinks(JournalName, NewDoc.CODE, '');\n  DeleteLinks(JournalName, '', NewDoc.CODE);\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal ; Материалы для документации/source/fsCoreScript.pas"},{"id":"undeletelinks","sectionId":"12-server-zhurnaly","title":"UnDeleteLinks","text":"UnDeleteLinks *** UnDeleteLinks — Восстановление связей **** procedure UnDeleteLinks(Journal, Code, Link: String) Входные параметры: - Journal: String — имя журнала (RTTI) - Code: String — CODE записи (RTTI) - Link: String — CODE связанной записи (RTTI) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Восстанавливает удалённые связи. Восстанавливает удалённые связи записи (с триггерами). Пример вызова: begin\n  UnDeleteLinks(JournalName, NewDoc.CODE, ''); // восстановить связи записи\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"deletelinksnt","sectionId":"12-server-zhurnaly","title":"DeleteLinksNT","text":"DeleteLinksNT *** DeleteLinksNT — Удаление связей без триггеров **** procedure DeleteLinksNT(Journal, Code, Link: String) Входные параметры: - Journal: String — имя журнала (ScriptsJournal) - Code: String — CODE записи; пустая строка — режим «все входящие на Link» (ScriptsJournal) - Link: String — CODE связанной записи; пустая строка — все исходящие с Code (ScriptsJournal) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Удаляет связи без запуска триггеров. Удаление связей без триггеров (ScriptsJournal). DeleteLinksNT(J, CODE, '') — все исходящие с записи; DeleteLinksNT(J, '', CODE) — все входящие на запись; оба аргумента заданы — одна пара. Пример вызова: begin\n  // ScriptsJournal при пометке DELETED\n  DeleteLinksNT(JournalName, NewDoc.CODE, ''); // все исходящие\n  DeleteLinksNT(JournalName, '', NewDoc.CODE); // все входящие\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal"},{"id":"undeletelinksnt","sectionId":"12-server-zhurnaly","title":"UnDeleteLinksNT","text":"UnDeleteLinksNT *** UnDeleteLinksNT — Восстановление связей без триггеров **** procedure UnDeleteLinksNT(Journal, Code, Link: String) Входные параметры: - Journal: String — имя журнала (RTTI) - Code: String — CODE записи (RTTI) - Link: String — CODE связанной записи (RTTI) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Восстанавливает связи без запуска триггеров. Восстановление связей без запуска триггеров. Пример вызова: begin\n  UnDeleteLinksNT(JournalName, NewDoc.CODE, ''); // без триггеров\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"asyncsetrecord","sectionId":"12-server-zhurnaly","title":"AsyncSetRecord","text":"AsyncSetRecord *** AsyncSetRecord — Асинхронное сохранение записи **** procedure AsyncSetRecord(Journal: String; Doc: Variant) Входные параметры: - Journal: String — имя журнала (ScriptsJournal) - Doc: Variant — документ для асинхронного сохранения с триггерами (ScriptsJournal) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Ставит сохранение записи в очередь сообщений. Асинхронное сохранение с триггерами — чтобы уведомить связанные плагины (ScriptsJournal SetSlaveDoc ). Пример вызова: begin\n  // ScriptsJournal SetSlaveDoc — с триггерами (уведомить AutoScale)\n  AsyncSetRecord(JournalName, _ObjEx([\n    'CODE', Args.CODE, 'Doc_Link', LDocID, 'Doc_Numb', LSlaveDOCUMENT_NUMBER\n  ]));\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal"},{"id":"asyncsetrecordnt","sectionId":"12-server-zhurnaly","title":"AsyncSetRecordNT","text":"AsyncSetRecordNT *** AsyncSetRecordNT — Асинхронное сохранение без триггеров **** procedure AsyncSetRecordNT(Journal: String; Doc: Variant) Входные параметры: - Journal: String — имя журнала (ScriptsJournal) - Doc: Variant — документ для асинхронного сохранения без триггеров (ScriptsJournal) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Асинхронное сохранение без запуска триггеров. Асинхронное сохранение без триггеров — массовые правки пары отвесов без рекурсии (ScriptsJournal). Пример вызова: begin\n  AsyncSetRecordNT(JournalName, _ObjEx([\n    'CODE', LMasterLinkDoc.CODE, 'Doc_Link', LDocID, 'Doc_Numb', LSlaveDOCUMENT_NUMBER\n  ]));\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal"},{"id":"asyncexecproc","sectionId":"12-server-zhurnaly","title":"AsyncExecProc","text":"AsyncExecProc *** AsyncExecProc — Асинхронный вызов процедуры **** procedure AsyncExecProc(Journal, Name: String; Args: Variant) Входные параметры: - Journal: String — имя журнала (ScriptsJournal) - Name: String — имя процедуры без префикса Proc_ (ScriptsJournal) - Args: Variant — аргументы процедуры (обычно _ObjEx / документ, ScriptsJournal) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Ставит выполнение хранимой процедуры в очередь. Асинхронный вызов хранимой процедуры (не блокирует OnAfterUpdate , ScriptsJournal). Пример вызова: begin\n  // ScriptsJournal OnAfterUpdate — не блокировать сохранение\n  if IsPackedGuid(NewDoc.RECORD_LINK) then\n    AsyncExecProc(JournalName, 'UpdateWeighing2', NewDoc);\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal"},{"id":"asyncsetlink","sectionId":"12-server-zhurnaly","title":"AsyncSetLink","text":"AsyncSetLink *** AsyncSetLink — Асинхронное создание связи **** procedure AsyncSetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String) Входные параметры: - Journal: String — журнал исходной записи (RTTI / ScriptsJournal) - Code: String — CODE исходной записи - Name: String — имя связи - Caption: String — подпись связи - LinkJournal: String — журнал связанной записи - LinkType: String — SLAVE / MASTER / LINK (ScriptsJournal) - Link: String — CODE связанной записи Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Ставит создание связи в очередь сообщений. Асинхронное создание связи с триггерами; аргументы как у AsyncSetLinkNT (ScriptsJournal). Пример вызова: begin\n  // как AsyncSetLinkNT (ScriptsJournal), но с триггерами\n  AsyncSetLink(JournalName, Args.CODE, Args.NAME, Args.CAPTION,\n    'DocsJournal', 'SLAVE', LDocID);\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal ; Материалы для документации/source/fsCoreScript.pas"},{"id":"asyncsetlinknt","sectionId":"12-server-zhurnaly","title":"AsyncSetLinkNT","text":"AsyncSetLinkNT *** AsyncSetLinkNT — Асинхронная связь без триггеров **** procedure AsyncSetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String) Входные параметры: - Journal: String — журнал исходной записи (ScriptsJournal) - Code: String — CODE исходной записи (ScriptsJournal) - Name: String — имя связи (ScriptsJournal) - Caption: String — подпись связи (ScriptsJournal) - LinkJournal: String — журнал связанной записи (ScriptsJournal) - LinkType: String — SLAVE / MASTER / LINK (ScriptsJournal) - Link: String — CODE связанной записи (ScriptsJournal) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Асинхронное создание связи без триггеров. Асинхронное создание связи без триггеров (ScriptsJournal Proc_SetSlaveDoc ). Пример вызова: begin\n  AsyncSetLinkNT(JournalName, Args.CODE, Args.NAME, Args.CAPTION,\n    'DocsJournal', 'SLAVE', LDocID);\nend Источник сведений: Материалы для документации/Скрипты/ScriptsJournal"},{"id":"getvarvalue","sectionId":"13-konstruktor-sreda","title":"GetVarValue","text":"GetVarValue *** GetVarValue — Чтение глобальной переменной **** function GetVarValue(Name: String): Variant Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: var\n  v: Variant;\nbegin\n  v := GetVarValue('Counter'); // прочитать глобальную переменную плагина\n  DebugLog(v);\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"setvarvalue","sectionId":"13-konstruktor-sreda","title":"SetVarValue","text":"SetVarValue *** SetVarValue — Запись глобальной переменной **** procedure SetVarValue(Name: String; Value: Variant) Входные параметры: - Name: String — имя глобальной переменной Конструктора (по fsCoreScript.pas ) - Value: Variant — новое значение переменной (по RTTI / fsCoreScript.pas ) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  SetVarValue('Counter', 1); // записать значение в глобальную переменную\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"}]};
