window.HELP_DATA = {"title":"UniServer Script API","sections":[{"id":"01-obshchie-funktsii","title":"Общие функции","intro":"Раздел объединяет базовые утилиты скриптового API, не привязанные к конкретной подсистеме сервера: безопасные преобразования Variant , проверку пустых значений, компактные GUID и короткую паузу Sleep . В UniServer почти все обменные значения (поля сообщений, параметры страниц, документы журналов, ответы веб-обработчиков) проходят через Variant , поэтому скрипт постоянно приводит данные к строке, ч","html":"<h1>Общие функции</h1>\n<p>Раздел объединяет базовые утилиты скриптового API, не привязанные к конкретной подсистеме сервера: безопасные преобразования <code>Variant</code>, проверку пустых значений, компактные GUID и короткую паузу <code>Sleep</code>. В UniServer почти все обменные значения (поля сообщений, параметры страниц, документы журналов, ответы веб-обработчиков) проходят через <code>Variant</code>, поэтому скрипт постоянно приводит данные к строке, числу или проверяет «пустоту» перед записью в журнал или отправкой сообщения. Функция <code>_ToStr</code> особенно важна для JSON-объектов и массивов (текстовое представление) и для дат (ISO 8601). <code>IsEmpty</code> / <code>VarIsEmptyOrNull</code> задают единый критерий «нет данных» для разных типов. <code>NewPackedGuid</code> / <code>IsPackedGuid</code> дают компактные идентификаторы записей и связей без ручной генерации. Эти вызовы — фундамент остальных разделов справки.</p>\n<p><a id=\"tostr\"></a></p>\n<h3><code>_ToStr</code></h3>\n<p>*** <code>_ToStr</code> — Преобразование значения в строку ****</p>\n<p><code>function _ToStr(V: Variant; Format: Boolean = False): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — JSON-объект / JSON-массив или иное значение для преобразования в строку (по EventScript_desc.odt и <code>fsCommon.pas</code>)<br>\n- <code>Format: Boolean = False</code> — выполнять ли форматирование (переводы строк, отступы) текста JSON при переводе в строку (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>строковое представление значения; для JSON — JSON-текст (по EventScript_desc.odt / <code>fsCommon.pas</code>)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для JSON-объекта или JSON-массива возвращает его текстовое представление.</li>\n<li>При <code>Format = True</code> применяет <code>JsonReformat</code>.</li>\n<li>Для даты формирует ISO 8601 с разделителем <code>T</code> и миллисекундами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // JSON-объект\n  s: String;                 // строковый результат\nbegin\n  x := _Json('{&quot;ID&quot;:10}');   // разобрать JSON-строку в объект\n  s := _ToStr(x);            // преобразовать объект в строку\n  s := _ToStr(x, True);      // то же с форматированием JSON\n  DebugLog(s);               // записать результат в лог\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"todouble\"></a></p>\n<h3><code>ToDouble</code></h3>\n<p>*** <code>ToDouble</code> — Преобразование в <code>Double</code> ****</p>\n<p><code>function ToDouble(V: Variant): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Строковое значение преобразуется после удаления пробелов; остальные значения передаются стандартному преобразованию Variant.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  d: Double;                 // числовой результат\nbegin\n  d := ToDouble('12.5');     // преобразовать строку в Double\n  DebugLog(_ToStr(d));       // вывести результат\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"newpackedguid\"></a></p>\n<h3><code>NewPackedGuid</code></h3>\n<p>*** <code>NewPackedGuid</code> — Создание packed GUID ****</p>\n<p><code>function NewPackedGuid: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Генерирует компактный идентификатор GUID.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  g: String;                 // новый идентификатор\nbegin\n  g := NewPackedGuid;        // создать packed GUID\n  DebugLog(g);               // записать в лог\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"ispackedguid\"></a></p>\n<h3><code>IsPackedGuid</code></h3>\n<p>*** <code>IsPackedGuid</code> — Проверка packed GUID ****</p>\n<p><code>function IsPackedGuid(Guid: String): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Guid: String</code> — проверяемая строка</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Проверяет, что строка соответствует формату packed GUID.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  ok: Boolean;               // результат проверки\nbegin\n  ok := IsPackedGuid(NewPackedGuid); // проверить формат GUID\n  DebugLog(_ToStr(ok));              // вывести результат проверки\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"sleep\"></a></p>\n<h3><code>Sleep</code></h3>\n<p>*** <code>Sleep</code> — Приостановка выполнения ****</p>\n<p><code>procedure Sleep(Milliseconds: Integer)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Milliseconds: Integer</code> — длительность паузы в миллисекундах; по <code>fsCommon.pas</code> ограничивается диапазоном 0…10000</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Значение задержки ограничивается диапазоном от 0 до 10000 мс.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  Sleep(500);                // пауза 500 мс (в реализации ограничено 0…10000)\n  DebugLog('готово');        // продолжение после паузы\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"isempty\"></a></p>\n<h3><code>IsEmpty</code></h3>\n<p>*** <code>IsEmpty</code> — Проверка пустого значения ****</p>\n<p><code>function IsEmpty(V: Variant): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для JSON-массива или объекта проверяется <code>Count = 0</code>.</li>\n<li>Для строк проверяется <code>Length = 0</code>; для остальных значений используется <code>VarIsEmptyOrNull</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой массив\n  if IsEmpty(x) then         // проверить пустоту\n    DebugLog('пусто');       // ветка для пустого значения\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"varisemptyornull\"></a></p>\n<h3><code>VarIsEmptyOrNull</code></h3>\n<p>*** <code>VarIsEmptyOrNull</code> — Проверка Empty или Null ****</p>\n<p><code>function VarIsEmptyOrNull(V: Variant): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вызывает <code>VarIsEmptyOrNull</code> для переданного Variant.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Variant;                // Variant без присвоенного значения\nbegin\n  if VarIsEmptyOrNull(v) then // проверка Empty/Null\n    DebugLog('Empty или Null');\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Общие функции Раздел объединяет базовые утилиты скриптового API, не привязанные к конкретной подсистеме сервера: безопасные преобразования Variant , проверку пустых значений, компактные GUID и короткую паузу Sleep . В UniServer почти все обменные значения (поля сообщений, параметры страниц, документы журналов, ответы веб-обработчиков) проходят через Variant , поэтому скрипт постоянно приводит данные к строке, числу или проверяет «пустоту» перед записью в журнал или отправкой сообщения. Функция _ToStr особенно важна для JSON-объектов и массивов (текстовое представление) и для дат (ISO 8601). IsEmpty / VarIsEmptyOrNull задают единый критерий «нет данных» для разных типов. NewPackedGuid / IsPackedGuid дают компактные идентификаторы записей и связей без ручной генерации. Эти вызовы — фундамент остальных разделов справки. _ToStr *** _ToStr — Преобразование значения в строку **** function _ToStr(V: Variant; Format: Boolean = False): String Входные параметры: - V: Variant — JSON-объект / JSON-массив или иное значение для преобразования в строку (по EventScript_desc.odt и fsCommon.pas ) - Format: Boolean = False — выполнять ли форматирование (переводы строк, отступы) текста JSON при переводе в строку (по EventScript_desc.odt) Возвращает: строковое представление значения; для JSON — JSON-текст (по EventScript_desc.odt / fsCommon.pas ) Сведения из исходников / ODT: Для JSON-объекта или JSON-массива возвращает его текстовое представление. При Format = True применяет JsonReformat . Для даты формирует ISO 8601 с разделителем T и миллисекундами. Пример вызова: var\n  x: Variant;                // JSON-объект\n  s: String;                 // строковый результат\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку в объект\n  s := _ToStr(x);            // преобразовать объект в строку\n  s := _ToStr(x, True);      // то же с форматированием JSON\n  DebugLog(s);               // записать результат в лог\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas ToDouble *** ToDouble — Преобразование в Double **** function ToDouble(V: Variant): Double Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Строковое значение преобразуется после удаления пробелов; остальные значения передаются стандартному преобразованию Variant. Пример вызова: var\n  d: Double;                 // числовой результат\nbegin\n  d := ToDouble('12.5');     // преобразовать строку в Double\n  DebugLog(_ToStr(d));       // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas NewPackedGuid *** NewPackedGuid — Создание packed GUID **** function NewPackedGuid: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Генерирует компактный идентификатор GUID. Пример вызова: var\n  g: String;                 // новый идентификатор\nbegin\n  g := NewPackedGuid;        // создать packed GUID\n  DebugLog(g);               // записать в лог\nend Источник сведений: Материалы для документации/source/fsCommon.pas IsPackedGuid *** IsPackedGuid — Проверка packed GUID **** function IsPackedGuid(Guid: String): Boolean Входные параметры: - Guid: String — проверяемая строка Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Проверяет, что строка соответствует формату packed GUID. Пример вызова: var\n  ok: Boolean;               // результат проверки\nbegin\n  ok := IsPackedGuid(NewPackedGuid); // проверить формат GUID\n  DebugLog(_ToStr(ok));              // вывести результат проверки\nend Источник сведений: Материалы для документации/source/fsCommon.pas Sleep *** Sleep — Приостановка выполнения **** procedure Sleep(Milliseconds: Integer) Входные параметры: - Milliseconds: Integer — длительность паузы в миллисекундах; по fsCommon.pas ограничивается диапазоном 0…10000 Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Значение задержки ограничивается диапазоном от 0 до 10000 мс. Пример вызова: begin\n  Sleep(500);                // пауза 500 мс (в реализации ограничено 0…10000)\n  DebugLog('готово');        // продолжение после паузы\nend Источник сведений: Материалы для документации/source/fsCommon.pas IsEmpty *** IsEmpty — Проверка пустого значения **** function IsEmpty(V: Variant): Boolean Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Для JSON-массива или объекта проверяется Count = 0 . Для строк проверяется Length = 0 ; для остальных значений используется VarIsEmptyOrNull . Пример вызова: var\n  x: Variant;            ","entryCount":7},{"id":"02-rabota-s-faylami","title":"Работа с файлами","intro":"Файловые функции позволяют скрипту читать и записывать текст относительно каталога плагина ( MyPath из конфигурации ядра): относительный путь разрешается именно к нему. На практике так подгружают вспомогательные тексты, фрагменты конфигурации или сохраняют диагностические дампы рядом с DLL плагина, не завязываясь на абсолютные пути сервера. Это не замена универсальным журналам и не хранилище бизне","html":"<h1>Работа с Файлами</h1>\n<p>Файловые функции позволяют скрипту читать и записывать текст относительно каталога плагина (<code>MyPath</code> из конфигурации ядра): относительный путь разрешается именно к нему. На практике так подгружают вспомогательные тексты, фрагменты конфигурации или сохраняют диагностические дампы рядом с DLL плагина, не завязываясь на абсолютные пути сервера. Это не замена универсальным журналам и не хранилище бизнес-документов: для весовых и учётных записей платформа использует журналы и сообщения. Кодировка веб-страниц и шаблонов в UniServer — UTF-8; текстовые файлы, которые затем попадают в HTML или JSON, следует вести в той же кодировке.</p>\n<p><a id=\"stringfromfile\"></a></p>\n<h3><code>StringFromFile</code></h3>\n<p>*** <code>StringFromFile</code> — Чтение текста из файла ****</p>\n<p><code>function StringFromFile(FileName: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>FileName: String</code> — путь к файлу; относительный путь резолвится от MyPath (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Относительный путь разрешается относительно <code>MyPath</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  s: String;                 // содержимое файла\nbegin\n  s := StringFromFile('data.txt'); // прочитать файл\n  DebugLog(s);                     // вывести содержимое\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"filefromstring\"></a></p>\n<h3><code>FileFromString</code></h3>\n<p>*** <code>FileFromString</code> — Запись текста в файл ****</p>\n<p><code>procedure FileFromString(Content: String; FileName: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Content: String</code> — текст для записи в файл<br>\n- <code>FileName: String</code> — путь к файлу; относительный путь резолвится от MyPath (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Относительный путь разрешается относительно <code>MyPath</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  FileFromString('text', 'out.txt'); // записать строку в файл\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Работа с Файлами Файловые функции позволяют скрипту читать и записывать текст относительно каталога плагина ( MyPath из конфигурации ядра): относительный путь разрешается именно к нему. На практике так подгружают вспомогательные тексты, фрагменты конфигурации или сохраняют диагностические дампы рядом с DLL плагина, не завязываясь на абсолютные пути сервера. Это не замена универсальным журналам и не хранилище бизнес-документов: для весовых и учётных записей платформа использует журналы и сообщения. Кодировка веб-страниц и шаблонов в UniServer — UTF-8; текстовые файлы, которые затем попадают в HTML или JSON, следует вести в той же кодировке. StringFromFile *** StringFromFile — Чтение текста из файла **** function StringFromFile(FileName: String): String Входные параметры: - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: var\n  s: String;                 // содержимое файла\nbegin\n  s := StringFromFile('data.txt'); // прочитать файл\n  DebugLog(s);                     // вывести содержимое\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas FileFromString *** FileFromString — Запись текста в файл **** procedure FileFromString(Content: String; FileName: String) Входные параметры: - Content: String — текст для записи в файл - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: begin\n  FileFromString('text', 'out.txt'); // записать строку в файл\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas","entryCount":2},{"id":"03-rabota-so-strokami","title":"Работа со строками","intro":"Строковый слой API закрывает повседневную обработку текста в PascalScript: регистр, длина, поиск, замена и форматирование. Поиск Pos и замена StringReplace выполняются без учёта регистра — это нужно учитывать при сравнении кодов, номеров ТС и имён полей. Семейство Format / FormatUtf8 / FormatJson / FormatFloat собирает строки для логов, HTTP-ответов и JSON: в веб-интерфейсе плагина (как в киоске A","html":"<h1>Работа со строками</h1>\n<p>Строковый слой API закрывает повседневную обработку текста в PascalScript: регистр, длина, поиск, замена и форматирование. Поиск <code>Pos</code> и замена <code>StringReplace</code> выполняются без учёта регистра — это нужно учитывать при сравнении кодов, номеров ТС и имён полей. Семейство <code>Format</code> / <code>FormatUtf8</code> / <code>FormatJson</code> / <code>FormatFloat</code> собирает строки для логов, HTTP-ответов и JSON: в веб-интерфейсе плагина (как в киоске AutoScaleWebKiosk1) страница часто отдаёт JSON или HTML, собранный из параметров и глобальных переменных. Строковые функции не «знают» о сервере — они готовят данные, которые затем уходят в <code>ReturnText</code>, сообщение или поле журнала.</p>\n<p><a id=\"quotedstr\"></a></p>\n<h3><code>QuotedStr</code></h3>\n<p>*** <code>QuotedStr</code> — Заключение строки в кавычки ****</p>\n<p><code>function QuotedStr(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает строку в кавычках Pascal.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  s: String;                 // результат с кавычками\nbegin\n  s := QuotedStr('abc');     // обернуть строку в кавычки\n  DebugLog(s);               // вывести результат\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"uppercase\"></a></p>\n<h3><code>UpperCase</code></h3>\n<p>*** <code>UpperCase</code> — Верхний регистр ****</p>\n<p><code>function UpperCase(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Переводит строку в верхний регистр.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(UpperCase('abc')); // преобразовать к верхнему регистру\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"lowercase\"></a></p>\n<h3><code>LowerCase</code></h3>\n<p>*** <code>LowerCase</code> — Нижний регистр ****</p>\n<p><code>function LowerCase(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Переводит строку в нижний регистр.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(LowerCase('ABC')); // преобразовать к нижнему регистру\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"length\"></a></p>\n<h3><code>Length</code></h3>\n<p>*** <code>Length</code> — Длина строки ****</p>\n<p><code>function Length(S: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает число символов в строке.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(Length('abc'))); // длина строки\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"pos\"></a></p>\n<h3><code>Pos</code></h3>\n<p>*** <code>Pos</code> — Поиск подстроки ****</p>\n<p><code>function Pos(SubStr: String; Str: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SubStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Str: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Используется регистронезависимый поиск <code>PosI</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(Pos('b', 'abc'))); // позиция подстроки (поиск без учёта регистра)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"stringreplace\"></a></p>\n<h3><code>StringReplace</code></h3>\n<p>*** <code>StringReplace</code> — Замена подстрок ****</p>\n<p><code>function StringReplace(const S, OldPattern, NewPattern: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>OldPattern: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NewPattern: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Используются флаги <code>rfReplaceAll</code> и <code>rfIgnoreCase</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(StringReplace('a-a', 'a', 'x')); // заменить все вхождения без учёта регистра\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"format\"></a></p>\n<h3><code>Format</code></h3>\n<p>*** <code>Format</code> — Форматирование строки ****</p>\n<p><code>function Format(Fmt: String; Args: array of Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: array of Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Собирает строку по шаблону и аргументам.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(Format('%s-%d', ['A', 1])); // сформировать строку по шаблону Format\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"formatutf8\"></a></p>\n<h3><code>FormatUtf8</code></h3>\n<p>*** <code>FormatUtf8</code> — Форматирование UTF-8 ****</p>\n<p><code>function FormatUtf8(Fmt: String; Args: array of Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: array of Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Форматирует строку с учётом UTF-8.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(FormatUtf8('%s', ['тест'])); // форматирование через FormatUtf8\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"formatjson\"></a></p>\n<h3><code>FormatJson</code></h3>\n<p>*** <code>FormatJson</code> — Форматирование для JSON ****</p>\n<p><code>function FormatJson(Fmt: String; Args: array of Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: array of Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Форматирует строку для использования в JSON-контексте.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(FormatJson('{&quot;n&quot;:?}', ['x'])); // форматирование с JSON-экранированием аргументов\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"formatfloat\"></a></p>\n<h3><code>FormatFloat</code></h3>\n<p>*** <code>FormatFloat</code> — Форматирование вещественного числа ****</p>\n<p><code>function FormatFloat(Fmt: String; Value: Double): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Fmt: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Value: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Форматирует <code>Double</code> по заданному шаблону.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(FormatFloat('0.00', 1.5)); // формат вещественного числа\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Работа со строками Строковый слой API закрывает повседневную обработку текста в PascalScript: регистр, длина, поиск, замена и форматирование. Поиск Pos и замена StringReplace выполняются без учёта регистра — это нужно учитывать при сравнении кодов, номеров ТС и имён полей. Семейство Format / FormatUtf8 / FormatJson / FormatFloat собирает строки для логов, HTTP-ответов и JSON: в веб-интерфейсе плагина (как в киоске AutoScaleWebKiosk1) страница часто отдаёт JSON или HTML, собранный из параметров и глобальных переменных. Строковые функции не «знают» о сервере — они готовят данные, которые затем уходят в ReturnText , сообщение или поле журнала. QuotedStr *** QuotedStr — Заключение строки в кавычки **** function QuotedStr(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает строку в кавычках Pascal. Пример вызова: var\n  s: String;                 // результат с кавычками\nbegin\n  s := QuotedStr('abc');     // обернуть строку в кавычки\n  DebugLog(s);               // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas UpperCase *** UpperCase — Верхний регистр **** function UpperCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в верхний регистр. Пример вызова: begin\n  DebugLog(UpperCase('abc')); // преобразовать к верхнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas LowerCase *** LowerCase — Нижний регистр **** function LowerCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в нижний регистр. Пример вызова: begin\n  DebugLog(LowerCase('ABC')); // преобразовать к нижнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas Length *** Length — Длина строки **** function Length(S: String): Integer Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает число символов в строке. Пример вызова: begin\n  DebugLog(_ToStr(Length('abc'))); // длина строки\nend Источник сведений: Материалы для документации/source/fsCommon.pas Pos *** Pos — Поиск подстроки **** function Pos(SubStr: String; Str: String): Integer Входные параметры: - SubStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Str: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используется регистронезависимый поиск PosI . Пример вызова: begin\n  DebugLog(_ToStr(Pos('b', 'abc'))); // позиция подстроки (поиск без учёта регистра)\nend Источник сведений: Материалы для документации/source/fsCommon.pas StringReplace *** StringReplace — Замена подстрок **** function StringReplace(const S, OldPattern, NewPattern: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - OldPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NewPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используются флаги rfReplaceAll и rfIgnoreCase . Пример вызова: begin\n  DebugLog(StringReplace('a-a', 'a', 'x')); // заменить все вхождения без учёта регистра\nend Источник сведений: Материалы для документации/source/fsCommon.pas Format *** Format — Форматирование строки **** function Format(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Собирает строку по шаблону и аргументам. Пример вызова: begin\n  DebugLog(Format","entryCount":10},{"id":"04-data-i-vremya","title":"Дата и время","intro":"Время в UniServer — сквозной контракт между плагинами, журналами и веб-страницами. Обменный текстовый формат — ISO 8601 ( Iso8601ToDateTime / DateTimeToIso8601 ): так же оформляются метки сообщений и многие поля документов. Функции интервалов ( SecondsBetween , MillisecondsBetween и др.) измеряют давность события — типичный приём при проверке устаревания или таймаутов в сценариях взвешивания. NowP","html":"<h1>работа с датой и временем</h1>\n<p>Время в UniServer — сквозной контракт между плагинами, журналами и веб-страницами. Обменный текстовый формат — ISO 8601 (<code>Iso8601ToDateTime</code> / <code>DateTimeToIso8601</code>): так же оформляются метки сообщений и многие поля документов. Функции интервалов (<code>SecondsBetween</code>, <code>MillisecondsBetween</code> и др.) измеряют давность события — типичный приём при проверке устаревания или таймаутов в сценариях взвешивания. <code>NowPrecise</code> / <code>NowUTCPrecise</code> дают текущее время с высокой точностью; семейство <code>Inc*</code> сдвигает метку на заданный шаг. Планировщик ядра (<code>TICoreScheduler</code>) оперирует локальным временем компьютера сервера — при сравнении «сейчас» и расписания важно не смешивать локальное и UTC без явного преобразования.</p>\n<p><a id=\"iso8601todatetime\"></a></p>\n<h3><code>Iso8601ToDateTime</code></h3>\n<p>*** <code>Iso8601ToDateTime</code> — Разбор ISO 8601 ****</p>\n<p><code>function Iso8601ToDateTime(S: String): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует строку даты и времени формата ISO 8601 в <code>TDateTime</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;              // дата/время\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать ISO8601\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"datetimetoiso8601\"></a></p>\n<h3><code>DateTimeToIso8601</code></h3>\n<p>*** <code>DateTimeToIso8601</code> — Форматирование ISO 8601 ****</p>\n<p><code>function DateTimeToIso8601(D: TDateTime): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>D: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует <code>TDateTime</code> в строку ISO 8601.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;              // дата/время\n  s: String;                 // строка ISO8601\nbegin\n  x := NowPrecise;           // текущее время\n  s := DateTimeToIso8601(x); // преобразовать в ISO8601\n  DebugLog(s);               // вывести строку\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"secondsbetween\"></a></p>\n<h3><code>SecondsBetween</code></h3>\n<p>*** <code>SecondsBetween</code> — Разница в секундах ****</p>\n<p><code>function SecondsBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество секунд между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;              // опорная дата\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать дату\n  DebugLog(_ToStr(SecondsBetween(NowPrecise, x))); // разница в секундах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"millisecondsbetween\"></a></p>\n<h3><code>MillisecondsBetween</code></h3>\n<p>*** <code>MillisecondsBetween</code> — Разница в миллисекундах ****</p>\n<p><code>function MillisecondsBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество миллисекунд между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MillisecondsBetween(NowPrecise, x))); // разница в миллисекундах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"minutesbetween\"></a></p>\n<h3><code>MinutesBetween</code></h3>\n<p>*** <code>MinutesBetween</code> — Разница в минутах ****</p>\n<p><code>function MinutesBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество минут между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MinutesBetween(NowPrecise, x))); // разница в минутах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hoursbetween\"></a></p>\n<h3><code>HoursBetween</code></h3>\n<p>*** <code>HoursBetween</code> — Разница в часах ****</p>\n<p><code>function HoursBetween(Now, Then: TDateTime): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Now: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Then: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Количество часов между двумя датами.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(HoursBetween(NowPrecise, x))); // разница в часах\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"nowprecise\"></a></p>\n<h3><code>NowPrecise</code></h3>\n<p>*** <code>NowPrecise</code> — Текущее локальное время ****</p>\n<p><code>function NowPrecise: TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает текущее локальное время с высокой точностью.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(NowPrecise)); // текущее локальное время\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"nowutcprecise\"></a></p>\n<h3><code>NowUTCPrecise</code></h3>\n<p>*** <code>NowUTCPrecise</code> — Текущее время UTC ****</p>\n<p><code>function NowUTCPrecise: TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает текущее время UTC с высокой точностью.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(NowUTCPrecise)); // текущее UTC-время\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"inchour\"></a></p>\n<h3><code>IncHour</code></h3>\n<p>*** <code>IncHour</code> — Увеличение часов ****</p>\n<p><code>function IncHour(const Value: TDateTime; const NumberOfHours: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfHours: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Сдвигает <code>TDateTime</code> на заданное число часов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncHour(NowPrecise, 1))); // сдвиг на 1 час\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incday\"></a></p>\n<h3><code>IncDay</code></h3>\n<p>*** <code>IncDay</code> — Увеличение дней ****</p>\n<p><code>function IncDay(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Сдвигает <code>TDateTime</code> на заданное число дней.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncDay(NowPrecise, 1))); // сдвиг на 1 день\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incminute\"></a></p>\n<h3><code>IncMinute</code></h3>\n<p>*** <code>IncMinute</code> — Увеличение минут ****</p>\n<p><code>function IncMinute(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Несмотря на имя RTTI-параметра <code>NumberOfDays</code>, реализация вызывает <code>DateUtils.IncMinute</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncMinute(NowPrecise, 1))); // сдвиг на 1 минуту\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incsecond\"></a></p>\n<h3><code>IncSecond</code></h3>\n<p>*** <code>IncSecond</code> — Увеличение секунд ****</p>\n<p><code>function IncSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Несмотря на имя RTTI-параметра <code>NumberOfDays</code>, реализация вызывает <code>DateUtils.IncSecond</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncSecond(NowPrecise, 1))); // сдвиг на 1 секунду\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"incmillisecond\"></a></p>\n<h3><code>IncMilliSecond</code></h3>\n<p>*** <code>IncMilliSecond</code> — Увеличение миллисекунд ****</p>\n<p><code>function IncMilliSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>NumberOfDays: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Несмотря на имя RTTI-параметра <code>NumberOfDays</code>, реализация вызывает <code>DateUtils.IncMilliSecond</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(IncMilliSecond(NowPrecise, 100))); // сдвиг на 100 мс\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"ticksperiodtotext\"></a></p>\n<h3><code>TicksPeriodToText</code></h3>\n<p>*** <code>TicksPeriodToText</code> — Период тиков в текст ****</p>\n<p><code>function TicksPeriodToText(const ATicks: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>ATicks: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует период в тиках в текстовое представление.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(TicksPeriodToText(1500)); // текстовое представление периода в тиках\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"работа с датой и временем Время в UniServer — сквозной контракт между плагинами, журналами и веб-страницами. Обменный текстовый формат — ISO 8601 ( Iso8601ToDateTime / DateTimeToIso8601 ): так же оформляются метки сообщений и многие поля документов. Функции интервалов ( SecondsBetween , MillisecondsBetween и др.) измеряют давность события — типичный приём при проверке устаревания или таймаутов в сценариях взвешивания. NowPrecise / NowUTCPrecise дают текущее время с высокой точностью; семейство Inc* сдвигает метку на заданный шаг. Планировщик ядра ( TICoreScheduler ) оперирует локальным временем компьютера сервера — при сравнении «сейчас» и расписания важно не смешивать локальное и UTC без явного преобразования. Iso8601ToDateTime *** Iso8601ToDateTime — Разбор ISO 8601 **** function Iso8601ToDateTime(S: String): TDateTime Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует строку даты и времени формата ISO 8601 в TDateTime . Пример вызова: var\n  x: TDateTime;              // дата/время\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать ISO8601\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas DateTimeToIso8601 *** DateTimeToIso8601 — Форматирование ISO 8601 **** function DateTimeToIso8601(D: TDateTime): String Входные параметры: - D: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует TDateTime в строку ISO 8601. Пример вызова: var\n  x: TDateTime;              // дата/время\n  s: String;                 // строка ISO8601\nbegin\n  x := NowPrecise;           // текущее время\n  s := DateTimeToIso8601(x); // преобразовать в ISO8601\n  DebugLog(s);               // вывести строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas SecondsBetween *** SecondsBetween — Разница в секундах **** function SecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество секунд между двумя датами. Пример вызова: var\n  x: TDateTime;              // опорная дата\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать дату\n  DebugLog(_ToStr(SecondsBetween(NowPrecise, x))); // разница в секундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas MillisecondsBetween *** MillisecondsBetween — Разница в миллисекундах **** function MillisecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество миллисекунд между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MillisecondsBetween(NowPrecise, x))); // разница в миллисекундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas MinutesBetween *** MinutesBetween — Разница в минутах **** function MinutesBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество минут между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MinutesBetween(NowPrecise, x))); // разница в минутах\nend Источник сведений: Материалы для документации/source/fsCommon.pas HoursBetween *** HoursBetween — Разница в часах **** function HoursBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания ","entryCount":14},{"id":"05-hex-preobrazovanie","title":"Hex-преобразование","intro":"Hex-преобразования обслуживают обмен с оборудованием и двоичными протоколами: вес, пакеты весов, кадры устройств часто приходят или уходят как hex-строки фиксированной ширины ( ByteToHex , WordToHex , IntToHex и обратные HexTo* ). StrToHex / HexToStr учитывают кодовую страницу через TSynAnsiConvert — это мост между текстовым представлением в скрипте и байтовым содержимым протокола. Раздел тесно св","html":"<h1>Hex преобразованеие чисел</h1>\n<p>Hex-преобразования обслуживают обмен с оборудованием и двоичными протоколами: вес, пакеты весов, кадры устройств часто приходят или уходят как hex-строки фиксированной ширины (<code>ByteToHex</code>, <code>WordToHex</code>, <code>IntToHex</code> и обратные <code>HexTo*</code>). <code>StrToHex</code> / <code>HexToStr</code> учитывают кодовую страницу через <code>TSynAnsiConvert</code> — это мост между текстовым представлением в скрипте и байтовым содержимым протокола. Раздел тесно связан с CRC: контрольные суммы считаются по hex-представлению буфера. Для прикладной логики киоска hex обычно не нужен; он появляется в плагинах, которые разбирают или собирают пакеты устройств.</p>\n<p><a id=\"bytetohex\"></a></p>\n<h3><code>ByteToHex</code></h3>\n<p>*** <code>ByteToHex</code> — Byte в hex ****</p>\n<p><code>function ByteToHex(Value: Byte): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Byte</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 2 символа.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(ByteToHex(255)); // Byte -&gt; hex (ширина 2)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"wordtohex\"></a></p>\n<h3><code>WordToHex</code></h3>\n<p>*** <code>WordToHex</code> — Word в hex ****</p>\n<p><code>function WordToHex(Value: Word): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 4 символа.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(WordToHex(65535)); // Word -&gt; hex (ширина 4)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"inttohex\"></a></p>\n<h3><code>IntToHex</code></h3>\n<p>*** <code>IntToHex</code> — Integer в hex ****</p>\n<p><code>function IntToHex(Value: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 8 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(IntToHex(1)); // Integer -&gt; hex (ширина 8)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"int64tohex\"></a></p>\n<h3><code>Int64ToHex</code></h3>\n<p>*** <code>Int64ToHex</code> — Int64 в hex ****</p>\n<p><code>function Int64ToHex(Value: Int64): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Int64</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает hex-строку фиксированной ширины 16 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(Int64ToHex(1)); // Int64 -&gt; hex (ширина 16)\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"doubletohex\"></a></p>\n<h3><code>DoubleToHex</code></h3>\n<p>*** <code>DoubleToHex</code> — Double в hex ****</p>\n<p><code>function DoubleToHex(Value: Double): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует <code>Double</code> в hex-строку.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DoubleToHex(1.0)); // Double -&gt; hex\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"singletohex\"></a></p>\n<h3><code>SingleToHex</code></h3>\n<p>*** <code>SingleToHex</code> — Single в hex ****</p>\n<p><code>function SingleToHex(Value: Single): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Single</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует <code>Single</code> в hex-строку.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(SingleToHex(1.0)); // Single -&gt; hex\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextobyte\"></a></p>\n<h3><code>HexToByte</code></h3>\n<p>*** <code>HexToByte</code> — Hex в Byte ****</p>\n<p><code>function HexToByte(Value: String): byte</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>byte</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Byte</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToByte('FF'))); // hex -&gt; Byte\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextoword\"></a></p>\n<h3><code>HexToWord</code></h3>\n<p>*** <code>HexToWord</code> — Hex в Word ****</p>\n<p><code>function HexToWord(Value: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в целое (Word/Integer по RTTI).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToWord('FFFF'))); // hex -&gt; Word\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextolongword\"></a></p>\n<h3><code>HexToLongWord</code></h3>\n<p>*** <code>HexToLongWord</code> — Hex в LongWord ****</p>\n<p><code>function HexToLongWord(Value: String): Word</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Word</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в LongWord (сигнатура RTTI: Word).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToLongWord('00000001'))); // hex -&gt; LongWord\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextoint\"></a></p>\n<h3><code>HexToInt</code></h3>\n<p>*** <code>HexToInt</code> — Hex в Integer ****</p>\n<p><code>function HexToInt(Value: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Integer</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToInt('00000001'))); // hex -&gt; Integer\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextoint64\"></a></p>\n<h3><code>HexToInt64</code></h3>\n<p>*** <code>HexToInt64</code> — Hex в Int64 ****</p>\n<p><code>function HexToInt64(Value: String): Int64</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Int64</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Int64</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToInt64('0000000000000001'))); // hex -&gt; Int64\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextodouble\"></a></p>\n<h3><code>HexToDouble</code></h3>\n<p>*** <code>HexToDouble</code> — Hex в Double ****</p>\n<p><code>function HexToDouble(Value: String): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Double</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToDouble(DoubleToHex(1.5)))); // hex -&gt; Double\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextosingle\"></a></p>\n<h3><code>HexToSingle</code></h3>\n<p>*** <code>HexToSingle</code> — Hex в Single ****</p>\n<p><code>function HexToSingle(Value: String): Single</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Single</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует hex-строку в <code>Single</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(HexToSingle(SingleToHex(1.5)))); // hex -&gt; Single\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"strtohex\"></a></p>\n<h3><code>StrToHex</code></h3>\n<p>*** <code>StrToHex</code> — Преобразование строки в hex ****</p>\n<p><code>function StrToHex(S: String; CodePage: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>CodePage: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для заданной кодовой страницы используется <code>TSynAnsiConvert</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(StrToHex('A', 1251)); // строка -&gt; hex с кодовой страницей\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextostr\"></a></p>\n<h3><code>HexToStr</code></h3>\n<p>*** <code>HexToStr</code> — Преобразование hex в строку ****</p>\n<p><code>function HexToStr(Value: String; CodePage: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>CodePage: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Для заданной кодовой страницы используется <code>TSynAnsiConvert</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(HexToStr(StrToHex('A', 1251), 1251)); // hex -&gt; строка с кодовой страницей\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Hex преобразованеие чисел Hex-преобразования обслуживают обмен с оборудованием и двоичными протоколами: вес, пакеты весов, кадры устройств часто приходят или уходят как hex-строки фиксированной ширины ( ByteToHex , WordToHex , IntToHex и обратные HexTo* ). StrToHex / HexToStr учитывают кодовую страницу через TSynAnsiConvert — это мост между текстовым представлением в скрипте и байтовым содержимым протокола. Раздел тесно связан с CRC: контрольные суммы считаются по hex-представлению буфера. Для прикладной логики киоска hex обычно не нужен; он появляется в плагинах, которые разбирают или собирают пакеты устройств. ByteToHex *** ByteToHex — Byte в hex **** function ByteToHex(Value: Byte): String Входные параметры: - Value: Byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 2 символа. Пример вызова: begin\n  DebugLog(ByteToHex(255)); // Byte -> hex (ширина 2)\nend Источник сведений: Материалы для документации/source/fsCommon.pas WordToHex *** WordToHex — Word в hex **** function WordToHex(Value: Word): String Входные параметры: - Value: Word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 4 символа. Пример вызова: begin\n  DebugLog(WordToHex(65535)); // Word -> hex (ширина 4)\nend Источник сведений: Материалы для документации/source/fsCommon.pas IntToHex *** IntToHex — Integer в hex **** function IntToHex(Value: Integer): String Входные параметры: - Value: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 8 символов. Пример вызова: begin\n  DebugLog(IntToHex(1)); // Integer -> hex (ширина 8)\nend Источник сведений: Материалы для документации/source/fsCommon.pas Int64ToHex *** Int64ToHex — Int64 в hex **** function Int64ToHex(Value: Int64): String Входные параметры: - Value: Int64 — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 16 символов. Пример вызова: begin\n  DebugLog(Int64ToHex(1)); // Int64 -> hex (ширина 16)\nend Источник сведений: Материалы для документации/source/fsCommon.pas DoubleToHex *** DoubleToHex — Double в hex **** function DoubleToHex(Value: Double): String Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Double в hex-строку. Пример вызова: begin\n  DebugLog(DoubleToHex(1.0)); // Double -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas SingleToHex *** SingleToHex — Single в hex **** function SingleToHex(Value: Single): String Входные параметры: - Value: Single — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Single в hex-строку. Пример вызова: begin\n  DebugLog(SingleToHex(1.0)); // Single -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToByte *** HexToByte — Hex в Byte **** function HexToByte(Value: String): byte Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа byte (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Byte . Пример вызова: begin\n  DebugLog(_ToStr(HexToByte('FF'))); // hex -> Byte\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToWord *** HexToWord — Hex в Word **** function HexToWord(Value: String): Integer Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в целое (Word/Integer по RTTI). Пример вызова: begin\n  DebugLog(_ToStr(HexToWord('FFFF'))); // hex -> Word\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToLongWord *** HexToLongWord — Hex в LongWord **** function HexToLongWord(Value: String): Word Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в ","entryCount":15},{"id":"06-format-veschestvennyh","title":"Формат вещественных","intro":"Вещественные значения в весовой системе нельзя произвольно округлять: погрешность и дискрет весов задают допустимое представление массы. SetFormatPrecision округляет по числу знаков после запятой; SetFormatRoundFromDiscret сначала приводит значение к сетке дискрета, затем — к заданной точности (при слишком малом дискрете подставляется нижняя граница из реализации). Эти функции применяют перед запи","html":"<h1>Формат представления вещественных чисел</h1>\n<p>Вещественные значения в весовой системе нельзя произвольно округлять: погрешность и дискрет весов задают допустимое представление массы. <code>SetFormatPrecision</code> округляет по числу знаков после запятой; <code>SetFormatRoundFromDiscret</code> сначала приводит значение к сетке дискрета, затем — к заданной точности (при слишком малом дискрете подставляется нижняя граница из реализации). Эти функции применяют перед записью веса в журнал, показом на странице киоска или передачей в сообщение другому плагину, чтобы везде использовалась одна и та же «законная» величина, согласованная с настройками оборудования.</p>\n<p><a id=\"setformatprecision\"></a></p>\n<h3><code>SetFormatPrecision</code></h3>\n<p>*** <code>SetFormatPrecision</code> — Округление по точности ****</p>\n<p><code>function SetFormatPrecision(Value: Double; Precision: Integer): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Precision: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вычисляет <code>round(Value * 10^Precision) / 10^Precision</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(SetFormatPrecision(1.2345, 2))); // округление до 2 знаков\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"setformatroundfromdiscret\"></a></p>\n<h3><code>SetFormatRoundFromDiscret</code></h3>\n<p>*** <code>SetFormatRoundFromDiscret</code> — Округление по дискрету ****</p>\n<p><code>function SetFormatRoundFromDiscret(Value: double; Precision: integer; Discret: double): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Precision: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Discret: double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Если <code>Discret &lt; 0.00000001</code>, он заменяется на <code>0.00000001</code>.</li>\n<li>Вычисляет <code>SetFormatPrecision(round(Value / Discret) * Discret, Precision)</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(SetFormatRoundFromDiscret(1.23, 2, 0.05))); // округление к дискрету\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Формат представления вещественных чисел Вещественные значения в весовой системе нельзя произвольно округлять: погрешность и дискрет весов задают допустимое представление массы. SetFormatPrecision округляет по числу знаков после запятой; SetFormatRoundFromDiscret сначала приводит значение к сетке дискрета, затем — к заданной точности (при слишком малом дискрете подставляется нижняя граница из реализации). Эти функции применяют перед записью веса в журнал, показом на странице киоска или передачей в сообщение другому плагину, чтобы везде использовалась одна и та же «законная» величина, согласованная с настройками оборудования. SetFormatPrecision *** SetFormatPrecision — Округление по точности **** function SetFormatPrecision(Value: Double; Precision: Integer): Double Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вычисляет round(Value * 10^Precision) / 10^Precision . Пример вызова: begin\n  DebugLog(_ToStr(SetFormatPrecision(1.2345, 2))); // округление до 2 знаков\nend Источник сведений: Материалы для документации/source/fsCommon.pas SetFormatRoundFromDiscret *** SetFormatRoundFromDiscret — Округление по дискрету **** function SetFormatRoundFromDiscret(Value: double; Precision: integer; Discret: double): Double Входные параметры: - Value: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Discret: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Если Discret < 0.00000001 , он заменяется на 0.00000001 . Вычисляет SetFormatPrecision(round(Value / Discret) * Discret, Precision) . Пример вызова: begin\n  DebugLog(_ToStr(SetFormatRoundFromDiscret(1.23, 2, 0.05))); // округление к дискрету\nend Источник сведений: Материалы для документации/source/fsCommon.pas","entryCount":2},{"id":"07-crc8-crc16","title":"CRC8 и CRC16","intro":"Контрольные суммы CRC8/CRC16 входят в проверку целостности пакетов устройств: скрипт получает данные как hex-строку, считает сумму по выбранному алгоритму (сумма по модулю 2, простая сумма или полином) и сравнивает с полем контрольной суммы в кадре. Результаты возвращаются снова в hex (2 символа для CRC8, 4 для CRC16). Раздел не описывает конкретный протокол весов — только вычислительный слой; пар","html":"<h1>Функции CRC8 и CRC16</h1>\n<p>Контрольные суммы CRC8/CRC16 входят в проверку целостности пакетов устройств: скрипт получает данные как hex-строку, считает сумму по выбранному алгоритму (сумма по модулю 2, простая сумма или полином) и сравнивает с полем контрольной суммы в кадре. Результаты возвращаются снова в hex (2 символа для CRC8, 4 для CRC16). Раздел не описывает конкретный протокол весов — только вычислительный слой; параметры полинома, инициализации и отражения задаёт документация устройства или плагина оборудования.</p>\n<p><a id=\"hextocrc8summod2\"></a></p>\n<h3><code>HexToCRC8SumMod2</code></h3>\n<p>*** <code>HexToCRC8SumMod2</code> — CRC8 в hex ****</p>\n<p><code>function HexToCRC8SumMod2(SourceStr: String; AIndex, Count: integer; FirstByte: byte): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Count: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>FirstByte: byte</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC8 представлен hex-строкой из 2 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(HexToCRC8SumMod2('0102', 0, 2, 0)); // CRC8 SumMod2 по hex-данным\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextocrc8sum\"></a></p>\n<h3><code>HexToCRC8Sum</code></h3>\n<p>*** <code>HexToCRC8Sum</code> — CRC8 в hex ****</p>\n<p><code>function HexToCRC8Sum(SourceStr: String; AIndex, ACount: integer; FirstByte: byte): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ACount: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>FirstByte: byte</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC8 представлен hex-строкой из 2 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(HexToCRC8Sum('0102', 0, 2, 0)); // CRC8 Sum по hex-данным\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextocrc8polinom\"></a></p>\n<h3><code>HexToCRC8Polinom</code></h3>\n<p>*** <code>HexToCRC8Polinom</code> — CRC8 по полиному ****</p>\n<p><code>function HexToCRC8Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ACount: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Poly: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Init: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ReflIn: boolean</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>XorOut: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC8 представлен hex-строкой из 2 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC8Polinom('0102', 0, 2, 0, 0, False, 0));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"hextocrc16polinom\"></a></p>\n<h3><code>HexToCRC16Polinom</code></h3>\n<p>*** <code>HexToCRC16Polinom</code> — CRC16 по полиному ****</p>\n<p><code>function HexToCRC16Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>SourceStr: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>AIndex: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ACount: integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Poly: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Init: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ReflIn: boolean</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>XorOut: word</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Результат CRC16 представлен hex-строкой из 4 символов.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC16Polinom('0102', 0, 2, 0, 0, False, 0));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>","text":"Функции CRC8 и CRC16 Контрольные суммы CRC8/CRC16 входят в проверку целостности пакетов устройств: скрипт получает данные как hex-строку, считает сумму по выбранному алгоритму (сумма по модулю 2, простая сумма или полином) и сравнивает с полем контрольной суммы в кадре. Результаты возвращаются снова в hex (2 символа для CRC8, 4 для CRC16). Раздел не описывает конкретный протокол весов — только вычислительный слой; параметры полинома, инициализации и отражения задаёт документация устройства или плагина оборудования. HexToCRC8SumMod2 *** HexToCRC8SumMod2 — CRC8 в hex **** function HexToCRC8SumMod2(SourceStr: String; AIndex, Count: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Count: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8SumMod2('0102', 0, 2, 0)); // CRC8 SumMod2 по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToCRC8Sum *** HexToCRC8Sum — CRC8 в hex **** function HexToCRC8Sum(SourceStr: String; AIndex, ACount: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8Sum('0102', 0, 2, 0)); // CRC8 Sum по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToCRC8Polinom *** HexToCRC8Polinom — CRC8 по полиному **** function HexToCRC8Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ReflIn: boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - XorOut: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC8Polinom('0102', 0, 2, 0, 0, False, 0));\nend Источник сведений: Материалы для документации/source/fsCommon.pas HexToCRC16Polinom *** HexToCRC16Polinom — CRC16 по полиному **** function HexToCRC16Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только т","entryCount":4},{"id":"08-tipy-dannyh","title":"Типы данных","intro":"Перечисление TDocVariantKind ( dvUndefined , dvObject , dvArray ) — способ в скрипте отличить JSON-объект от JSON-массива и от неинициализированного значения. Платформа активно использует JSON как носитель структур: аргументы журналов, полезная нагрузка сообщений, параметры шаблонов страниц. Перед обходом элементов через _Count / Name / Value принято читать _Kind и ветвить логику: иначе объект и м","html":"<h1>Типы данных</h1>\n<p>Перечисление <code>TDocVariantKind</code> (<code>dvUndefined</code>, <code>dvObject</code>, <code>dvArray</code>) — способ в скрипте отличить JSON-объект от JSON-массива и от неинициализированного значения. Платформа активно использует JSON как носитель структур: аргументы журналов, полезная нагрузка сообщений, параметры шаблонов страниц. Перед обходом элементов через <code>_Count</code> / <code>Name</code> / <code>Value</code> принято читать <code>_Kind</code> и ветвить логику: иначе объект и массив обрабатываются одинаково ошибочно. Этот тип — ключ к разделу работы с JSON-объектами.</p>\n<p><a id=\"tdocvariantkind\"></a></p>\n<h3><code>TDocVariantKind</code></h3>\n<p>*** <code>TDocVariantKind</code> — Тип JSON-переменной ****</p>\n<p><code>type TDocVariantKind = (dvUndefined, dvObject, dvArray)</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Тип-перечисление с вариантами, указанными в декларации <code>functions.txt</code>.</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Перечисление <code>dvUndefined</code> / <code>dvObject</code> / <code>dvArray</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := TDocVariantKind(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/functions.txt</code></p>\n<hr>","text":"Типы данных Перечисление TDocVariantKind ( dvUndefined , dvObject , dvArray ) — способ в скрипте отличить JSON-объект от JSON-массива и от неинициализированного значения. Платформа активно использует JSON как носитель структур: аргументы журналов, полезная нагрузка сообщений, параметры шаблонов страниц. Перед обходом элементов через _Count / Name / Value принято читать _Kind и ветвить логику: иначе объект и массив обрабатываются одинаково ошибочно. Этот тип — ключ к разделу работы с JSON-объектами. TDocVariantKind *** TDocVariantKind — Тип JSON-переменной **** type TDocVariantKind = (dvUndefined, dvObject, dvArray) Входные параметры: Параметры отсутствуют. Возвращает: Тип-перечисление с вариантами, указанными в декларации functions.txt . Сведения из исходников / ODT: Перечисление dvUndefined / dvObject / dvArray . Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := TDocVariantKind(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/functions.txt","entryCount":1},{"id":"09-json-obekty","title":"JSON-объекты","intro":"JSON — основной структурный формат данных в скриптах UniServer. Сообщения несут Value типа Variant , часто как JSON-объект; фильтры GetRecords , документы SetRecord , параметры ReturnPage и ответы страниц данных собираются через _Obj / _Arr / _Json . Функции _ArrEx / _ObjEx создают структуру сразу со значениями; _Copy копирует JSON через строковое представление. QuotedStrJSON и EscapeJSON готовят ","html":"<h1>работы с JSON объектами</h1>\n<p>JSON — основной структурный формат данных в скриптах UniServer. Сообщения несут <code>Value</code> типа <code>Variant</code>, часто как JSON-объект; фильтры <code>GetRecords</code>, документы <code>SetRecord</code>, параметры <code>ReturnPage</code> и ответы страниц данных собираются через <code>_Obj</code> / <code>_Arr</code> / <code>_Json</code>. Функции <code>_ArrEx</code> / <code>_ObjEx</code> создают структуру сразу со значениями; <code>_Copy</code> копирует JSON через строковое представление. <code>QuotedStrJSON</code> и <code>EscapeJSON</code> готовят безопасные фрагменты для ручной сборки JSON-текста. После создания объекта доступны свойства и методы (<code>_Kind</code>, <code>_Count</code>, <code>AddValue</code>, <code>Exists</code> и др.) — ими же пользуются примеры EventScript при разборе состояния устройств и выборке записей журнала. Правило платформы: сначала структура (<code>_Json</code> / <code>_Obj</code>), затем поля; обратно в текст — <code>_ToStr</code> или <code>ToString</code>.</p>\n<p><a id=\"quotedstrjson\"></a></p>\n<h3><code>QuotedStrJSON</code></h3>\n<p>*** <code>QuotedStrJSON</code> — Строка в кавычках JSON ****</p>\n<p><code>function QuotedStrJSON(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Заключает строку в JSON-кавычки.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(QuotedStrJSON('a&quot;b')); // строка в JSON-кавычках с экранированием\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"escapejson\"></a></p>\n<h3><code>EscapeJSON</code></h3>\n<p>*** <code>EscapeJSON</code> — Экранирование JSON ****</p>\n<p><code>function EscapeJSON(S: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Экранирует спецсимволы для JSON-строки.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(EscapeJSON('a&quot;b')); // экранирование спецсимволов JSON без внешних кавычек\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"arr\"></a></p>\n<h3><code>_Arr</code></h3>\n<p>*** <code>_Arr</code> — Создание JSON-массива ****</p>\n<p><code>function _Arr: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>переменная для хранения JSON-массива (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт переменную для хранения JSON-массива.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой JSON-массив\n  x.Add(10);                 // добавить элемент\n  DebugLog(_ToStr(x));       // вывести массив как строку\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"arrex\"></a></p>\n<h3><code>_ArrEx</code></h3>\n<p>*** <code>_ArrEx</code> — Создание JSON-массива со значениями ****</p>\n<p><code>function _ArrEx(A: array of Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>A: array of Variant</code> — массив начальных значений для JSON-массива (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>JSON-массив с добавленными значениями (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт JSON-массив и добавляет переданные значения.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _ArrEx([1, 2, 3]);     // создать массив с начальными значениями\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"obj\"></a></p>\n<h3><code>_Obj</code></h3>\n<p>*** <code>_Obj</code> — Создание JSON-объекта ****</p>\n<p><code>function _Obj: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>переменная для хранения JSON-объекта (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт переменную для хранения JSON-объекта.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _Obj();               // создать пустой JSON-объект\n  x.AddValue('ID', 10);      // добавить поле\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"objex\"></a></p>\n<h3><code>_ObjEx</code></h3>\n<p>*** <code>_ObjEx</code> — Создание JSON-объекта с парами ****</p>\n<p><code>function _ObjEx(A: array of Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>A: array of Variant</code> — пары имя–значение для JSON-объекта (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>JSON-объект с переданными парами имя–значение (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт JSON-объект из переданных пар имя—значение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _ObjEx(['ID', 10, 'Name', 'X']); // объект из пар имя/значение\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"json\"></a></p>\n<h3><code>_Json</code></h3>\n<p>*** <code>_Json</code> — Разбор JSON-строки ****</p>\n<p><code>function _Json(S: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>S: String</code> — строка описания массива или объекта JSON (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>JSON-объект или JSON-массив, созданный из строки (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт JSON-объект или JSON-массив из строки описания.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;\nbegin\n  x := _Json('{&quot;ID&quot;:10}');   // разобрать JSON-строку\n  DebugLog(_ToStr(x));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<p><a id=\"copy\"></a></p>\n<h3><code>_Copy</code></h3>\n<p>*** <code>_Copy</code> — Копирование значения ****</p>\n<p><code>function _Copy(V: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>JSON-объект или JSON-массив копируется через его строковое представление; прочие значения возвращаются без такого копирования.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  a, b: Variant;\nbegin\n  a := _Json('{&quot;ID&quot;:10}');   // исходный объект\n  b := _Copy(a);             // копия JSON-значения\n  DebugLog(_ToStr(b));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCommon.pas</code></p>\n<hr>\n<h2><code>JSON-объект / JSON-массив</code>: свойства и методы</h2>\n<p><a id=\"json-json-kind\"></a></p>\n<h3><code>JSON-объект / JSON-массив._Kind</code></h3>\n<p>*** <code>JSON-объект / JSON-массив._Kind</code> — Тип JSON-переменной ****</p>\n<p><code>property _Kind: TDocVariantKind</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDocVariantKind</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает <code>dvObject</code> для объекта или <code>dvArray</code> для массива.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Свойство `_Kind` доступно у соответствующего объекта интерфейса.\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-count\"></a></p>\n<h3><code>JSON-объект / JSON-массив._Count</code></h3>\n<p>*** <code>JSON-объект / JSON-массив._Count</code> — Количество элементов ****</p>\n<p><code>property _Count: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает количество элементов массива или объекта.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Свойство `_Count` доступно у соответствующего объекта интерфейса.\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-name\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Name</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Name</code> — Имя элемента объекта ****</p>\n<p><code>function Name(Idx: Integer): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Idx: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает имя значения в объекте по индексу.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Name(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-value\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Value</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Value</code> — Значение элемента ****</p>\n<p><code>function Value(Idx: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Idx: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает или задаёт значение в массиве либо объекте.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Value(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-exists\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Exists</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Exists</code> — Проверка существования ****</p>\n<p><code>function Exists(Name: String): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Проверяет имя в объекте или значение в массиве.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Exists(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-add\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Add</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Add</code> — Добавление в массив ****</p>\n<p><code>function Add(V: Variant): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Добавляет значение в массив и возвращает его индекс.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Add(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-addvalue\"></a></p>\n<h3><code>JSON-объект / JSON-массив.AddValue</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.AddValue</code> — Добавление в объект ****</p>\n<p><code>function AddValue(Name: String; V: Variant): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>V: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Добавляет имя и значение в объект и возвращает индекс.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := AddValue(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-delete\"></a></p>\n<h3><code>JSON-объект / JSON-массив.Delete</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.Delete</code> — Удаление элемента ****</p>\n<p><code>procedure Delete(Idx: Integer)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Idx: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Удаляет элемент из массива или объекта.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Delete(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>\n<p><a id=\"json-json-tostring\"></a></p>\n<h3><code>JSON-объект / JSON-массив.ToString</code></h3>\n<p>*** <code>JSON-объект / JSON-массив.ToString</code> — Преобразование в строку ****</p>\n<p><code>function ToString: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Преобразует массив или объект в строку.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := ToString(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsJsonVar.pas</code></p>\n<hr>","text":"работы с JSON объектами JSON — основной структурный формат данных в скриптах UniServer. Сообщения несут Value типа Variant , часто как JSON-объект; фильтры GetRecords , документы SetRecord , параметры ReturnPage и ответы страниц данных собираются через _Obj / _Arr / _Json . Функции _ArrEx / _ObjEx создают структуру сразу со значениями; _Copy копирует JSON через строковое представление. QuotedStrJSON и EscapeJSON готовят безопасные фрагменты для ручной сборки JSON-текста. После создания объекта доступны свойства и методы ( _Kind , _Count , AddValue , Exists и др.) — ими же пользуются примеры EventScript при разборе состояния устройств и выборке записей журнала. Правило платформы: сначала структура ( _Json / _Obj ), затем поля; обратно в текст — _ToStr или ToString . QuotedStrJSON *** QuotedStrJSON — Строка в кавычках JSON **** function QuotedStrJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Заключает строку в JSON-кавычки. Пример вызова: begin\n  DebugLog(QuotedStrJSON('a\"b')); // строка в JSON-кавычках с экранированием\nend Источник сведений: Материалы для документации/source/fsCommon.pas EscapeJSON *** EscapeJSON — Экранирование JSON **** function EscapeJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Экранирует спецсимволы для JSON-строки. Пример вызова: begin\n  DebugLog(EscapeJSON('a\"b')); // экранирование спецсимволов JSON без внешних кавычек\nend Источник сведений: Материалы для документации/source/fsCommon.pas _Arr *** _Arr — Создание JSON-массива **** function _Arr: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-массива (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-массива. Пример вызова: var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой JSON-массив\n  x.Add(10);                 // добавить элемент\n  DebugLog(_ToStr(x));       // вывести массив как строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _ArrEx *** _ArrEx — Создание JSON-массива со значениями **** function _ArrEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — массив начальных значений для JSON-массива (по EventScript_desc.odt) Возвращает: JSON-массив с добавленными значениями (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-массив и добавляет переданные значения. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ArrEx([1, 2, 3]);     // создать массив с начальными значениями\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _Obj *** _Obj — Создание JSON-объекта **** function _Obj: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-объекта (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-объекта. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Obj();               // создать пустой JSON-объект\n  x.AddValue('ID', 10);      // добавить поле\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _ObjEx *** _ObjEx — Создание JSON-объекта с парами **** function _ObjEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — пары имя–значение для JSON-объекта (по EventScript_desc.odt) Возвращает: JSON-объект с переданными парами имя–значение (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект из переданных пар имя—значение. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ObjEx(['ID', 10, 'Name', 'X']); // объект из пар имя/значение\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _Json *** _Json — Разбор JSON-строки **** function _Json(S: String): Variant Входные параметры: - S: String — строка описания массива или объекта JSON (по EventScript_desc.odt) Возвращает: JSON-объект или JSON-массив, созданный из строки (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект или JSON-массив из строки описания. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas _Copy *** _Co","entryCount":17},{"id":"10-obshchie-funktsii-servera","title":"Функции сервера","intro":"Этот раздел — «короткий вход» скрипта к ядру: диагностика и чтение конфигурации. DebugLog пишет значение в лог плагина ( EventScript.log ) и незаменим при отладке обработчиков страниц и сообщений. GetConfigValue и типизированные GetConfig*Def читают параметры сервера по имени (например Core.ServerID ). В регистрации RTTI эти методы помечены как старые, сохранённые для совместимости: современный пу","html":"<h1>Общие функции для работы с сервером</h1>\n<p>Этот раздел — «короткий вход» скрипта к ядру: диагностика и чтение конфигурации. <code>DebugLog</code> пишет значение в лог плагина (<code>EventScript.log</code>) и незаменим при отладке обработчиков страниц и сообщений. <code>GetConfigValue</code> и типизированные <code>GetConfig*Def</code> читают параметры сервера по имени (например <code>Core.ServerID</code>). В регистрации RTTI эти методы помечены как старые, сохранённые для совместимости: современный путь — объекты <code>TICoreConfig</code> / <code>TICoreMessages</code> из раздела «Интерфейс сервера». Для новых сценариев предпочтительнее интерфейсы; глобальные <code>GetConfig*</code> оставляют в существующих скриптах и там, где нужен минимальный вызов без объекта конфигурации.</p>\n<p><a id=\"debuglog\"></a></p>\n<h3><code>DebugLog</code></h3>\n<p>*** <code>DebugLog</code> — Вывод строки в лог файл плагина ****</p>\n<p><code>procedure DebugLog(V: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>V: Variant</code> — параметр для вывода в лог файл (по примеру в <code>functions.txt</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Процедура ничего не возвращает. (по примеру в <code>functions.txt</code> блок «Возвращает:» пуст)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Записывает указанное значение в <code>EventScript.log</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: String;                 // строка для вывода\nbegin\n  x := '{&quot;ID&quot;:10}';          // подготовить текст\n  DebugLog(x);               // записать значение в лог файл плагина\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/functions.txt</code>; <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getconfigvalue\"></a></p>\n<h3><code>GetConfigValue</code></h3>\n<p>*** <code>GetConfigValue</code> — Прочитать значение параметра настройки по наименованию ****</p>\n<p><code>function GetConfigValue(Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — строка с именем параметра настройки (по примеру в <code>functions.txt</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>значение параметра настройки (по примеру в <code>functions.txt</code>)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает конфигурацию по имени.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  x: Variant;                // значение настройки\nbegin\n  x := GetConfigValue('Core.ServerID'); // прочитать параметр по имени\n  DebugLog(x);                          // вывести прочитанное значение в лог\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/functions.txt</code>; <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getconfigfloatdef\"></a></p>\n<h3><code>GetConfigFloatDef</code></h3>\n<p>*** <code>GetConfigFloatDef</code> — Чтение <code>Double</code> с значением по умолчанию ****</p>\n<p><code>function GetConfigFloatDef(Name: String; ADefValue: Double): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ADefValue: Double</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает <code>CoreConfig.Values</code> и применяет преобразование с <code>ADefValue</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Double;\nbegin\n  v := GetConfigFloatDef('SomeFloat', 0.0); // читать Double с значением по умолчанию\n  DebugLog(_ToStr(v));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getconfigintdef\"></a></p>\n<h3><code>GetConfigIntDef</code></h3>\n<p>*** <code>GetConfigIntDef</code> — Чтение <code>Integer</code> с значением по умолчанию ****</p>\n<p><code>function GetConfigIntDef(Name: String; ADefValue: Integer): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ADefValue: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает <code>CoreConfig.Values</code> и применяет преобразование с <code>ADefValue</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Integer;\nbegin\n  v := GetConfigIntDef('SomeInt', 0); // читать Integer с значением по умолчанию\n  DebugLog(_ToStr(v));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getconfigbooldef\"></a></p>\n<h3><code>GetConfigBoolDef</code></h3>\n<p>*** <code>GetConfigBoolDef</code> — Чтение <code>Boolean</code> с значением по умолчанию ****</p>\n<p><code>function GetConfigBoolDef(Name: String; ADefValue: Boolean): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>ADefValue: Boolean</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает <code>CoreConfig.Values</code> и применяет преобразование с <code>ADefValue</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Boolean;\nbegin\n  v := GetConfigBoolDef('SomeBool', False); // читать Boolean с значением по умолчанию\n  DebugLog(_ToStr(v));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Общие функции для работы с сервером Этот раздел — «короткий вход» скрипта к ядру: диагностика и чтение конфигурации. DebugLog пишет значение в лог плагина ( EventScript.log ) и незаменим при отладке обработчиков страниц и сообщений. GetConfigValue и типизированные GetConfig*Def читают параметры сервера по имени (например Core.ServerID ). В регистрации RTTI эти методы помечены как старые, сохранённые для совместимости: современный путь — объекты TICoreConfig / TICoreMessages из раздела «Интерфейс сервера». Для новых сценариев предпочтительнее интерфейсы; глобальные GetConfig* оставляют в существующих скриптах и там, где нужен минимальный вызов без объекта конфигурации. DebugLog *** DebugLog — Вывод строки в лог файл плагина **** procedure DebugLog(V: Variant) Входные параметры: - V: Variant — параметр для вывода в лог файл (по примеру в functions.txt ) Возвращает: Процедура ничего не возвращает. (по примеру в functions.txt блок «Возвращает:» пуст) Сведения из исходников / ODT: Записывает указанное значение в EventScript.log . Пример вызова: var\n  x: String;                 // строка для вывода\nbegin\n  x := '{\"ID\":10}';          // подготовить текст\n  DebugLog(x);               // записать значение в лог файл плагина\nend Источник сведений: Материалы для документации/functions.txt ; Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetConfigValue *** GetConfigValue — Прочитать значение параметра настройки по наименованию **** function GetConfigValue(Name: String): Variant Входные параметры: - Name: String — строка с именем параметра настройки (по примеру в functions.txt ) Возвращает: значение параметра настройки (по примеру в functions.txt ) Сведения из исходников / ODT: Читает конфигурацию по имени. Пример вызова: var\n  x: Variant;                // значение настройки\nbegin\n  x := GetConfigValue('Core.ServerID'); // прочитать параметр по имени\n  DebugLog(x);                          // вывести прочитанное значение в лог\nend Источник сведений: Материалы для документации/functions.txt ; Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetConfigFloatDef *** GetConfigFloatDef — Чтение Double с значением по умолчанию **** function GetConfigFloatDef(Name: String; ADefValue: Double): Double Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Double;\nbegin\n  v := GetConfigFloatDef('SomeFloat', 0.0); // читать Double с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetConfigIntDef *** GetConfigIntDef — Чтение Integer с значением по умолчанию **** function GetConfigIntDef(Name: String; ADefValue: Integer): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Integer;\nbegin\n  v := GetConfigIntDef('SomeInt', 0); // читать Integer с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetConfigBoolDef *** GetConfigBoolDef — Чтение Boolean с значением по умолчанию **** function GetConfigBoolDef(Name: String; ADefValue: Boolean): Boolean Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Boolean;\nbegin\n  v := GetConfigBoolDef('SomeBool', False); // читать Boolean с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas","entryCount":5},{"id":"11-otpravka-soobscheniy","title":"Отправка сообщений","intro":"Сообщения — шина взаимодействия плагинов и ядра UniServer. Имя имеет вид ИмяПлагина.ИмяСообщения (например Camera1.FrameJpg ); у сообщения есть Value ( Variant ), опциональный Blob , метка времени и признак устаревания. NewMessage / NewMessageEx создают объект; PostMsg ставит его в очередь асинхронно (если нет обработчиков — операция не доставляет сообщение); SendMsg ждёт завершения обработки у вс","html":"<h1>Отправка сообщений</h1>\n<p>Сообщения — шина взаимодействия плагинов и ядра UniServer. Имя имеет вид <code>ИмяПлагина.ИмяСообщения</code> (например <code>Camera1.FrameJpg</code>); у сообщения есть <code>Value</code> (<code>Variant</code>), опциональный <code>Blob</code>, метка времени и признак устаревания. <code>NewMessage</code> / <code>NewMessageEx</code> создают объект; <code>PostMsg</code> ставит его в очередь асинхронно (если нет обработчиков — операция не доставляет сообщение); <code>SendMsg</code> ждёт завершения обработки у всех подписчиков до <code>Timeout</code> мс (<code>-1</code> — без лимита) и возвращает результат из поля <code>Result</code>. Обработчики сообщений работают в отдельных потоках ядра; устаревшие сообщения можно не обрабатывать. Через эту же шину уходят операции журналов (<code>Journal.Operation</code>) и системные события вроде <code>Core.ServerStarted</code>. В конструкторе (AutoScaleWebKiosk1) глобальные переменные могут публиковаться сообщениями — обмен «страница ↔ логика ↔ оборудование» строится на той же модели.</p>\n<p><a id=\"newmessage\"></a></p>\n<h3><code>NewMessage</code></h3>\n<p>*** <code>NewMessage</code> — Создание сообщения ****</p>\n<p><code>function NewMessage(MsgName: String; Value: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>MsgName: String</code> — имя сообщения вида <code>ИмяПлагина.ИмяСообщения</code> (по PluginAPI_desc.odt)<br>\n- <code>Value: Variant</code> — полезная нагрузка сообщения (тип Variant по RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Имя сообщения имеет вид <code>ИмяПлагина.ИмяСообщения</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;             // сообщение\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // создать сообщение по имени\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"newmessageex\"></a></p>\n<h3><code>NewMessageEx</code></h3>\n<p>*** <code>NewMessageEx</code> — Создание расширенного сообщения ****</p>\n<p><code>function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>MsgName: String</code> — имя сообщения вида <code>ИмяПлагина.ИмяСообщения</code> (по PluginAPI_desc.odt)<br>\n- <code>Value: Variant</code> — полезная нагрузка сообщения (<code>Variant</code>, по PluginAPI_desc.odt / RTTI)<br>\n- <code>Blob: Variant</code> — двоичные данные сообщения (<code>IBlob</code> / <code>TIBlob</code>, по PluginAPI_desc.odt)<br>\n- <code>CurrentUser: Variant</code> — пользователь, от имени которого создаётся сообщение (<code>IUser</code> / <code>TIUser</code>, по TLB)<br>\n- <code>Timeout: Integer</code> — таймаут сообщения в миллисекундах (по RTTI / TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Имя сообщения имеет вид <code>ИмяПлагина.ИмяСообщения</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"postmsg\"></a></p>\n<h3><code>PostMsg</code></h3>\n<p>*** <code>PostMsg</code> — Асинхронная отправка сообщения ****</p>\n<p><code>function PostMsg(Msg: Variant): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Msg: Variant</code> — сообщение для асинхронной постановки в очередь (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>по PluginAPI_desc.odt для интерфейса ядра PostMsg возвращает WordBool/False при отсутствии обработчиков; в глобальной RTTI-процедуре <code>procedure PostMsg</code> возвращаемого значения нет</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Постановка в очередь выполняется асинхронно; возвращает <code>False</code>, если обработчиков нет.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // подготовить сообщение\n  PostMsg(LMsg);                              // отправить асинхронно\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"sendmsg\"></a></p>\n<h3><code>SendMsg</code></h3>\n<p>*** <code>SendMsg</code> — Синхронная отправка сообщения ****</p>\n<p><code>function SendMsg(Msg: Variant; Timeout: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Msg: Variant</code> — сообщение для синхронной отправки (по PluginAPI_desc.odt)<br>\n- <code>Timeout: Integer</code> — таймаут ожидания в миллисекундах; <code>-1</code> — бесконечное ожидание (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>результат обработки (поле Result сообщения) после ожидания до Timeout (по PluginAPI_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ожидает обработку до <code>Timeout</code> мс; <code>-1</code> означает бесконечное ожидание.</li>\n<li>Результат берётся из поля <code>Result</code> сообщения.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg, LRes: Variant;\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // подготовить сообщение\n  LRes := SendMsg(LMsg, 1000);                // отправить синхронно, ждать до 1000 мс\n  DebugLog(LRes);                             // результат обработки\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>","text":"Отправка сообщений Сообщения — шина взаимодействия плагинов и ядра UniServer. Имя имеет вид ИмяПлагина.ИмяСообщения (например Camera1.FrameJpg ); у сообщения есть Value ( Variant ), опциональный Blob , метка времени и признак устаревания. NewMessage / NewMessageEx создают объект; PostMsg ставит его в очередь асинхронно (если нет обработчиков — операция не доставляет сообщение); SendMsg ждёт завершения обработки у всех подписчиков до Timeout мс ( -1 — без лимита) и возвращает результат из поля Result . Обработчики сообщений работают в отдельных потоках ядра; устаревшие сообщения можно не обрабатывать. Через эту же шину уходят операции журналов ( Journal.Operation ) и системные события вроде Core.ServerStarted . В конструкторе (AutoScaleWebKiosk1) глобальные переменные могут публиковаться сообщениями — обмен «страница ↔ логика ↔ оборудование» строится на той же модели. NewMessage *** NewMessage — Создание сообщения **** function NewMessage(MsgName: String; Value: Variant): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения (тип Variant по RTTI) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;             // сообщение\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // создать сообщение по имени\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas NewMessageEx *** NewMessageEx — Создание расширенного сообщения **** function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения ( Variant , по PluginAPI_desc.odt / RTTI) - Blob: Variant — двоичные данные сообщения ( IBlob / TIBlob , по PluginAPI_desc.odt) - CurrentUser: Variant — пользователь, от имени которого создаётся сообщение ( IUser / TIUser , по TLB) - Timeout: Integer — таймаут сообщения в миллисекундах (по RTTI / TLB) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas PostMsg *** PostMsg — Асинхронная отправка сообщения **** function PostMsg(Msg: Variant): Boolean Входные параметры: - Msg: Variant — сообщение для асинхронной постановки в очередь (по PluginAPI_desc.odt) Возвращает: по PluginAPI_desc.odt для интерфейса ядра PostMsg возвращает WordBool/False при отсутствии обработчиков; в глобальной RTTI-процедуре procedure PostMsg возвращаемого значения нет Сведения из исходников / ODT: Постановка в очередь выполняется асинхронно; возвращает False , если обработчиков нет. Пример вызова: var\n  LMsg: Variant;\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // подготовить сообщение\n  PostMsg(LMsg);                              // отправить асинхронно\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt SendMsg *** SendMsg — Синхронная отправка сообщения **** function SendMsg(Msg: Variant; Timeout: Integer): Variant Входные параметры: - Msg: Variant — сообщение для синхронной отправки (по PluginAPI_desc.odt) - Timeout: Integer — таймаут ожидания в миллисекундах; -1 — бесконечное ожидание (по PluginAPI_desc.odt) Возвращает: результат обработки (поле Result сообщения) после ожидания до Timeout (по PluginAPI_desc.odt) Сведения из исходников / ODT: Ожидает обработку до Timeout мс; -1 означает бесконечное ожидание. Результат берётся из поля Result сообщения. Пример вызова: var\n  LMsg, LRes: Variant;\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // подготовить сообщение\n  LRes := SendMsg(LMsg, 1000);                // отправить синхронно, ждать до 1000 мс\n  DebugLog(LRes);                             // результат обработки\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt","entryCount":4},{"id":"12-globalnye-peremennye","title":"Глобальные переменные","intro":"Глобальные переменные конструктора — общее состояние сценария плагина: входные и выходные параметры страниц, настройки, внутренние флаги, значения, публикуемые сообщениями. В веб-UI конструктора (раздел SMC AutoScaleWebKiosk1) они делятся на входные, выходные, настройки, внутренние, опубликованные и с обработчиком изменения. Скрипт страницы или обработчика читает и пишет их через GetVarValue / Set","html":"<h1>Плагин Конструктор / Работа с глобальными переменными Конструктора</h1>\n<p>Глобальные переменные конструктора — общее состояние сценария плагина: входные и выходные параметры страниц, настройки, внутренние флаги, значения, публикуемые сообщениями. В веб-UI конструктора (раздел SMC AutoScaleWebKiosk1) они делятся на входные, выходные, настройки, внутренние, опубликованные и с обработчиком изменения. Скрипт страницы или обработчика читает и пишет их через <code>GetVarValue</code> / <code>SetVarValue</code>; <code>GetVarModTime</code>, <code>GetVarModCount</code>, <code>GetVarModFreq</code> дают метаданные изменений (удобно для опроса «изменилось ли» без сравнения всего значения). При отсутствии переменной с указанным именем вызывается исключение — имя должно совпадать с объявленным в конструкторе. Это не замена журналам: глобальные переменные живут в контексте работающего плагина-сценария, а не как долговременный справочник.</p>\n<p><a id=\"getvarmodtime\"></a></p>\n<h3><code>GetVarModTime</code></h3>\n<p>*** <code>GetVarModTime</code> — Время изменения глобальной переменной ****</p>\n<p><code>function GetVarModTime(Name: String): TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(DateTimeToIso8601(GetVarModTime('Counter'))); // время последнего изменения\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getvarmodcount\"></a></p>\n<h3><code>GetVarModCount</code></h3>\n<p>*** <code>GetVarModCount</code> — Счётчик изменений глобальной переменной ****</p>\n<p><code>function GetVarModCount(Name: String): Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetVarModCount('Counter'))); // счётчик изменений\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getvarmodfreq\"></a></p>\n<h3><code>GetVarModFreq</code></h3>\n<p>*** <code>GetVarModFreq</code> — Частота изменений глобальной переменной ****</p>\n<p><code>function GetVarModFreq(Name: String): Double</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Double</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetVarModFreq('Counter'))); // частота изменений\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getvarvalue\"></a></p>\n<h3><code>GetVarValue</code></h3>\n<p>*** <code>GetVarValue</code> — Чтение глобальной переменной ****</p>\n<p><code>function GetVarValue(Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  v: Variant;\nbegin\n  v := GetVarValue('Counter'); // прочитать глобальную переменную плагина\n  DebugLog(v);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setvarvalue\"></a></p>\n<h3><code>SetVarValue</code></h3>\n<p>*** <code>SetVarValue</code> — Запись глобальной переменной ****</p>\n<p><code>procedure SetVarValue(Name: String; Value: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Value: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Работает с глобальной переменной; при отсутствии переменной генерируется исключение.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  SetVarValue('Counter', 1); // записать значение в глобальную переменную\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Плагин Конструктор / Работа с глобальными переменными Конструктора Глобальные переменные конструктора — общее состояние сценария плагина: входные и выходные параметры страниц, настройки, внутренние флаги, значения, публикуемые сообщениями. В веб-UI конструктора (раздел SMC AutoScaleWebKiosk1) они делятся на входные, выходные, настройки, внутренние, опубликованные и с обработчиком изменения. Скрипт страницы или обработчика читает и пишет их через GetVarValue / SetVarValue ; GetVarModTime , GetVarModCount , GetVarModFreq дают метаданные изменений (удобно для опроса «изменилось ли» без сравнения всего значения). При отсутствии переменной с указанным именем вызывается исключение — имя должно совпадать с объявленным в конструкторе. Это не замена журналам: глобальные переменные живут в контексте работающего плагина-сценария, а не как долговременный справочник. GetVarModTime *** GetVarModTime — Время изменения глобальной переменной **** function GetVarModTime(Name: String): TDateTime Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(GetVarModTime('Counter'))); // время последнего изменения\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetVarModCount *** GetVarModCount — Счётчик изменений глобальной переменной **** function GetVarModCount(Name: String): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(_ToStr(GetVarModCount('Counter'))); // счётчик изменений\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetVarModFreq *** GetVarModFreq — Частота изменений глобальной переменной **** function GetVarModFreq(Name: String): Double Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(_ToStr(GetVarModFreq('Counter'))); // частота изменений\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas GetVarValue *** GetVarValue — Чтение глобальной переменной **** function GetVarValue(Name: String): Variant Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: var\n  v: Variant;\nbegin\n  v := GetVarValue('Counter'); // прочитать глобальную переменную плагина\n  DebugLog(v);\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas SetVarValue *** SetVarValue — Запись глобальной переменной **** procedure SetVarValue(Name: String; Value: Variant) Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Value: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  SetVarValue('Counter', 1); // записать значение в глобальную переменную\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas","entryCount":5},{"id":"13-interfeys-servera","title":"Интерфейс сервера","intro":"Раздел описывает объекты ядра, доступные скрипту как типизированные интерфейсы: конфигурация ( TICoreConfig ), сообщения ( TICoreMessages , TIMessage , TIBlob ), планировщик ( TICoreScheduler ), локаль ( TICoreLocale ), пользователь ( TIUser ), HTTP-запрос и ответ ( TIWebServerRequest , TIWebServerResponse ). Плагин встраивает страницы вызовом регистрации обработчика; при обращении к /core/plugins","html":"<h1>Интерфейс сервера</h1>\n<p>Раздел описывает объекты ядра, доступные скрипту как типизированные интерфейсы: конфигурация (<code>TICoreConfig</code>), сообщения (<code>TICoreMessages</code>, <code>TIMessage</code>, <code>TIBlob</code>), планировщик (<code>TICoreScheduler</code>), локаль (<code>TICoreLocale</code>), пользователь (<code>TIUser</code>), HTTP-запрос и ответ (<code>TIWebServerRequest</code>, <code>TIWebServerResponse</code>). Плагин встраивает страницы вызовом регистрации обработчика; при обращении к <code>/core/plugins/ИмяПлагина/ИмяСтраницы</code> ядро вызывает обработчик с парой Request/Response. Ответ формируют <code>ReturnPage</code> (шаблон из <code>Views</code> в UTF-8, встройка в общий каркас сайта), <code>ReturnError</code>, <code>ReturnText</code> / <code>ReturnBlob</code>. Страницы принято разделять на интерфейс, действия и данные с разными флагами аутентификации и кэша. <code>TIBlob</code> передаёт двоичные данные без лишнего копирования между плагинами. Планировщик ставит отправку сообщений по crontab-подобной строке или таймеру. Этот слой — полная модель «законов» сервера; упрощённые <code>GetConfig*</code> / <code>PostMsg</code> из предыдущих разделов дублируют часть возможностей для совместимости.</p>\n<p>Состав интерфейсов и сигнатуры подтверждены RTTI (<code>functions.txt</code>) и <code>PluginAPI_TLB.pas</code>. Поведенческие сведения взяты из PluginAPI_desc; где в материалах остаётся только сигнатура — стоит маркер дополнения.</p>\n<p><a id=\"tiblob\"></a></p>\n<h2><code>TIBlob</code></h2>\n<p>Обёртка скрипта над COM-интерфейсом <code>IBlob</code>: контейнер двоичных данных без лишнего копирования между плагинами. У blob есть имя, тип содержимого, размер и сами данные; плагин может реализовать <code>IBlob</code> эффективно под свою модель памяти (по PluginAPI_desc.odt). В скрипте <code>Data</code> отдаётся как Variant-массив байт (копирование в <code>VarArray</code> в <code>fsCoreScript.pas</code>).</p>\n<p><a id=\"tiblob-tojson\"></a></p>\n<h3><code>TIBlob.ToJson</code></h3>\n<p>*** <code>TIBlob.ToJson</code> — JSON-представление blob ****</p>\n<p><code>function ToJson: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вызывает <code>IBlob.ToJSON</code> (по <code>fsCoreScript.pas</code> / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"tiblob-name\"></a></p>\n<h3><code>TIBlob.Name</code></h3>\n<p>*** <code>TIBlob.Name</code> — Имя blob ****</p>\n<p><code>property Name: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>IBlob.Name</code> — имя двоичного вложения (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIBlob.Name` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Name));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiblob-contenttype\"></a></p>\n<h3><code>TIBlob.ContentType</code></h3>\n<p>*** <code>TIBlob.ContentType</code> — Тип содержимого ****</p>\n<p><code>property ContentType: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>IBlob.ContentType</code> — тип содержимого двоичных данных (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIBlob.ContentType` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ContentType));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiblob-data\"></a></p>\n<h3><code>TIBlob.Data</code></h3>\n<p>*** <code>TIBlob.Data</code> — Данные blob ****</p>\n<p><code>property Data: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>В COM — указатель на данные (<code>IBlob.Data</code>).</li>\n<li>В скриптовой обёртке копируется в Variant-массив байт (<code>VarArray</code> of <code>varByte</code>) в <code>TIBlob.GetData</code> (<code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIBlob.Data` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Data));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"tiblob-size\"></a></p>\n<h3><code>TIBlob.Size</code></h3>\n<p>*** <code>TIBlob.Size</code> — Размер данных ****</p>\n<p><code>property Size: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Число байт в blob (<code>IBlob.Size</code>, по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIBlob.Size` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Size));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig\"></a></p>\n<h2><code>TICoreConfig</code></h2>\n<p>Обёртка над <code>ICoreConfig</code>: пути сервера и плагина, адрес/порт, словарь параметров <code>Values</code> / <code>Defaults</code>, метаданные сообщений и ролей. Чтение <code>Values[Name]</code> — тот же слой конфигурации, что обслуживают устаревшие глобальные <code>GetConfig*</code>. <code>MyPath</code> — каталог текущего плагина; от него резолвятся относительные пути <code>StringFromFile</code> / <code>FileFromString</code> (по TLB / <code>fsCoreScript.pas</code>).</p>\n<p><a id=\"ticoreconfig-binpath\"></a></p>\n<h3><code>TICoreConfig.BinPath</code></h3>\n<p>*** <code>TICoreConfig.BinPath</code> — Каталог двоичных файлов сервера ****</p>\n<p><code>property BinPath: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ICoreConfig.BinPath</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.BinPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.BinPath));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-datapath\"></a></p>\n<h3><code>TICoreConfig.DataPath</code></h3>\n<p>*** <code>TICoreConfig.DataPath</code> — Каталог данных сервера ****</p>\n<p><code>property DataPath: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ICoreConfig.DataPath</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.DataPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.DataPath));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-defaults\"></a></p>\n<h3><code>TICoreConfig.Defaults</code></h3>\n<p>*** <code>TICoreConfig.Defaults</code> — Значение конфигурации по умолчанию ****</p>\n<p><code>property Defaults[Name: String]: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя параметра конфигурации (по TLB <code>Defaults[Index]</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Индексное свойство <code>Defaults[Name]</code> — значение по умолчанию для параметра (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.Defaults` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Defaults));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-logpath\"></a></p>\n<h3><code>TICoreConfig.LogPath</code></h3>\n<p>*** <code>TICoreConfig.LogPath</code> — Каталог журналов ****</p>\n<p><code>property LogPath: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ICoreConfig.LogPath</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.LogPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LogPath));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-logverbose\"></a></p>\n<h3><code>TICoreConfig.LogVerbose</code></h3>\n<p>*** <code>TICoreConfig.LogVerbose</code> — Подробный режим логирования ****</p>\n<p><code>property LogVerbose: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ICoreConfig.LogVerbose</code> (по TLB); в скриптовой обёртке читается как Boolean (<code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.LogVerbose` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LogVerbose));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-msgfortype\"></a></p>\n<h3><code>TICoreConfig.MsgForType</code></h3>\n<p>*** <code>TICoreConfig.MsgForType</code> — Имя сообщения по типу содержимого ****</p>\n<p><code>property MsgForType[Name: String; Value: Variant]: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — тип содержимого / ключ сопоставления (в TLB параметр <code>ContentType</code>)<br>\n- <code>Value: Variant</code> — признак команды (<code>IsCommand</code> в TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Индексное свойство <code>MsgForType[ContentType, IsCommand]</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.MsgForType` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgForType));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-msginfo\"></a></p>\n<h3><code>TICoreConfig.MsgInfo</code></h3>\n<p>*** <code>TICoreConfig.MsgInfo</code> — Описание сообщения ****</p>\n<p><code>property MsgInfo[Name: String]: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя сообщения (по TLB <code>MsgInfo[MsgName]</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Индексное свойство <code>MsgInfo[MsgName]</code> — информация о сообщении; изменение связано с системным <code>Core.MsgInfoChanged</code> (по TLB / PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.MsgInfo` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgInfo));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-msginfos\"></a></p>\n<h3><code>TICoreConfig.MsgInfos</code></h3>\n<p>*** <code>TICoreConfig.MsgInfos</code> — Сводная информация о сообщениях ****</p>\n<p><code>property MsgInfos: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>MsgInfos</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.MsgInfos` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgInfos));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-msgnames\"></a></p>\n<h3><code>TICoreConfig.MsgNames</code></h3>\n<p>*** <code>TICoreConfig.MsgNames</code> — Имена сообщений ****</p>\n<p><code>property MsgNames: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>MsgNames</code> — перечень имён сообщений (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.MsgNames` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgNames));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-mypath\"></a></p>\n<h3><code>TICoreConfig.MyPath</code></h3>\n<p>*** <code>TICoreConfig.MyPath</code> — Каталог текущего плагина ****</p>\n<p><code>property MyPath: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>MyPath</code>; от него разрешаются относительные пути файловых функций скрипта (по TLB / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.MyPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MyPath));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-pluginoptsdocjson\"></a></p>\n<h3><code>TICoreConfig.PluginOptsDocJson</code></h3>\n<p>*** <code>TICoreConfig.PluginOptsDocJson</code> — JSON-описание опций плагина ****</p>\n<p><code>property PluginOptsDocJson[Name: String]: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя плагина (по TLB <code>PluginOptsDocJson[PluginName]</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Индексное свойство <code>PluginOptsDocJson[PluginName]</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.PluginOptsDocJson` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.PluginOptsDocJson));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-pluginpath\"></a></p>\n<h3><code>TICoreConfig.PluginPath</code></h3>\n<p>*** <code>TICoreConfig.PluginPath</code> — Каталог плагинов ****</p>\n<p><code>property PluginPath: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ICoreConfig.PluginPath</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.PluginPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.PluginPath));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-roles\"></a></p>\n<h3><code>TICoreConfig.Roles</code></h3>\n<p>*** <code>TICoreConfig.Roles</code> — Роли сервера ****</p>\n<p><code>property Roles: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>Roles</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.Roles` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Roles));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-serveraddr\"></a></p>\n<h3><code>TICoreConfig.ServerAddr</code></h3>\n<p>*** <code>TICoreConfig.ServerAddr</code> — Адрес сервера ****</p>\n<p><code>property ServerAddr: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ServerAddr</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.ServerAddr` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ServerAddr));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-serverport\"></a></p>\n<h3><code>TICoreConfig.ServerPort</code></h3>\n<p>*** <code>TICoreConfig.ServerPort</code> — Порт сервера ****</p>\n<p><code>property ServerPort: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ServerPort</code> (по TLB); в COM — Integer.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.ServerPort` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ServerPort));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-url\"></a></p>\n<h3><code>TICoreConfig.URL</code></h3>\n<p>*** <code>TICoreConfig.URL</code> — Базовый URL сервера ****</p>\n<p><code>property URL: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>URL</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.URL` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.URL));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoreconfig-values\"></a></p>\n<h3><code>TICoreConfig.Values</code></h3>\n<p>*** <code>TICoreConfig.Values</code> — Значение параметра конфигурации ****</p>\n<p><code>property Values[Name: String]: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя параметра конфигурации (по TLB <code>Values[Index]</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Индексное свойство <code>Values[Name]</code> — чтение/запись параметра конфигурации.</li>\n<li>Глобальные <code>GetConfig*</code> читают тот же слой через <code>CoreConfig.Values</code> (по TLB / <code>fsCoreScript.pas</code> / <code>functions.txt</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreConfig.Values` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Values));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale\"></a></p>\n<h2><code>TICoreLocale</code></h2>\n<p>Обёртка над <code>ICoreLocale</code>: форматирование валюты, даты, веса, форм множественного числа и перевод строк с учётом текущего языка сервера (сигнатуры — TLB / RTTI).</p>\n<p><a id=\"ticorelocale-formatcurrency\"></a></p>\n<h3><code>TICoreLocale.FormatCurrency</code></h3>\n<p>*** <code>TICoreLocale.FormatCurrency</code> — Форматирование денежной суммы ****</p>\n<p><code>function FormatCurrency(Value: Double; Currency: String; Spellout, Cents: Boolean): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Double</code> — сумма (по TLB)<br>\n- <code>Currency: String</code> — код/обозначение валюты (по TLB)<br>\n- <code>Spellout: Boolean</code> — прописью (по имени параметра TLB)<br>\n- <code>Cents: Boolean</code> — включать копейки/центы (по имени параметра TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Метод <code>ICoreLocale.FormatCurrency</code> (по TLB / RTTI).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.FormatCurrency(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale-formatdate\"></a></p>\n<h3><code>TICoreLocale.FormatDate</code></h3>\n<p>*** <code>TICoreLocale.FormatDate</code> — Форматирование даты ****</p>\n<p><code>function FormatDate(Value: TDateTime; WithTime: Boolean): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: TDateTime</code> — дата/время (по TLB)<br>\n- <code>WithTime: Boolean</code> — включать время в строку (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Метод <code>ICoreLocale.FormatDate</code>; параметр <code>WithTime</code> включает время (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.FormatDate(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale-formatplural\"></a></p>\n<h3><code>TICoreLocale.FormatPlural</code></h3>\n<p>*** <code>TICoreLocale.FormatPlural</code> — Форма множественного числа ****</p>\n<p><code>function FormatPlural(Value: Integer; Plurals: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Integer</code> — число для выбора формы множественного числа (по TLB)<br>\n- <code>Plurals: String</code> — набор форм множественного числа (по TLB; формат строки в текстовых материалах не раскрыт)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Метод <code>ICoreLocale.FormatPlural</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.FormatPlural(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale-formatweight\"></a></p>\n<h3><code>TICoreLocale.FormatWeight</code></h3>\n<p>*** <code>TICoreLocale.FormatWeight</code> — Форматирование массы ****</p>\n<p><code>function FormatWeight(Value: Double; Spellout: Boolean): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: Double</code> — значение массы (по TLB)<br>\n- <code>Spellout: Boolean</code> — прописью (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Метод <code>ICoreLocale.FormatWeight</code> (по TLB / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.FormatWeight(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale-translate\"></a></p>\n<h3><code>TICoreLocale.Translate</code></h3>\n<p>*** <code>TICoreLocale.Translate</code> — Перевод строки ****</p>\n<p><code>function Translate(Value: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — строка для перевода (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Метод <code>ICoreLocale.Translate</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.Translate(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale-language\"></a></p>\n<h3><code>TICoreLocale.Language</code></h3>\n<p>*** <code>TICoreLocale.Language</code> — Язык локали ****</p>\n<p><code>property Language: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>Language</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreLocale.Language` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Language));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale-languagecode\"></a></p>\n<h3><code>TICoreLocale.LanguageCode</code></h3>\n<p>*** <code>TICoreLocale.LanguageCode</code> — Код языка ****</p>\n<p><code>property LanguageCode: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>LanguageCode</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreLocale.LanguageCode` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LanguageCode));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorelocale-languageid\"></a></p>\n<h3><code>TICoreLocale.LanguageID</code></h3>\n<p>*** <code>TICoreLocale.LanguageID</code> — Идентификатор языка ****</p>\n<p><code>property LanguageID: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>LanguageID</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreLocale.LanguageID` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LanguageID));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoremessages\"></a></p>\n<h2><code>TICoreMessages</code></h2>\n<p>Обёртка над <code>ICoreMessages</code> — шина сообщений ядра: создание <code>IMessage</code>, асинхронный <code>PostMsg</code>, синхронный <code>SendMsg</code>, проверка подписчиков <code>HasHandlerFor</code>. Имя сообщения — <code>ИмяПлагина.ИмяСообщения</code>. Обработчики работают в потоках <code>TMessageHandler</code>; после обработки нельзя удерживать ссылку на <code>IMessage</code> (ссылку на <code>IBlob</code> удерживать можно) — по PluginAPI_desc.odt.</p>\n<p><a id=\"ticoremessages-hashandlerfor\"></a></p>\n<h3><code>TICoreMessages.HasHandlerFor</code></h3>\n<p>*** <code>TICoreMessages.HasHandlerFor</code> — Проверка подписчиков ****</p>\n<p><code>function HasHandlerFor(MsgName: String): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>MsgName: String</code> — имя сообщения (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p><code>True</code>, если в системе есть подписчики на указанное имя сообщения (по PluginAPI_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает, есть ли в системе обработчики для имени сообщения (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  if CoreMessages.HasHandlerFor('Camera1.FrameJpg') then\n    DebugLog('есть подписчики');\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"ticoremessages-newmessage\"></a></p>\n<h3><code>TICoreMessages.NewMessage</code></h3>\n<p>*** <code>TICoreMessages.NewMessage</code> — Создание сообщения ****</p>\n<p><code>function NewMessage(MsgName: String; Value: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>MsgName: String</code> — имя вида <code>ИмяПлагина.ИмяСообщения</code> (по PluginAPI_desc.odt)<br>\n- <code>Value: Variant</code> — полезная нагрузка <code>Value</code> (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>созданный объект сообщения (по PluginAPI_desc.odt / TLB)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт <code>IMessage</code> с именем вида <code>ИмяПлагина.ИмяСообщения</code> и значением <code>Value</code> (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\nbegin\n  LMsg := CoreMessages.NewMessage('Camera1.FrameJpg', ''); // создать сообщение\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"ticoremessages-newmessageex\"></a></p>\n<h3><code>TICoreMessages.NewMessageEx</code></h3>\n<p>*** <code>TICoreMessages.NewMessageEx</code> — Создание расширенного сообщения ****</p>\n<p><code>function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>MsgName: String</code> — имя вида <code>ИмяПлагина.ИмяСообщения</code> (по PluginAPI_desc.odt)<br>\n- <code>Value: Variant</code> — полезная нагрузка <code>Value</code> (по PluginAPI_desc.odt)<br>\n- <code>Blob: Variant</code> — двоичные данные <code>IBlob</code> (по PluginAPI_desc.odt)<br>\n- <code>CurrentUser: Variant</code> — пользователь <code>IUser</code> (по TLB)<br>\n- <code>Timeout: Integer</code> — таймаут сообщения (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>созданный объект сообщения с Blob/User/Timeout (по PluginAPI_desc.odt / TLB)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт сообщение с <code>Value</code>, <code>Blob</code>, пользователем и таймаутом (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticoremessages-newmessagefromjson\"></a></p>\n<h3><code>TICoreMessages.NewMessageFromJson</code></h3>\n<p>*** <code>TICoreMessages.NewMessageFromJson</code> — Создание сообщения из JSON ****</p>\n<p><code>function NewMessageFromJson(Json, NewMsgName: String; Remote: Boolean): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Json: String</code> — JSON-текст сообщения (по TLB)<br>\n- <code>NewMsgName: String</code> — имя сообщения (по TLB)<br>\n- <code>Remote: Boolean</code> — признак Remote (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Метод <code>NewMessageFromJSON</code> COM-интерфейса (по TLB / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.NewMessageFromJson(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"ticoremessages-postmsg\"></a></p>\n<h3><code>TICoreMessages.PostMsg</code></h3>\n<p>*** <code>TICoreMessages.PostMsg</code> — Асинхронная отправка ****</p>\n<p><code>function PostMsg(Msg: Variant): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Msg: Variant</code> — объект сообщения (<code>IMessage</code> / <code>TIMessage</code>) для постановки в очередь (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p><code>True</code>, если сообщение поставлено в очередь хотя бы одному обработчику; <code>False</code>, если обработчиков нет (по PluginAPI_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ставит сообщение в очередь каждого обработчика; <code>True</code> при успешной постановке, <code>False</code> если обработчиков нет (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\n  Ok: Boolean;\nbegin\n  LMsg := CoreMessages.NewMessage('Camera1.FrameJpg', '');\n  Ok := CoreMessages.PostMsg(LMsg); // True, если есть обработчики\n  DebugLog(_ToStr(Ok));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"ticoremessages-sendmsg\"></a></p>\n<h3><code>TICoreMessages.SendMsg</code></h3>\n<p>*** <code>TICoreMessages.SendMsg</code> — Синхронная отправка ****</p>\n<p><code>function SendMsg(Msg: Variant; Timeout: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Msg: Variant</code> — объект сообщения для синхронной отправки (по PluginAPI_desc.odt)<br>\n- <code>Timeout: Integer</code> — таймаут ожидания в мс; <code>-1</code> — бесконечное ожидание (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>значение поля <code>Result</code> сообщения после ожидания обработки (по PluginAPI_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ждёт завершения обработки всеми подписчиками до <code>Timeout</code> мс (<code>-1</code> — без лимита).</li>\n<li>Окончание определяется по счётчику ссылок сообщения; результат — поле <code>Result</code> (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg, LRes: Variant;\nbegin\n  LMsg := CoreMessages.NewMessage('Camera1.FrameJpg', '');\n  LRes := CoreMessages.SendMsg(LMsg, 1000); // ждать до 1000 мс; -1 — бесконечно\n  DebugLog(LRes);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"ticoremessages-registeredmessages\"></a></p>\n<h3><code>TICoreMessages.RegisteredMessages</code></h3>\n<p>*** <code>TICoreMessages.RegisteredMessages</code> — Зарегистрированные сообщения ****</p>\n<p><code>property RegisteredMessages: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>RegisteredMessages</code> интерфейса <code>ICoreMessages</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TICoreMessages.RegisteredMessages` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.RegisteredMessages));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorescheduler\"></a></p>\n<h2><code>TICoreScheduler</code></h2>\n<p>Обёртка над <code>ICoreScheduler</code>: отложенная или периодическая отправка сообщений. Расписание <code>AddTask</code> — строка из 6 полей (секунда…день недели), локальное время компьютера сервера, синтаксис как crontab; день недели и день месяца вместе обрабатываются по «ИЛИ». <code>AddTimer</code> шлёт сообщение каждые N секунд. В скриптовой RTTI флаг <code>RunOnce</code> отображается на опцию <code>SchedulerRunOnce</code> ядра (PluginAPI_desc.odt / <code>fsCoreScript.pas</code>). Примеры в старом ODT с укороченной сигнатурой <code>AddTask</code> считаются устаревшими относительно текущего RTTI.</p>\n<p><a id=\"ticorescheduler-addtask\"></a></p>\n<h3><code>TICoreScheduler.AddTask</code></h3>\n<p>*** <code>TICoreScheduler.AddTask</code> — Задача по расписанию ****</p>\n<p><code>function AddTask(Tab, UpdateTaskID: String; Msg: Variant; RunOnce: Boolean): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Tab: String</code> — строка расписания из 6 частей (Секунда Минута Час День Месяц ДеньНедели), локальное время сервера; синтаксис как crontab (по PluginAPI_desc.odt)<br>\n- <code>UpdateTaskID: String</code> — идентификатор задачи для обновления существующей; пустая строка — создать новую (по сигнатуре TLB <code>UpdateTaskID</code>)<br>\n- <code>Msg: Variant</code> — сообщение, которое планировщик отправит при срабатывании (по PluginAPI_desc.odt)<br>\n- <code>RunOnce: Boolean</code> — если True, в опции ядра добавляется <code>SchedulerRunOnce</code> (по <code>fsCoreScript.pas</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p>идентификатор задачи планировщика (по TLB / <code>fsCoreScript.pas</code>)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Планирует отправку сообщения по crontab-подобной строке <code>Tab</code> (6 полей, локальное время сервера).</li>\n<li><code>RunOnce=True</code> добавляет флаг <code>SchedulerRunOnce</code> в опции ядра (по PluginAPI_desc.odt / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\n  TaskID: String;\nbegin\n  LMsg := CoreMessages.NewMessage('MyPlugin.Tick', '');\n  // каждый день в 00:05:00, локальное время сервера\n  TaskID := CoreScheduler.AddTask('0 5 0 * * *', '', LMsg, False);\n  DebugLog(TaskID);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"ticorescheduler-addtimer\"></a></p>\n<h3><code>TICoreScheduler.AddTimer</code></h3>\n<p>*** <code>TICoreScheduler.AddTimer</code> — Периодический таймер ****</p>\n<p><code>function AddTimer(Seconds: Integer; Msg: Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Seconds: Integer</code> — период отправки сообщения в секундах (по PluginAPI_desc.odt)<br>\n- <code>Msg: Variant</code> — сообщение для периодической отправки (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>идентификатор таймера (по TLB / <code>fsCoreScript.pas</code>)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Отправляет сообщение каждые <code>Seconds</code> секунд (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  LMsg: Variant;\n  TimerID: String;\nbegin\n  LMsg := CoreMessages.NewMessage('MyPlugin.Tick', '');\n  TimerID := CoreScheduler.AddTimer(30, LMsg); // каждые 30 секунд\n  DebugLog(TimerID);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"ticorescheduler-removetask\"></a></p>\n<h3><code>TICoreScheduler.RemoveTask</code></h3>\n<p>*** <code>TICoreScheduler.RemoveTask</code> — Удаление задачи ****</p>\n<p><code>procedure RemoveTask(TaskID: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>TaskID: String</code> — идентификатор задачи, возвращённый <code>AddTask</code> (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Снимает задачу по идентификатору (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Obj.RemoveTask(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"ticorescheduler-removetimer\"></a></p>\n<h3><code>TICoreScheduler.RemoveTimer</code></h3>\n<p>*** <code>TICoreScheduler.RemoveTimer</code> — Удаление таймера ****</p>\n<p><code>procedure RemoveTimer(TimerID: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>TimerID: String</code> — идентификатор таймера, возвращённый <code>AddTimer</code> (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Снимает таймер по идентификатору (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Obj.RemoveTimer(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiuser\"></a></p>\n<h2><code>TIUser</code></h2>\n<p>Обёртка над <code>IUser</code>: сведения о пользователе HTTP-сессии или сообщения — идентификатор, имя, отображаемое имя, группа, роли, IP. Используется в <code>AccessCheck</code> страниц и в поле <code>User</code> сообщения (PluginAPI_desc.odt / TLB).</p>\n<p><a id=\"tiuser-hasrole\"></a></p>\n<h3><code>TIUser.HasRole</code></h3>\n<p>*** <code>TIUser.HasRole</code> — Проверка роли ****</p>\n<p><code>function HasRole(RoleName: String): Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>RoleName: String</code> — имя роли для проверки (по TLB <code>IUser.HasRole</code>)</p>\n<p><strong>Возвращает:</strong></p>\n<p><code>True</code>, если у пользователя есть указанная роль (по TLB)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает, входит ли роль в набор ролей пользователя (по TLB).</li>\n<li>Используется при проверке доступа к страницам (<code>AccessCheck</code>, по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.HasRole(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiuser-tojson\"></a></p>\n<h3><code>TIUser.ToJson</code></h3>\n<p>*** <code>TIUser.ToJson</code> — JSON пользователя ****</p>\n<p><code>function ToJson: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вызывает <code>IUser.ToJSON</code> (по TLB / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"tiuser-displayname\"></a></p>\n<h3><code>TIUser.DisplayName</code></h3>\n<p>*** <code>TIUser.DisplayName</code> — Отображаемое имя ****</p>\n<p><code>property DisplayName: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>DisplayName</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIUser.DisplayName` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.DisplayName));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiuser-groupid\"></a></p>\n<h3><code>TIUser.GroupID</code></h3>\n<p>*** <code>TIUser.GroupID</code> — Идентификатор группы ****</p>\n<p><code>property GroupID: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>GroupID</code> (тип <code>UserGroupType</code> в COM, Integer в скрипте) (по TLB / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIUser.GroupID` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.GroupID));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"tiuser-groupname\"></a></p>\n<h3><code>TIUser.GroupName</code></h3>\n<p>*** <code>TIUser.GroupName</code> — Имя группы ****</p>\n<p><code>property GroupName: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>GroupName</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIUser.GroupName` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.GroupName));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiuser-id\"></a></p>\n<h3><code>TIUser.ID</code></h3>\n<p>*** <code>TIUser.ID</code> — Идентификатор пользователя ****</p>\n<p><code>property ID: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>ID</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIUser.ID` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ID));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiuser-ip\"></a></p>\n<h3><code>TIUser.IP</code></h3>\n<p>*** <code>TIUser.IP</code> — IP-адрес ****</p>\n<p><code>property IP: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>IP</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIUser.IP` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.IP));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiuser-name\"></a></p>\n<h3><code>TIUser.Name</code></h3>\n<p>*** <code>TIUser.Name</code> — Имя пользователя ****</p>\n<p><code>property Name: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>Name</code> (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIUser.Name` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Name));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiuser-roles\"></a></p>\n<h3><code>TIUser.Roles</code></h3>\n<p>*** <code>TIUser.Roles</code> — Роли пользователя ****</p>\n<p><code>property Roles: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>Roles</code> (<code>OleVariant</code> / Variant) (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIUser.Roles` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Roles));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"timessage\"></a></p>\n<h2><code>TIMessage</code></h2>\n<p>Обёртка над <code>IMessage</code>: сообщение шины. Поля в основном только для чтения; исключение — <code>Result</code>, которое заполняют обработчики при <code>SendMsg</code>. Состав: полезная нагрузка <code>Value</code> (<code>Variant</code>), опциональный <code>Blob</code>, метка времени, признак устаревания <code>Expired</code> (устаревшие можно не обрабатывать), пользователь, таймаут, признак <code>Remote</code> (PluginAPI_desc.odt / TLB).</p>\n<p><a id=\"timessage-tojson\"></a></p>\n<h3><code>TIMessage.ToJson</code></h3>\n<p>*** <code>TIMessage.ToJson</code> — JSON сообщения ****</p>\n<p><code>function ToJson: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вызывает <code>IMessage.ToJSON</code> (по TLB / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"timessage-blob\"></a></p>\n<h3><code>TIMessage.Blob</code></h3>\n<p>*** <code>TIMessage.Blob</code> — Двоичные данные сообщения ****</p>\n<p><code>property Blob: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Объект <code>IBlob</code> с двоичной полезной нагрузкой; может передаваться между плагинами без копирования данных (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.Blob` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Blob));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"timessage-expired\"></a></p>\n<h3><code>TIMessage.Expired</code></h3>\n<p>*** <code>TIMessage.Expired</code> — Признак устаревания ****</p>\n<p><code>property Expired: Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Если сообщение устарело, обработчик может его не обрабатывать (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.Expired` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Expired));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"timessage-remote\"></a></p>\n<h3><code>TIMessage.Remote</code></h3>\n<p>*** <code>TIMessage.Remote</code> — Признак удалённого сообщения ****</p>\n<p><code>property Remote: Boolean</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Boolean</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>Remote</code> интерфейса <code>IMessage</code> (по TLB). Развёрнутая семантика в текстовом ODT не описана.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.Remote` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Remote));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"timessage-result\"></a></p>\n<h3><code>TIMessage.Result</code></h3>\n<p>*** <code>TIMessage.Result</code> — Результат обработки ****</p>\n<p><code>property Result: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Единственное поле сообщения, доступное для записи; используется при синхронном <code>SendMsg</code> (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.Result` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Result));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"timessage-timeout\"></a></p>\n<h3><code>TIMessage.Timeout</code></h3>\n<p>*** <code>TIMessage.Timeout</code> — Таймаут сообщения ****</p>\n<p><code>property Timeout: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Свойство <code>Timeout</code> (по TLB / RTTI).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.Timeout` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Timeout));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"timessage-timestamp\"></a></p>\n<h3><code>TIMessage.Timestamp</code></h3>\n<p>*** <code>TIMessage.Timestamp</code> — Время отправки ****</p>\n<p><code>property Timestamp: TDateTime</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TDateTime</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Дата/время отправки сообщения (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.Timestamp` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Timestamp));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"timessage-user\"></a></p>\n<h3><code>TIMessage.User</code></h3>\n<p>*** <code>TIMessage.User</code> — Пользователь сообщения ****</p>\n<p><code>property User: TIUser</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TIUser</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Объект <code>IUser</code> / <code>TIUser</code>, связанный с сообщением (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.User` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.User));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"timessage-value\"></a></p>\n<h3><code>TIMessage.Value</code></h3>\n<p>*** <code>TIMessage.Value</code> — Полезная нагрузка ****</p>\n<p><code>property Value: Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Простое значение типа <code>Variant</code> (часто JSON-структура) (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIMessage.Value` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Value));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest\"></a></p>\n<h2><code>TIWebServerRequest</code></h2>\n<p>Обёртка над <code>IWebServerRequest</code>: входящий HTTP-запрос страницы плагина. Доступны параметры (<code>Input</code> / <code>InputStr</code> / <code>InputArr</code>), заголовки, cookie, метод, URI, имя страницы и текущий пользователь. Вызывается из пула HTTP-потоков в <code>HandleRequest</code> (PluginAPI_desc.odt / TLB).</p>\n<p><a id=\"tiwebserverrequest-cookie\"></a></p>\n<h3><code>TIWebServerRequest.Cookie</code></h3>\n<p>*** <code>TIWebServerRequest.Cookie</code> — Cookie запроса ****</p>\n<p><code>function Cookie(Name: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя cookie запроса (по PluginAPI_desc.odt / TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Чтение cookie по имени (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.Cookie(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-header\"></a></p>\n<h3><code>TIWebServerRequest.Header</code></h3>\n<p>*** <code>TIWebServerRequest.Header</code> — Заголовок запроса ****</p>\n<p><code>function Header(Name: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя HTTP-заголовка запроса (по PluginAPI_desc.odt / TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Чтение HTTP-заголовка по имени (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.Header(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-input\"></a></p>\n<h3><code>TIWebServerRequest.Input</code></h3>\n<p>*** <code>TIWebServerRequest.Input</code> — Параметр запроса ****</p>\n<p><code>function Input(Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя параметра запроса (по PluginAPI_desc.odt / TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Параметр HTTP-запроса по имени (<code>OleVariant</code>) (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.Input(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-inputarr\"></a></p>\n<h3><code>TIWebServerRequest.InputArr</code></h3>\n<p>*** <code>TIWebServerRequest.InputArr</code> — Параметр-массив ****</p>\n<p><code>function InputArr(Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя параметра-массива запроса (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Параметр запроса как массив значений (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.InputArr(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-inputstr\"></a></p>\n<h3><code>TIWebServerRequest.InputStr</code></h3>\n<p>*** <code>TIWebServerRequest.InputStr</code> — Строковый параметр ****</p>\n<p><code>function InputStr(Name: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя строкового параметра запроса (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Параметр запроса как строка (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.InputStr(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-tojson\"></a></p>\n<h3><code>TIWebServerRequest.ToJson</code></h3>\n<p>*** <code>TIWebServerRequest.ToJson</code> — JSON запроса ****</p>\n<p><code>function ToJson: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Вызывает <code>IWebServerRequest.ToJSON</code> (по TLB / <code>fsCoreScript.pas</code>).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-currentuser\"></a></p>\n<h3><code>TIWebServerRequest.CurrentUser</code></h3>\n<p>*** <code>TIWebServerRequest.CurrentUser</code> — Текущий пользователь ****</p>\n<p><code>property CurrentUser: TIUser</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>TIUser</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Пользователь сессии / авторизации запроса; используется в проверках доступа (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIWebServerRequest.CurrentUser` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.CurrentUser));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-method\"></a></p>\n<h3><code>TIWebServerRequest.Method</code></h3>\n<p>*** <code>TIWebServerRequest.Method</code> — HTTP-метод ****</p>\n<p><code>property Method: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Метод запроса (GET, POST, …) (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIWebServerRequest.Method` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Method));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-page\"></a></p>\n<h3><code>TIWebServerRequest.Page</code></h3>\n<p>*** <code>TIWebServerRequest.Page</code> — Имя страницы ****</p>\n<p><code>property Page: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Имя зарегистрированной страницы плагина (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIWebServerRequest.Page` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Page));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverrequest-uri\"></a></p>\n<h3><code>TIWebServerRequest.URI</code></h3>\n<p>*** <code>TIWebServerRequest.URI</code> — URI запроса ****</p>\n<p><code>property URI: String</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>URI входящего запроса (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIWebServerRequest.URI` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.URI));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse\"></a></p>\n<h2><code>TIWebServerResponse</code></h2>\n<p>Обёртка над <code>IWebServerResponse</code>: формирование ответа. <code>ReturnPage</code> — HTML по шаблону из <code>Views</code> внутри общего каркаса сайта (partials вроде <code>{{&gt;header}}</code>); <code>ReturnError</code> — страница по <code>Error.html</code>; <code>ReturnText</code> / <code>ReturnBlob</code> — произвольный ответ (в COM для двоичных данных — <code>ReturnData</code>). Тексты и шаблоны — UTF-8. Дополнительно: cookie, заголовки, redirect, <code>CacheControl</code> (флаги кэша страницы из PluginAPI_desc.odt).</p>\n<p><a id=\"tiwebserverresponse-redirect\"></a></p>\n<h3><code>TIWebServerResponse.Redirect</code></h3>\n<p>*** <code>TIWebServerResponse.Redirect</code> — Перенаправление ****</p>\n<p><code>procedure Redirect(PageName: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>PageName: String</code> — имя страницы для перенаправления (по TLB / RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Перенаправление на указанную страницу (по TLB / RTTI).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Obj.Redirect(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse-returnblob\"></a></p>\n<h3><code>TIWebServerResponse.ReturnBlob</code></h3>\n<p>*** <code>TIWebServerResponse.ReturnBlob</code> — Двоичный ответ ****</p>\n<p><code>procedure ReturnBlob(Blob: Variant; Status: Integer)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Blob: Variant</code> — двоичное тело ответа (скрипковый аналог ReturnData, по PluginAPI_desc.odt / RTTI)<br>\n- <code>Status: Integer</code> — HTTP-статус (по RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Отдаёт двоичные данные с HTTP-статусом; скриптовый аналог COM-метода <code>ReturnData</code> (по PluginAPI_desc.odt / RTTI).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Obj.ReturnBlob(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/functions.txt</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse-returnerror\"></a></p>\n<h3><code>TIWebServerResponse.ReturnError</code></h3>\n<p>*** <code>TIWebServerResponse.ReturnError</code> — Страница ошибки ****</p>\n<p><code>procedure ReturnError(Text: String; Status: Integer)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Text: String</code> — текст ошибки; страница строится по шаблону Error.html (по PluginAPI_desc.odt)<br>\n- <code>Status: Integer</code> — HTTP-статус (по RTTI)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Формирует страницу ошибки по шаблону <code>Error.html</code> внутри общего каркаса сайта (по PluginAPI_desc.odt).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  Response.ReturnError('Ошибка обработки запроса', 500);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse-returnpage\"></a></p>\n<h3><code>TIWebServerResponse.ReturnPage</code></h3>\n<p>*** <code>TIWebServerResponse.ReturnPage</code> — Ответ HTML-страницей ****</p>\n<p><code>procedure ReturnPage(Name: String; InputJSON: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя HTML-шаблона в папке Views плагина (по PluginAPI_desc.odt)<br>\n- <code>InputJSON: Variant</code> — данные для шаблона основной части страницы (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Строит страницу по шаблону из папки <code>Views</code> плагина и встраивает в основной шаблон сайта; параметры передаются в шаблон (по PluginAPI_desc.odt).</li>\n<li>В шаблоне можно подключать partials, например <code>{{&gt;header}}</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // шаблон из Views плагина; InputJSON — данные для шаблона\n  Response.ReturnPage('Kiosk', _ObjEx(['Title', 'Готовность']));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse-returntext\"></a></p>\n<h3><code>TIWebServerResponse.ReturnText</code></h3>\n<p>*** <code>TIWebServerResponse.ReturnText</code> — Текстовый HTTP-ответ ****</p>\n<p><code>procedure ReturnText(Text, ContentType: String; Status: Integer)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Text: String</code> — полный текст ответа (по PluginAPI_desc.odt)<br>\n- <code>ContentType: String</code> — тип содержимого (по PluginAPI_desc.odt)<br>\n- <code>Status: Integer</code> — HTTP-статус (по PluginAPI_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Задаёт полный текст ответа, Content-Type и HTTP-статус (по PluginAPI_desc.odt).</li>\n<li>Текстовые ответы и шаблоны ожидаются в UTF-8.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  Response.ReturnText('{&quot;ok&quot;:true}', 'application/json', 200);\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse-setcookie\"></a></p>\n<h3><code>TIWebServerResponse.SetCookie</code></h3>\n<p>*** <code>TIWebServerResponse.SetCookie</code> — Установка cookie ****</p>\n<p><code>procedure SetCookie(Name, Value: String; Expires: TDateTime)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Name: String</code> — имя cookie (по TLB)<br>\n- <code>Value: String</code> — значение cookie (по TLB)<br>\n- <code>Expires: TDateTime</code> — срок действия cookie (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Задаёт cookie ответа с сроком действия (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Obj.SetCookie(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse-setheader\"></a></p>\n<h3><code>TIWebServerResponse.SetHeader</code></h3>\n<p>*** <code>TIWebServerResponse.SetHeader</code> — Установка заголовка ****</p>\n<p><code>procedure SetHeader(Value: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Value: String</code> — строка HTTP-заголовка ответа (по TLB)</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Добавляет/задаёт заголовок HTTP-ответа (по TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Obj.SetHeader(...); // см. параметры и сведения выше\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>\n<p><a id=\"tiwebserverresponse-cachecontrol\"></a></p>\n<h3><code>TIWebServerResponse.CacheControl</code></h3>\n<p>*** <code>TIWebServerResponse.CacheControl</code> — Флаги кэширования ответа ****</p>\n<p><code>property CacheControl: Integer</code></p>\n<p><strong>Входные параметры:</strong><br>\n<em>Параметры отсутствуют.</em></p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Integer</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Соответствует <code>WebPageOptionsType</code> / флагам вроде <code>WebPageCacheControlNoCache</code>, <code>WebPageCacheControlAgeDay</code> и др. (по PluginAPI_desc.odt / TLB).</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // Чтение свойства `TIWebServerResponse.CacheControl` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.CacheControl));\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/PluginAPI_desc.txt</code>; <code>Материалы для документации/source/PluginAPI_TLB.pas</code></p>\n<hr>","text":"Интерфейс сервера Раздел описывает объекты ядра, доступные скрипту как типизированные интерфейсы: конфигурация ( TICoreConfig ), сообщения ( TICoreMessages , TIMessage , TIBlob ), планировщик ( TICoreScheduler ), локаль ( TICoreLocale ), пользователь ( TIUser ), HTTP-запрос и ответ ( TIWebServerRequest , TIWebServerResponse ). Плагин встраивает страницы вызовом регистрации обработчика; при обращении к /core/plugins/ИмяПлагина/ИмяСтраницы ядро вызывает обработчик с парой Request/Response. Ответ формируют ReturnPage (шаблон из Views в UTF-8, встройка в общий каркас сайта), ReturnError , ReturnText / ReturnBlob . Страницы принято разделять на интерфейс, действия и данные с разными флагами аутентификации и кэша. TIBlob передаёт двоичные данные без лишнего копирования между плагинами. Планировщик ставит отправку сообщений по crontab-подобной строке или таймеру. Этот слой — полная модель «законов» сервера; упрощённые GetConfig* / PostMsg из предыдущих разделов дублируют часть возможностей для совместимости. Состав интерфейсов и сигнатуры подтверждены RTTI ( functions.txt ) и PluginAPI_TLB.pas . Поведенческие сведения взяты из PluginAPI_desc; где в материалах остаётся только сигнатура — стоит маркер дополнения. TIBlob Обёртка скрипта над COM-интерфейсом IBlob : контейнер двоичных данных без лишнего копирования между плагинами. У blob есть имя, тип содержимого, размер и сами данные; плагин может реализовать IBlob эффективно под свою модель памяти (по PluginAPI_desc.odt). В скрипте Data отдаётся как Variant-массив байт (копирование в VarArray в fsCoreScript.pas ). TIBlob.ToJson *** TIBlob.ToJson — JSON-представление blob **** function ToJson: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вызывает IBlob.ToJSON (по fsCoreScript.pas / TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas TIBlob.Name *** TIBlob.Name — Имя blob **** property Name: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство IBlob.Name — имя двоичного вложения (по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIBlob.Name` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Name));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas TIBlob.ContentType *** TIBlob.ContentType — Тип содержимого **** property ContentType: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство IBlob.ContentType — тип содержимого двоичных данных (по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIBlob.ContentType` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ContentType));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas TIBlob.Data *** TIBlob.Data — Данные blob **** property Data: Variant Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: В COM — указатель на данные ( IBlob.Data ). В скриптовой обёртке копируется в Variant-массив байт ( VarArray of varByte ) в TIBlob.GetData ( fsCoreScript.pas ). Пример вызова: begin\n  // Чтение свойства `TIBlob.Data` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Data));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas TIBlob.Size *** TIBlob.Size — Размер данных **** property Size: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Число байт в blob ( IBlob.Size , по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIBlob.Size` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Size));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas TICoreConfig Обёртка над ICoreConfig : пути сервера и плагина, адрес/порт, словарь параметров Values / Defaults , метаданные сообщений и ролей. Чтение Values[Name] — тот же слой конфигурации, что обслуживают устаревшие глобальные GetConfig* . MyPath — каталог текущего плагина; от него резолвятся относительные пути StringFromFile / FileFromString (по TLB / fsCoreScript.pas ). TICoreConfig.BinPath *** TICoreConfig.BinPath — Каталог двоичных файлов сервера **** property BinPath: String Входные параметры: Параметры отсутствуют. Возвращает: Значен","entryCount":86},{"id":"14-universalnye-zhurnaly","title":"Универсальные журналы","intro":"Универсальные журналы — постоянное хранилище бизнес-данных UniServer (взвешивания, справочники, связанные документы). Скрипт обращается к журналу по имени: GetRecord / GetRecords читают, SetRecord сохраняет документ- Variant (часто JSON-объект с полями вроде CODE , NUMB_TS ). Варианты *NT выполняют изменение без запуска триггеров; Async* ставят операцию в очередь сообщений без ожидания. GetQuery в","html":"<h1>Работа с универсальными журналами и справочниками</h1>\n<p>Универсальные журналы — постоянное хранилище бизнес-данных UniServer (взвешивания, справочники, связанные документы). Скрипт обращается к журналу по имени: <code>GetRecord</code> / <code>GetRecords</code> читают, <code>SetRecord</code> сохраняет документ-<code>Variant</code> (часто JSON-объект с полями вроде <code>CODE</code>, <code>NUMB_TS</code>). Варианты <code>*NT</code> выполняют изменение без запуска триггеров; <code>Async*</code> ставят операцию в очередь сообщений без ожидания. <code>GetQuery</code> выполняет SQL к SQLite-таблице журнала (имя таблицы всегда <code>Journal</code>); <code>GetQueryEx</code> — к внешней БД с синтаксисом этой БД. Связи (<code>SetLink</code> / <code>GetLink</code> / <code>DeleteLinks</code>) и вложения (<code>GetBlob</code> / <code>SetBlob</code>) дополняют документ без дублирования всей структуры. Сохранение идёт через шину сообщений (<code>Journal.Operation</code> и <code>SendMsg</code> в реализации) — журнал связан с общей моделью обмена платформы, а не является изолированным SQL-API.</p>\n<p><a id=\"getquery\"></a></p>\n<h3><code>GetQuery</code></h3>\n<p>*** <code>GetQuery</code> — SQL-запрос к журналу ****</p>\n<p><code>function GetQuery(Journal, SQL: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>SQL: String</code> — SQL-запрос; синтаксис SQLite; имя таблицы всегда Journal (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Использует SQLite; имя таблицы журнала всегда <code>Journal</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetQuery('WeighingJournal', 'SELECT MAX(RowID) FROM Journal'))); // SQL к журналу\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getqueryex\"></a></p>\n<h3><code>GetQueryEx</code></h3>\n<p>*** <code>GetQueryEx</code> — SQL-запрос к внешней БД ****</p>\n<p><code>function GetQueryEx(Journal, SQL: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>SQL: String</code> — SQL-запрос к внешней БД; синтаксис внешней БД (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Выполняет запрос к внешней базе данных.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  DebugLog(_ToStr(GetQueryEx('WeighingJournal', 'SELECT MAX(ID) FROM Weighing'))); // SQL к внешней БД\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getrecord\"></a></p>\n<h3><code>GetRecord</code></h3>\n<p>*** <code>GetRecord</code> — Чтение записи журнала ****</p>\n<p><code>function GetRecord(Journal, Code: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>Code: String</code> — идентификатор записи (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Получает запись по идентификатору <code>Code</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  Doc: Variant;\nbegin\n  Doc := GetRecord('WeighingJournal', 'D307EB...205027'); // запись по CODE\n  DebugLog(Doc.NUMB_TS);                                  // поле записи\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getrecords\"></a></p>\n<h3><code>GetRecords</code></h3>\n<p>*** <code>GetRecords</code> — Поиск записей журнала ****</p>\n<p><code>function GetRecords(Journal: String; Args: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>Args: Variant</code> — параметры поиска (в примерах ODT: Filter, SortField, SortDesc, MaxRows)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>В <code>Args</code> используются поля <code>Filter</code>, <code>SortField</code>, <code>SortDesc</code>, <code>MaxRows</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  Args, Docs, Doc: Variant;\n  i: Integer;\nbegin\n  Args := _Obj();                        // аргументы выборки\n  Args.Filter := _Obj();                 // фильтр\n  Args.Filter.NUMB_TS := 'A999AB45';     // условие по полю\n  Args.SortField := 'DATETIME_CREATE';   // сортировка\n  Args.SortDesc := True;                 // по убыванию\n  Args.MaxRows := 10;                    // лимит строк\n  Docs := GetRecords('WeighingJournal', Args); // выполнить поиск\n  for i := 0 to Docs._Count - 1 do begin\n    Doc := Docs.Value(i);                // очередная запись\n    DebugLog(Doc.NUMB_TS);               // вывести поле\n  end;\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setrecord\"></a></p>\n<h3><code>SetRecord</code></h3>\n<p>*** <code>SetRecord</code> — Сохранение записи журнала ****</p>\n<p><code>function SetRecord(Journal: String; Doc: Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>Doc: Variant</code> — данные записи (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Перед отправкой удаляет поле <code>DBID</code> из документа.</li>\n<li>Операция передаётся сообщением <code>Journal.Operation</code> через <code>SendMsg</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  Doc: Variant;\n  Code: String;\nbegin\n  Doc := _Obj();                         // документ записи\n  Doc.CODE := 'D307EB...205027';         // код записи\n  Doc.NUMB_TS := 'A999AB45';              // поле данных\n  Code := SetRecord('WeighingJournal', Doc); // сохранить запись\n  DebugLog(Code);                        // результат операции\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setrecordnt\"></a></p>\n<h3><code>SetRecordNT</code></h3>\n<p>*** <code>SetRecordNT</code> — Сохранение без триггеров ****</p>\n<p><code>function SetRecordNT(Journal: String; Doc: Variant): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — имя журнала (по EventScript_desc.odt)<br>\n- <code>Doc: Variant</code> — данные записи (по EventScript_desc.odt)</p>\n<p><strong>Возвращает:</strong></p>\n<p>результат операции записи; изменение выполняется без запуска триггеров (по EventScript_desc.odt)</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Выполняет изменение записи без запуска триггеров.</li>\n<li>Операция передаётся сообщением <code>Journal.Operation</code> через <code>SendMsg</code>.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  Doc: Variant;\nbegin\n  Doc := _Obj();\n  Doc.CODE := 'D307EB...205027';\n  Doc.NUMB_TS := 'A999AB45';\n  DebugLog(SetRecordNT('WeighingJournal', Doc)); // сохранить без запуска триггеров\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getview\"></a></p>\n<h3><code>GetView</code></h3>\n<p>*** <code>GetView</code> — Данные представления ****</p>\n<p><code>function GetView(Journal, Name: String; Params, Filter: Variant; SortField: String; SortDesc: Boolean; FirstRow, MaxRows: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Params: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Filter: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>SortField: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>SortDesc: Boolean</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>FirstRow: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>MaxRows: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Получает данные именованного представления журнала.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetView(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"execproc\"></a></p>\n<h3><code>ExecProc</code></h3>\n<p>*** <code>ExecProc</code> — Хранимая процедура журнала ****</p>\n<p><code>function ExecProc(Journal, Name: String; Args: Variant): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Выполняет хранимую процедуру журнала.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  Code: String;\nbegin\n  Code := '';\n  DebugLog(_ToStr(ExecProc('WeighingJournal', 'SetSlaveDoc', _ArrEx([Code])))); // хранимая процедура\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/_odt_extract/EventScript_desc.txt</code>; <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getblobs\"></a></p>\n<h3><code>GetBlobs</code></h3>\n<p>*** <code>GetBlobs</code> — Список вложений записи ****</p>\n<p><code>function GetBlobs(Journal, Code: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает вложения (blob) записи журнала.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetBlobs(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getblob\"></a></p>\n<h3><code>GetBlob</code></h3>\n<p>*** <code>GetBlob</code> — Чтение вложения ****</p>\n<p><code>function GetBlob(Journal, Code, Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Читает именованное вложение записи.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetBlob(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setblob\"></a></p>\n<h3><code>SetBlob</code></h3>\n<p>*** <code>SetBlob</code> — Запись вложения ****</p>\n<p><code>procedure SetBlob(Journal, Code, Name: String; Blob: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Blob: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Сохраняет именованное вложение записи.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // SetBlob(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setlink\"></a></p>\n<h3><code>SetLink</code></h3>\n<p>*** <code>SetLink</code> — Создание связи ****</p>\n<p><code>function SetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Caption: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkJournal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkType: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт связь записи с другой записью/журналом.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := SetLink(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"setlinknt\"></a></p>\n<h3><code>SetLinkNT</code></h3>\n<p>*** <code>SetLinkNT</code> — Создание связи без триггеров ****</p>\n<p><code>function SetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Caption: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkJournal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkType: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Создаёт связь без запуска триггеров.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := SetLinkNT(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getlink\"></a></p>\n<h3><code>GetLink</code></h3>\n<p>*** <code>GetLink</code> — Чтение связи ****</p>\n<p><code>function GetLink(Journal, Code, Name: String): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает данные связи по имени.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetLink(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getlink-link\"></a></p>\n<h3><code>GetLink_Link</code></h3>\n<p>*** <code>GetLink_Link</code> — Идентификатор связанной записи ****</p>\n<p><code>function GetLink_Link(Journal, Code, Name: String): String</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>String</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает строковый идентификатор связи.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetLink_Link(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"getlinks\"></a></p>\n<h3><code>GetLinks</code></h3>\n<p>*** <code>GetLinks</code> — Список связей ****</p>\n<p><code>function GetLinks(Journal, Code: String; MaxRows: Integer): Variant</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>MaxRows: Integer</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p>Значение типа <code>Variant</code> (тип подтверждён сигнатурой RTTI).</p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Возвращает связи записи.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetLinks(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"deletelinks\"></a></p>\n<h3><code>DeleteLinks</code></h3>\n<p>*** <code>DeleteLinks</code> — Удаление связей ****</p>\n<p><code>procedure DeleteLinks(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Удаляет связи записи.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // DeleteLinks(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"undeletelinks\"></a></p>\n<h3><code>UnDeleteLinks</code></h3>\n<p>*** <code>UnDeleteLinks</code> — Восстановление связей ****</p>\n<p><code>procedure UnDeleteLinks(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Восстанавливает удалённые связи.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // UnDeleteLinks(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"deletelinksnt\"></a></p>\n<h3><code>DeleteLinksNT</code></h3>\n<p>*** <code>DeleteLinksNT</code> — Удаление связей без триггеров ****</p>\n<p><code>procedure DeleteLinksNT(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Удаляет связи без запуска триггеров.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // DeleteLinksNT(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"undeletelinksnt\"></a></p>\n<h3><code>UnDeleteLinksNT</code></h3>\n<p>*** <code>UnDeleteLinksNT</code> — Восстановление связей без триггеров ****</p>\n<p><code>procedure UnDeleteLinksNT(Journal, Code, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Восстанавливает связи без запуска триггеров.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // UnDeleteLinksNT(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"asyncsetrecord\"></a></p>\n<h3><code>AsyncSetRecord</code></h3>\n<p>*** <code>AsyncSetRecord</code> — Асинхронное сохранение записи ****</p>\n<p><code>procedure AsyncSetRecord(Journal: String; Doc: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Doc: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ставит сохранение записи в очередь сообщений.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // AsyncSetRecord(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"asyncsetrecordnt\"></a></p>\n<h3><code>AsyncSetRecordNT</code></h3>\n<p>*** <code>AsyncSetRecordNT</code> — Асинхронное сохранение без триггеров ****</p>\n<p><code>procedure AsyncSetRecordNT(Journal: String; Doc: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Doc: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Асинхронное сохранение без запуска триггеров.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // AsyncSetRecordNT(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"asyncexecproc\"></a></p>\n<h3><code>AsyncExecProc</code></h3>\n<p>*** <code>AsyncExecProc</code> — Асинхронный вызов процедуры ****</p>\n<p><code>procedure AsyncExecProc(Journal, Name: String; Args: Variant)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Args: Variant</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ставит выполнение хранимой процедуры в очередь.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // AsyncExecProc(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"asyncsetlink\"></a></p>\n<h3><code>AsyncSetLink</code></h3>\n<p>*** <code>AsyncSetLink</code> — Асинхронное создание связи ****</p>\n<p><code>procedure AsyncSetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Caption: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkJournal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkType: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Ставит создание связи в очередь сообщений.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // AsyncSetLink(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>\n<p><a id=\"asyncsetlinknt\"></a></p>\n<h3><code>AsyncSetLinkNT</code></h3>\n<p>*** <code>AsyncSetLinkNT</code> — Асинхронная связь без триггеров ****</p>\n<p><code>procedure AsyncSetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String)</code></p>\n<p><strong>Входные параметры:</strong><br>\n- <code>Journal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Code: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Name: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Caption: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkJournal: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>LinkType: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).<br>\n- <code>Link: String</code> — &gt; <span style=\"color:#b00020;font-weight:bold;background:#fff3cd;padding:2px 6px;\">⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ:</span> в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI).</p>\n<p><strong>Возвращает:</strong></p>\n<p><em>Процедура ничего не возвращает.</em></p>\n<p><strong>Сведения из исходников / ODT:</strong></p>\n<ul>\n<li>Асинхронное создание связи без триггеров.</li>\n</ul>\n<p><strong>Пример вызова:</strong></p>\n<pre><code class=\"language-pascal\">begin\n  // AsyncSetLinkNT(...);\n  // &gt; ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend\n</code></pre>\n<p><em>Источник сведений:</em> <code>Материалы для документации/source/fsCoreScript.pas</code></p>\n<hr>","text":"Работа с универсальными журналами и справочниками Универсальные журналы — постоянное хранилище бизнес-данных UniServer (взвешивания, справочники, связанные документы). Скрипт обращается к журналу по имени: GetRecord / GetRecords читают, SetRecord сохраняет документ- Variant (часто JSON-объект с полями вроде CODE , NUMB_TS ). Варианты *NT выполняют изменение без запуска триггеров; Async* ставят операцию в очередь сообщений без ожидания. GetQuery выполняет SQL к SQLite-таблице журнала (имя таблицы всегда Journal ); GetQueryEx — к внешней БД с синтаксисом этой БД. Связи ( SetLink / GetLink / DeleteLinks ) и вложения ( GetBlob / SetBlob ) дополняют документ без дублирования всей структуры. Сохранение идёт через шину сообщений ( Journal.Operation и SendMsg в реализации) — журнал связан с общей моделью обмена платформы, а не является изолированным SQL-API. GetQuery *** GetQuery — SQL-запрос к журналу **** function GetQuery(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос; синтаксис SQLite; имя таблицы всегда Journal (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Использует SQLite; имя таблицы журнала всегда Journal . Пример вызова: begin\n  DebugLog(_ToStr(GetQuery('WeighingJournal', 'SELECT MAX(RowID) FROM Journal'))); // SQL к журналу\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetQueryEx *** GetQueryEx — SQL-запрос к внешней БД **** function GetQueryEx(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос к внешней БД; синтаксис внешней БД (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Выполняет запрос к внешней базе данных. Пример вызова: begin\n  DebugLog(_ToStr(GetQueryEx('WeighingJournal', 'SELECT MAX(ID) FROM Weighing'))); // SQL к внешней БД\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetRecord *** GetRecord — Чтение записи журнала **** function GetRecord(Journal, Code: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - Code: String — идентификатор записи (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Получает запись по идентификатору Code . Пример вызова: var\n  Doc: Variant;\nbegin\n  Doc := GetRecord('WeighingJournal', 'D307EB...205027'); // запись по CODE\n  DebugLog(Doc.NUMB_TS);                                  // поле записи\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas GetRecords *** GetRecords — Поиск записей журнала **** function GetRecords(Journal: String; Args: Variant): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - Args: Variant — параметры поиска (в примерах ODT: Filter, SortField, SortDesc, MaxRows) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: В Args используются поля Filter , SortField , SortDesc , MaxRows . Пример вызова: var\n  Args, Docs, Doc: Variant;\n  i: Integer;\nbegin\n  Args := _Obj();                        // аргументы выборки\n  Args.Filter := _Obj();                 // фильтр\n  Args.Filter.NUMB_TS := 'A999AB45';     // условие по полю\n  Args.SortField := 'DATETIME_CREATE';   // сортировка\n  Args.SortDesc := True;                 // по убыванию\n  Args.MaxRows := 10;                    // лимит строк\n  Docs := GetRecords('WeighingJournal', Args); // выполнить поиск\n  for i := 0 to Docs._Count - 1 do begin\n    Doc := Docs.Value(i);                // очередная запись\n    DebugLog(Doc.NUMB_TS);               // вывести поле\n  end;\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas SetRecord *** SetRecord — Сохранение записи журнала **** function SetRecord(Journal: String; Doc: Variant): String Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - Doc: Variant — данные записи (по EventScript_desc.odt) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Перед отправкой удаляет поле DBID из документа. Операция передаётся сообщением Journal.Operation через SendMsg . Пример вызова: var\n  Doc: Variant;\n  Code: String;\nbegin\n  Doc := _Obj();                         // документ записи\n  Doc.CODE := 'D307EB...205027';         // код записи\n  Doc.NUMB_TS := 'A999AB45';              // поле данных\n  Code := SetRecord('WeighingJournal', Doc); // сохранить запись\n  DebugLog(Code);                        // результат опе","entryCount":25}],"entries":[{"id":"tostr","sectionId":"01-obshchie-funktsii","title":"_ToStr","text":"_ToStr *** _ToStr — Преобразование значения в строку **** function _ToStr(V: Variant; Format: Boolean = False): String Входные параметры: - V: Variant — JSON-объект / JSON-массив или иное значение для преобразования в строку (по EventScript_desc.odt и fsCommon.pas ) - Format: Boolean = False — выполнять ли форматирование (переводы строк, отступы) текста JSON при переводе в строку (по EventScript_desc.odt) Возвращает: строковое представление значения; для JSON — JSON-текст (по EventScript_desc.odt / fsCommon.pas ) Сведения из исходников / ODT: Для JSON-объекта или JSON-массива возвращает его текстовое представление. При Format = True применяет JsonReformat . Для даты формирует ISO 8601 с разделителем T и миллисекундами. Пример вызова: var\n  x: Variant;                // JSON-объект\n  s: String;                 // строковый результат\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку в объект\n  s := _ToStr(x);            // преобразовать объект в строку\n  s := _ToStr(x, True);      // то же с форматированием JSON\n  DebugLog(s);               // записать результат в лог\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"todouble","sectionId":"01-obshchie-funktsii","title":"ToDouble","text":"ToDouble *** ToDouble — Преобразование в Double **** function ToDouble(V: Variant): Double Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Строковое значение преобразуется после удаления пробелов; остальные значения передаются стандартному преобразованию Variant. Пример вызова: var\n  d: Double;                 // числовой результат\nbegin\n  d := ToDouble('12.5');     // преобразовать строку в Double\n  DebugLog(_ToStr(d));       // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"newpackedguid","sectionId":"01-obshchie-funktsii","title":"NewPackedGuid","text":"NewPackedGuid *** NewPackedGuid — Создание packed GUID **** function NewPackedGuid: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Генерирует компактный идентификатор GUID. Пример вызова: var\n  g: String;                 // новый идентификатор\nbegin\n  g := NewPackedGuid;        // создать packed GUID\n  DebugLog(g);               // записать в лог\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"ispackedguid","sectionId":"01-obshchie-funktsii","title":"IsPackedGuid","text":"IsPackedGuid *** IsPackedGuid — Проверка packed GUID **** function IsPackedGuid(Guid: String): Boolean Входные параметры: - Guid: String — проверяемая строка Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Проверяет, что строка соответствует формату packed GUID. Пример вызова: var\n  ok: Boolean;               // результат проверки\nbegin\n  ok := IsPackedGuid(NewPackedGuid); // проверить формат GUID\n  DebugLog(_ToStr(ok));              // вывести результат проверки\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"sleep","sectionId":"01-obshchie-funktsii","title":"Sleep","text":"Sleep *** Sleep — Приостановка выполнения **** procedure Sleep(Milliseconds: Integer) Входные параметры: - Milliseconds: Integer — длительность паузы в миллисекундах; по fsCommon.pas ограничивается диапазоном 0…10000 Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Значение задержки ограничивается диапазоном от 0 до 10000 мс. Пример вызова: begin\n  Sleep(500);                // пауза 500 мс (в реализации ограничено 0…10000)\n  DebugLog('готово');        // продолжение после паузы\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"isempty","sectionId":"01-obshchie-funktsii","title":"IsEmpty","text":"IsEmpty *** IsEmpty — Проверка пустого значения **** function IsEmpty(V: Variant): Boolean Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Для JSON-массива или объекта проверяется Count = 0 . Для строк проверяется Length = 0 ; для остальных значений используется VarIsEmptyOrNull . Пример вызова: var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой массив\n  if IsEmpty(x) then         // проверить пустоту\n    DebugLog('пусто');       // ветка для пустого значения\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"varisemptyornull","sectionId":"01-obshchie-funktsii","title":"VarIsEmptyOrNull","text":"VarIsEmptyOrNull *** VarIsEmptyOrNull — Проверка Empty или Null **** function VarIsEmptyOrNull(V: Variant): Boolean Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вызывает VarIsEmptyOrNull для переданного Variant. Пример вызова: var\n  v: Variant;                // Variant без присвоенного значения\nbegin\n  if VarIsEmptyOrNull(v) then // проверка Empty/Null\n    DebugLog('Empty или Null');\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"stringfromfile","sectionId":"02-rabota-s-faylami","title":"StringFromFile","text":"StringFromFile *** StringFromFile — Чтение текста из файла **** function StringFromFile(FileName: String): String Входные параметры: - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: var\n  s: String;                 // содержимое файла\nbegin\n  s := StringFromFile('data.txt'); // прочитать файл\n  DebugLog(s);                     // вывести содержимое\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"filefromstring","sectionId":"02-rabota-s-faylami","title":"FileFromString","text":"FileFromString *** FileFromString — Запись текста в файл **** procedure FileFromString(Content: String; FileName: String) Входные параметры: - Content: String — текст для записи в файл - FileName: String — путь к файлу; относительный путь резолвится от MyPath (по fsCoreScript.pas ) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Относительный путь разрешается относительно MyPath . Пример вызова: begin\n  FileFromString('text', 'out.txt'); // записать строку в файл\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"quotedstr","sectionId":"03-rabota-so-strokami","title":"QuotedStr","text":"QuotedStr *** QuotedStr — Заключение строки в кавычки **** function QuotedStr(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает строку в кавычках Pascal. Пример вызова: var\n  s: String;                 // результат с кавычками\nbegin\n  s := QuotedStr('abc');     // обернуть строку в кавычки\n  DebugLog(s);               // вывести результат\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"uppercase","sectionId":"03-rabota-so-strokami","title":"UpperCase","text":"UpperCase *** UpperCase — Верхний регистр **** function UpperCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в верхний регистр. Пример вызова: begin\n  DebugLog(UpperCase('abc')); // преобразовать к верхнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"lowercase","sectionId":"03-rabota-so-strokami","title":"LowerCase","text":"LowerCase *** LowerCase — Нижний регистр **** function LowerCase(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Переводит строку в нижний регистр. Пример вызова: begin\n  DebugLog(LowerCase('ABC')); // преобразовать к нижнему регистру\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"length","sectionId":"03-rabota-so-strokami","title":"Length","text":"Length *** Length — Длина строки **** function Length(S: String): Integer Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает число символов в строке. Пример вызова: begin\n  DebugLog(_ToStr(Length('abc'))); // длина строки\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"pos","sectionId":"03-rabota-so-strokami","title":"Pos","text":"Pos *** Pos — Поиск подстроки **** function Pos(SubStr: String; Str: String): Integer Входные параметры: - SubStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Str: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используется регистронезависимый поиск PosI . Пример вызова: begin\n  DebugLog(_ToStr(Pos('b', 'abc'))); // позиция подстроки (поиск без учёта регистра)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"stringreplace","sectionId":"03-rabota-so-strokami","title":"StringReplace","text":"StringReplace *** StringReplace — Замена подстрок **** function StringReplace(const S, OldPattern, NewPattern: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - OldPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NewPattern: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Используются флаги rfReplaceAll и rfIgnoreCase . Пример вызова: begin\n  DebugLog(StringReplace('a-a', 'a', 'x')); // заменить все вхождения без учёта регистра\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"format","sectionId":"03-rabota-so-strokami","title":"Format","text":"Format *** Format — Форматирование строки **** function Format(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Собирает строку по шаблону и аргументам. Пример вызова: begin\n  DebugLog(Format('%s-%d', ['A', 1])); // сформировать строку по шаблону Format\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"formatutf8","sectionId":"03-rabota-so-strokami","title":"FormatUtf8","text":"FormatUtf8 *** FormatUtf8 — Форматирование UTF-8 **** function FormatUtf8(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Форматирует строку с учётом UTF-8. Пример вызова: begin\n  DebugLog(FormatUtf8('%s', ['тест'])); // форматирование через FormatUtf8\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"formatjson","sectionId":"03-rabota-so-strokami","title":"FormatJson","text":"FormatJson *** FormatJson — Форматирование для JSON **** function FormatJson(Fmt: String; Args: array of Variant): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: array of Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Форматирует строку для использования в JSON-контексте. Пример вызова: begin\n  DebugLog(FormatJson('{\"n\":?}', ['x'])); // форматирование с JSON-экранированием аргументов\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"formatfloat","sectionId":"03-rabota-so-strokami","title":"FormatFloat","text":"FormatFloat *** FormatFloat — Форматирование вещественного числа **** function FormatFloat(Fmt: String; Value: Double): String Входные параметры: - Fmt: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Форматирует Double по заданному шаблону. Пример вызова: begin\n  DebugLog(FormatFloat('0.00', 1.5)); // формат вещественного числа\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"iso8601todatetime","sectionId":"04-data-i-vremya","title":"Iso8601ToDateTime","text":"Iso8601ToDateTime *** Iso8601ToDateTime — Разбор ISO 8601 **** function Iso8601ToDateTime(S: String): TDateTime Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует строку даты и времени формата ISO 8601 в TDateTime . Пример вызова: var\n  x: TDateTime;              // дата/время\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать ISO8601\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"datetimetoiso8601","sectionId":"04-data-i-vremya","title":"DateTimeToIso8601","text":"DateTimeToIso8601 *** DateTimeToIso8601 — Форматирование ISO 8601 **** function DateTimeToIso8601(D: TDateTime): String Входные параметры: - D: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует TDateTime в строку ISO 8601. Пример вызова: var\n  x: TDateTime;              // дата/время\n  s: String;                 // строка ISO8601\nbegin\n  x := NowPrecise;           // текущее время\n  s := DateTimeToIso8601(x); // преобразовать в ISO8601\n  DebugLog(s);               // вывести строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"secondsbetween","sectionId":"04-data-i-vremya","title":"SecondsBetween","text":"SecondsBetween *** SecondsBetween — Разница в секундах **** function SecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество секунд между двумя датами. Пример вызова: var\n  x: TDateTime;              // опорная дата\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00'); // разобрать дату\n  DebugLog(_ToStr(SecondsBetween(NowPrecise, x))); // разница в секундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"millisecondsbetween","sectionId":"04-data-i-vremya","title":"MillisecondsBetween","text":"MillisecondsBetween *** MillisecondsBetween — Разница в миллисекундах **** function MillisecondsBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество миллисекунд между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MillisecondsBetween(NowPrecise, x))); // разница в миллисекундах\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"minutesbetween","sectionId":"04-data-i-vremya","title":"MinutesBetween","text":"MinutesBetween *** MinutesBetween — Разница в минутах **** function MinutesBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество минут между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(MinutesBetween(NowPrecise, x))); // разница в минутах\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hoursbetween","sectionId":"04-data-i-vremya","title":"HoursBetween","text":"HoursBetween *** HoursBetween — Разница в часах **** function HoursBetween(Now, Then: TDateTime): Integer Входные параметры: - Now: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Then: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Количество часов между двумя датами. Пример вызова: var\n  x: TDateTime;\nbegin\n  x := Iso8601ToDateTime('2020-01-01T12:00:00');\n  DebugLog(_ToStr(HoursBetween(NowPrecise, x))); // разница в часах\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"nowprecise","sectionId":"04-data-i-vremya","title":"NowPrecise","text":"NowPrecise *** NowPrecise — Текущее локальное время **** function NowPrecise: TDateTime Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает текущее локальное время с высокой точностью. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(NowPrecise)); // текущее локальное время\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"nowutcprecise","sectionId":"04-data-i-vremya","title":"NowUTCPrecise","text":"NowUTCPrecise *** NowUTCPrecise — Текущее время UTC **** function NowUTCPrecise: TDateTime Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает текущее время UTC с высокой точностью. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(NowUTCPrecise)); // текущее UTC-время\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"inchour","sectionId":"04-data-i-vremya","title":"IncHour","text":"IncHour *** IncHour — Увеличение часов **** function IncHour(const Value: TDateTime; const NumberOfHours: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfHours: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Сдвигает TDateTime на заданное число часов. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncHour(NowPrecise, 1))); // сдвиг на 1 час\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incday","sectionId":"04-data-i-vremya","title":"IncDay","text":"IncDay *** IncDay — Увеличение дней **** function IncDay(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Сдвигает TDateTime на заданное число дней. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncDay(NowPrecise, 1))); // сдвиг на 1 день\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incminute","sectionId":"04-data-i-vremya","title":"IncMinute","text":"IncMinute *** IncMinute — Увеличение минут **** function IncMinute(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Несмотря на имя RTTI-параметра NumberOfDays , реализация вызывает DateUtils.IncMinute . Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncMinute(NowPrecise, 1))); // сдвиг на 1 минуту\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incsecond","sectionId":"04-data-i-vremya","title":"IncSecond","text":"IncSecond *** IncSecond — Увеличение секунд **** function IncSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Несмотря на имя RTTI-параметра NumberOfDays , реализация вызывает DateUtils.IncSecond . Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncSecond(NowPrecise, 1))); // сдвиг на 1 секунду\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"incmillisecond","sectionId":"04-data-i-vremya","title":"IncMilliSecond","text":"IncMilliSecond *** IncMilliSecond — Увеличение миллисекунд **** function IncMilliSecond(const Value: TDateTime; const NumberOfDays: Integer): TDateTime Входные параметры: - Value: TDateTime — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - NumberOfDays: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Несмотря на имя RTTI-параметра NumberOfDays , реализация вызывает DateUtils.IncMilliSecond . Пример вызова: begin\n  DebugLog(DateTimeToIso8601(IncMilliSecond(NowPrecise, 100))); // сдвиг на 100 мс\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"ticksperiodtotext","sectionId":"04-data-i-vremya","title":"TicksPeriodToText","text":"TicksPeriodToText *** TicksPeriodToText — Период тиков в текст **** function TicksPeriodToText(const ATicks: Integer): String Входные параметры: - ATicks: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует период в тиках в текстовое представление. Пример вызова: begin\n  DebugLog(TicksPeriodToText(1500)); // текстовое представление периода в тиках\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"bytetohex","sectionId":"05-hex-preobrazovanie","title":"ByteToHex","text":"ByteToHex *** ByteToHex — Byte в hex **** function ByteToHex(Value: Byte): String Входные параметры: - Value: Byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 2 символа. Пример вызова: begin\n  DebugLog(ByteToHex(255)); // Byte -> hex (ширина 2)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"wordtohex","sectionId":"05-hex-preobrazovanie","title":"WordToHex","text":"WordToHex *** WordToHex — Word в hex **** function WordToHex(Value: Word): String Входные параметры: - Value: Word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 4 символа. Пример вызова: begin\n  DebugLog(WordToHex(65535)); // Word -> hex (ширина 4)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"inttohex","sectionId":"05-hex-preobrazovanie","title":"IntToHex","text":"IntToHex *** IntToHex — Integer в hex **** function IntToHex(Value: Integer): String Входные параметры: - Value: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 8 символов. Пример вызова: begin\n  DebugLog(IntToHex(1)); // Integer -> hex (ширина 8)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"int64tohex","sectionId":"05-hex-preobrazovanie","title":"Int64ToHex","text":"Int64ToHex *** Int64ToHex — Int64 в hex **** function Int64ToHex(Value: Int64): String Входные параметры: - Value: Int64 — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает hex-строку фиксированной ширины 16 символов. Пример вызова: begin\n  DebugLog(Int64ToHex(1)); // Int64 -> hex (ширина 16)\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"doubletohex","sectionId":"05-hex-preobrazovanie","title":"DoubleToHex","text":"DoubleToHex *** DoubleToHex — Double в hex **** function DoubleToHex(Value: Double): String Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Double в hex-строку. Пример вызова: begin\n  DebugLog(DoubleToHex(1.0)); // Double -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"singletohex","sectionId":"05-hex-preobrazovanie","title":"SingleToHex","text":"SingleToHex *** SingleToHex — Single в hex **** function SingleToHex(Value: Single): String Входные параметры: - Value: Single — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует Single в hex-строку. Пример вызова: begin\n  DebugLog(SingleToHex(1.0)); // Single -> hex\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextobyte","sectionId":"05-hex-preobrazovanie","title":"HexToByte","text":"HexToByte *** HexToByte — Hex в Byte **** function HexToByte(Value: String): byte Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа byte (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Byte . Пример вызова: begin\n  DebugLog(_ToStr(HexToByte('FF'))); // hex -> Byte\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextoword","sectionId":"05-hex-preobrazovanie","title":"HexToWord","text":"HexToWord *** HexToWord — Hex в Word **** function HexToWord(Value: String): Integer Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в целое (Word/Integer по RTTI). Пример вызова: begin\n  DebugLog(_ToStr(HexToWord('FFFF'))); // hex -> Word\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextolongword","sectionId":"05-hex-preobrazovanie","title":"HexToLongWord","text":"HexToLongWord *** HexToLongWord — Hex в LongWord **** function HexToLongWord(Value: String): Word Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Word (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в LongWord (сигнатура RTTI: Word). Пример вызова: begin\n  DebugLog(_ToStr(HexToLongWord('00000001'))); // hex -> LongWord\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextoint","sectionId":"05-hex-preobrazovanie","title":"HexToInt","text":"HexToInt *** HexToInt — Hex в Integer **** function HexToInt(Value: String): Integer Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Integer . Пример вызова: begin\n  DebugLog(_ToStr(HexToInt('00000001'))); // hex -> Integer\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextoint64","sectionId":"05-hex-preobrazovanie","title":"HexToInt64","text":"HexToInt64 *** HexToInt64 — Hex в Int64 **** function HexToInt64(Value: String): Int64 Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Int64 (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Int64 . Пример вызова: begin\n  DebugLog(_ToStr(HexToInt64('0000000000000001'))); // hex -> Int64\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextodouble","sectionId":"05-hex-preobrazovanie","title":"HexToDouble","text":"HexToDouble *** HexToDouble — Hex в Double **** function HexToDouble(Value: String): Double Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Double . Пример вызова: begin\n  DebugLog(_ToStr(HexToDouble(DoubleToHex(1.5)))); // hex -> Double\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextosingle","sectionId":"05-hex-preobrazovanie","title":"HexToSingle","text":"HexToSingle *** HexToSingle — Hex в Single **** function HexToSingle(Value: String): Single Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Single (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует hex-строку в Single . Пример вызова: begin\n  DebugLog(_ToStr(HexToSingle(SingleToHex(1.5)))); // hex -> Single\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"strtohex","sectionId":"05-hex-preobrazovanie","title":"StrToHex","text":"StrToHex *** StrToHex — Преобразование строки в hex **** function StrToHex(S: String; CodePage: Integer): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - CodePage: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Для заданной кодовой страницы используется TSynAnsiConvert . Пример вызова: begin\n  DebugLog(StrToHex('A', 1251)); // строка -> hex с кодовой страницей\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextostr","sectionId":"05-hex-preobrazovanie","title":"HexToStr","text":"HexToStr *** HexToStr — Преобразование hex в строку **** function HexToStr(Value: String; CodePage: Integer): String Входные параметры: - Value: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - CodePage: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Для заданной кодовой страницы используется TSynAnsiConvert . Пример вызова: begin\n  DebugLog(HexToStr(StrToHex('A', 1251), 1251)); // hex -> строка с кодовой страницей\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"setformatprecision","sectionId":"06-format-veschestvennyh","title":"SetFormatPrecision","text":"SetFormatPrecision *** SetFormatPrecision — Округление по точности **** function SetFormatPrecision(Value: Double; Precision: Integer): Double Входные параметры: - Value: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вычисляет round(Value * 10^Precision) / 10^Precision . Пример вызова: begin\n  DebugLog(_ToStr(SetFormatPrecision(1.2345, 2))); // округление до 2 знаков\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"setformatroundfromdiscret","sectionId":"06-format-veschestvennyh","title":"SetFormatRoundFromDiscret","text":"SetFormatRoundFromDiscret *** SetFormatRoundFromDiscret — Округление по дискрету **** function SetFormatRoundFromDiscret(Value: double; Precision: integer; Discret: double): Double Входные параметры: - Value: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Precision: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Discret: double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Если Discret < 0.00000001 , он заменяется на 0.00000001 . Вычисляет SetFormatPrecision(round(Value / Discret) * Discret, Precision) . Пример вызова: begin\n  DebugLog(_ToStr(SetFormatRoundFromDiscret(1.23, 2, 0.05))); // округление к дискрету\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc8summod2","sectionId":"07-crc8-crc16","title":"HexToCRC8SumMod2","text":"HexToCRC8SumMod2 *** HexToCRC8SumMod2 — CRC8 в hex **** function HexToCRC8SumMod2(SourceStr: String; AIndex, Count: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Count: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8SumMod2('0102', 0, 2, 0)); // CRC8 SumMod2 по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc8sum","sectionId":"07-crc8-crc16","title":"HexToCRC8Sum","text":"HexToCRC8Sum *** HexToCRC8Sum — CRC8 в hex **** function HexToCRC8Sum(SourceStr: String; AIndex, ACount: integer; FirstByte: byte): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstByte: byte — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  DebugLog(HexToCRC8Sum('0102', 0, 2, 0)); // CRC8 Sum по hex-данным\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc8polinom","sectionId":"07-crc8-crc16","title":"HexToCRC8Polinom","text":"HexToCRC8Polinom *** HexToCRC8Polinom — CRC8 по полиному **** function HexToCRC8Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ReflIn: boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - XorOut: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC8 представлен hex-строкой из 2 символов. Пример вызова: begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC8Polinom('0102', 0, 2, 0, 0, False, 0));\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"hextocrc16polinom","sectionId":"07-crc8-crc16","title":"HexToCRC16Polinom","text":"HexToCRC16Polinom *** HexToCRC16Polinom — CRC16 по полиному **** function HexToCRC16Polinom(SourceStr: String; AIndex, ACount: integer; Poly, Init: word; ReflIn: boolean; XorOut: word): String Входные параметры: - SourceStr: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - AIndex: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ACount: integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Poly: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Init: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ReflIn: boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - XorOut: word — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Результат CRC16 представлен hex-строкой из 4 символов. Пример вызова: begin\n  // Параметры Poly/Init/ReflIn/XorOut задаются по требованиям протокола оборудования\n  DebugLog(HexToCRC16Polinom('0102', 0, 2, 0, 0, False, 0));\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"tdocvariantkind","sectionId":"08-tipy-dannyh","title":"TDocVariantKind","text":"TDocVariantKind *** TDocVariantKind — Тип JSON-переменной **** type TDocVariantKind = (dvUndefined, dvObject, dvArray) Входные параметры: Параметры отсутствуют. Возвращает: Тип-перечисление с вариантами, указанными в декларации functions.txt . Сведения из исходников / ODT: Перечисление dvUndefined / dvObject / dvArray . Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := TDocVariantKind(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/functions.txt"},{"id":"quotedstrjson","sectionId":"09-json-obekty","title":"QuotedStrJSON","text":"QuotedStrJSON *** QuotedStrJSON — Строка в кавычках JSON **** function QuotedStrJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Заключает строку в JSON-кавычки. Пример вызова: begin\n  DebugLog(QuotedStrJSON('a\"b')); // строка в JSON-кавычках с экранированием\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"escapejson","sectionId":"09-json-obekty","title":"EscapeJSON","text":"EscapeJSON *** EscapeJSON — Экранирование JSON **** function EscapeJSON(S: String): String Входные параметры: - S: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Экранирует спецсимволы для JSON-строки. Пример вызова: begin\n  DebugLog(EscapeJSON('a\"b')); // экранирование спецсимволов JSON без внешних кавычек\nend Источник сведений: Материалы для документации/source/fsCommon.pas"},{"id":"arr","sectionId":"09-json-obekty","title":"_Arr","text":"_Arr *** _Arr — Создание JSON-массива **** function _Arr: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-массива (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-массива. Пример вызова: var\n  x: Variant;                // JSON-массив\nbegin\n  x := _Arr();               // создать пустой JSON-массив\n  x.Add(10);                 // добавить элемент\n  DebugLog(_ToStr(x));       // вывести массив как строку\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"arrex","sectionId":"09-json-obekty","title":"_ArrEx","text":"_ArrEx *** _ArrEx — Создание JSON-массива со значениями **** function _ArrEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — массив начальных значений для JSON-массива (по EventScript_desc.odt) Возвращает: JSON-массив с добавленными значениями (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-массив и добавляет переданные значения. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ArrEx([1, 2, 3]);     // создать массив с начальными значениями\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"obj","sectionId":"09-json-obekty","title":"_Obj","text":"_Obj *** _Obj — Создание JSON-объекта **** function _Obj: Variant Входные параметры: Параметры отсутствуют. Возвращает: переменная для хранения JSON-объекта (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт переменную для хранения JSON-объекта. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Obj();               // создать пустой JSON-объект\n  x.AddValue('ID', 10);      // добавить поле\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"objex","sectionId":"09-json-obekty","title":"_ObjEx","text":"_ObjEx *** _ObjEx — Создание JSON-объекта с парами **** function _ObjEx(A: array of Variant): Variant Входные параметры: - A: array of Variant — пары имя–значение для JSON-объекта (по EventScript_desc.odt) Возвращает: JSON-объект с переданными парами имя–значение (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект из переданных пар имя—значение. Пример вызова: var\n  x: Variant;\nbegin\n  x := _ObjEx(['ID', 10, 'Name', 'X']); // объект из пар имя/значение\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"json","sectionId":"09-json-obekty","title":"_Json","text":"_Json *** _Json — Разбор JSON-строки **** function _Json(S: String): Variant Входные параметры: - S: String — строка описания массива или объекта JSON (по EventScript_desc.odt) Возвращает: JSON-объект или JSON-массив, созданный из строки (по EventScript_desc.odt) Сведения из исходников / ODT: Создаёт JSON-объект или JSON-массив из строки описания. Пример вызова: var\n  x: Variant;\nbegin\n  x := _Json('{\"ID\":10}');   // разобрать JSON-строку\n  DebugLog(_ToStr(x));\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCommon.pas"},{"id":"copy","sectionId":"09-json-obekty","title":"_Copy","text":"_Copy *** _Copy — Копирование значения **** function _Copy(V: Variant): Variant Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: JSON-объект или JSON-массив копируется через его строковое представление; прочие значения возвращаются без такого копирования. Пример вызова: var\n  a, b: Variant;\nbegin\n  a := _Json('{\"ID\":10}');   // исходный объект\n  b := _Copy(a);             // копия JSON-значения\n  DebugLog(_ToStr(b));\nend Источник сведений: Материалы для документации/source/fsCommon.pas JSON-объект / JSON-массив : свойства и методы"},{"id":"json-json-kind","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив._Kind","text":"JSON-объект / JSON-массив._Kind *** JSON-объект / JSON-массив._Kind — Тип JSON-переменной **** property _Kind: TDocVariantKind Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TDocVariantKind (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает dvObject для объекта или dvArray для массива. Пример вызова: begin\n  // Свойство `_Kind` доступно у соответствующего объекта интерфейса.\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-count","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив._Count","text":"JSON-объект / JSON-массив._Count *** JSON-объект / JSON-массив._Count — Количество элементов **** property _Count: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает количество элементов массива или объекта. Пример вызова: begin\n  // Свойство `_Count` доступно у соответствующего объекта интерфейса.\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: пример чтения/записи конкретного свойства в материалах отсутствует.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-name","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив.Name","text":"JSON-объект / JSON-массив.Name *** JSON-объект / JSON-массив.Name — Имя элемента объекта **** function Name(Idx: Integer): String Входные параметры: - Idx: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает имя значения в объекте по индексу. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Name(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-value","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив.Value","text":"JSON-объект / JSON-массив.Value *** JSON-объект / JSON-массив.Value — Значение элемента **** function Value(Idx: Integer): Variant Входные параметры: - Idx: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает или задаёт значение в массиве либо объекте. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Value(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-exists","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив.Exists","text":"JSON-объект / JSON-массив.Exists *** JSON-объект / JSON-массив.Exists — Проверка существования **** function Exists(Name: String): Boolean Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Проверяет имя в объекте или значение в массиве. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Exists(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-add","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив.Add","text":"JSON-объект / JSON-массив.Add *** JSON-объект / JSON-массив.Add — Добавление в массив **** function Add(V: Variant): Integer Входные параметры: - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Добавляет значение в массив и возвращает его индекс. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := Add(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-addvalue","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив.AddValue","text":"JSON-объект / JSON-массив.AddValue *** JSON-объект / JSON-массив.AddValue — Добавление в объект **** function AddValue(Name: String; V: Variant): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - V: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Добавляет имя и значение в объект и возвращает индекс. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := AddValue(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-delete","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив.Delete","text":"JSON-объект / JSON-массив.Delete *** JSON-объект / JSON-массив.Delete — Удаление элемента **** procedure Delete(Idx: Integer) Входные параметры: - Idx: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Удаляет элемент из массива или объекта. Пример вызова: begin\n  // Delete(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"json-json-tostring","sectionId":"09-json-obekty","title":"JSON-объект / JSON-массив.ToString","text":"JSON-объект / JSON-массив.ToString *** JSON-объект / JSON-массив.ToString — Преобразование в строку **** function ToString: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Преобразует массив или объект в строку. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := ToString(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsJsonVar.pas"},{"id":"debuglog","sectionId":"10-obshchie-funktsii-servera","title":"DebugLog","text":"DebugLog *** DebugLog — Вывод строки в лог файл плагина **** procedure DebugLog(V: Variant) Входные параметры: - V: Variant — параметр для вывода в лог файл (по примеру в functions.txt ) Возвращает: Процедура ничего не возвращает. (по примеру в functions.txt блок «Возвращает:» пуст) Сведения из исходников / ODT: Записывает указанное значение в EventScript.log . Пример вызова: var\n  x: String;                 // строка для вывода\nbegin\n  x := '{\"ID\":10}';          // подготовить текст\n  DebugLog(x);               // записать значение в лог файл плагина\nend Источник сведений: Материалы для документации/functions.txt ; Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getconfigvalue","sectionId":"10-obshchie-funktsii-servera","title":"GetConfigValue","text":"GetConfigValue *** GetConfigValue — Прочитать значение параметра настройки по наименованию **** function GetConfigValue(Name: String): Variant Входные параметры: - Name: String — строка с именем параметра настройки (по примеру в functions.txt ) Возвращает: значение параметра настройки (по примеру в functions.txt ) Сведения из исходников / ODT: Читает конфигурацию по имени. Пример вызова: var\n  x: Variant;                // значение настройки\nbegin\n  x := GetConfigValue('Core.ServerID'); // прочитать параметр по имени\n  DebugLog(x);                          // вывести прочитанное значение в лог\nend Источник сведений: Материалы для документации/functions.txt ; Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getconfigfloatdef","sectionId":"10-obshchie-funktsii-servera","title":"GetConfigFloatDef","text":"GetConfigFloatDef *** GetConfigFloatDef — Чтение Double с значением по умолчанию **** function GetConfigFloatDef(Name: String; ADefValue: Double): Double Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Double — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Double;\nbegin\n  v := GetConfigFloatDef('SomeFloat', 0.0); // читать Double с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getconfigintdef","sectionId":"10-obshchie-funktsii-servera","title":"GetConfigIntDef","text":"GetConfigIntDef *** GetConfigIntDef — Чтение Integer с значением по умолчанию **** function GetConfigIntDef(Name: String; ADefValue: Integer): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Integer;\nbegin\n  v := GetConfigIntDef('SomeInt', 0); // читать Integer с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getconfigbooldef","sectionId":"10-obshchie-funktsii-servera","title":"GetConfigBoolDef","text":"GetConfigBoolDef *** GetConfigBoolDef — Чтение Boolean с значением по умолчанию **** function GetConfigBoolDef(Name: String; ADefValue: Boolean): Boolean Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - ADefValue: Boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает CoreConfig.Values и применяет преобразование с ADefValue . Пример вызова: var\n  v: Boolean;\nbegin\n  v := GetConfigBoolDef('SomeBool', False); // читать Boolean с значением по умолчанию\n  DebugLog(_ToStr(v));\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"newmessage","sectionId":"11-otpravka-soobscheniy","title":"NewMessage","text":"NewMessage *** NewMessage — Создание сообщения **** function NewMessage(MsgName: String; Value: Variant): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения (тип Variant по RTTI) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;             // сообщение\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // создать сообщение по имени\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"newmessageex","sectionId":"11-otpravka-soobscheniy","title":"NewMessageEx","text":"NewMessageEx *** NewMessageEx — Создание расширенного сообщения **** function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant Входные параметры: - MsgName: String — имя сообщения вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка сообщения ( Variant , по PluginAPI_desc.odt / RTTI) - Blob: Variant — двоичные данные сообщения ( IBlob / TIBlob , по PluginAPI_desc.odt) - CurrentUser: Variant — пользователь, от имени которого создаётся сообщение ( IUser / TIUser , по TLB) - Timeout: Integer — таймаут сообщения в миллисекундах (по RTTI / TLB) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя сообщения имеет вид ИмяПлагина.ИмяСообщения . Пример вызова: var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"postmsg","sectionId":"11-otpravka-soobscheniy","title":"PostMsg","text":"PostMsg *** PostMsg — Асинхронная отправка сообщения **** function PostMsg(Msg: Variant): Boolean Входные параметры: - Msg: Variant — сообщение для асинхронной постановки в очередь (по PluginAPI_desc.odt) Возвращает: по PluginAPI_desc.odt для интерфейса ядра PostMsg возвращает WordBool/False при отсутствии обработчиков; в глобальной RTTI-процедуре procedure PostMsg возвращаемого значения нет Сведения из исходников / ODT: Постановка в очередь выполняется асинхронно; возвращает False , если обработчиков нет. Пример вызова: var\n  LMsg: Variant;\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // подготовить сообщение\n  PostMsg(LMsg);                              // отправить асинхронно\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"sendmsg","sectionId":"11-otpravka-soobscheniy","title":"SendMsg","text":"SendMsg *** SendMsg — Синхронная отправка сообщения **** function SendMsg(Msg: Variant; Timeout: Integer): Variant Входные параметры: - Msg: Variant — сообщение для синхронной отправки (по PluginAPI_desc.odt) - Timeout: Integer — таймаут ожидания в миллисекундах; -1 — бесконечное ожидание (по PluginAPI_desc.odt) Возвращает: результат обработки (поле Result сообщения) после ожидания до Timeout (по PluginAPI_desc.odt) Сведения из исходников / ODT: Ожидает обработку до Timeout мс; -1 означает бесконечное ожидание. Результат берётся из поля Result сообщения. Пример вызова: var\n  LMsg, LRes: Variant;\nbegin\n  LMsg := NewMessage('Camera1.FrameJpg', ''); // подготовить сообщение\n  LRes := SendMsg(LMsg, 1000);                // отправить синхронно, ждать до 1000 мс\n  DebugLog(LRes);                             // результат обработки\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"getvarmodtime","sectionId":"12-globalnye-peremennye","title":"GetVarModTime","text":"GetVarModTime *** GetVarModTime — Время изменения глобальной переменной **** function GetVarModTime(Name: String): TDateTime Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(DateTimeToIso8601(GetVarModTime('Counter'))); // время последнего изменения\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getvarmodcount","sectionId":"12-globalnye-peremennye","title":"GetVarModCount","text":"GetVarModCount *** GetVarModCount — Счётчик изменений глобальной переменной **** function GetVarModCount(Name: String): Integer Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(_ToStr(GetVarModCount('Counter'))); // счётчик изменений\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getvarmodfreq","sectionId":"12-globalnye-peremennye","title":"GetVarModFreq","text":"GetVarModFreq *** GetVarModFreq — Частота изменений глобальной переменной **** function GetVarModFreq(Name: String): Double Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Double (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  DebugLog(_ToStr(GetVarModFreq('Counter'))); // частота изменений\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getvarvalue","sectionId":"12-globalnye-peremennye","title":"GetVarValue","text":"GetVarValue *** GetVarValue — Чтение глобальной переменной **** function GetVarValue(Name: String): Variant Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: var\n  v: Variant;\nbegin\n  v := GetVarValue('Counter'); // прочитать глобальную переменную плагина\n  DebugLog(v);\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"setvarvalue","sectionId":"12-globalnye-peremennye","title":"SetVarValue","text":"SetVarValue *** SetVarValue — Запись глобальной переменной **** procedure SetVarValue(Name: String; Value: Variant) Входные параметры: - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Value: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Работает с глобальной переменной; при отсутствии переменной генерируется исключение. Пример вызова: begin\n  SetVarValue('Counter', 1); // записать значение в глобальную переменную\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"tiblob","sectionId":"13-interfeys-servera","title":"TIBlob","text":"TIBlob Обёртка скрипта над COM-интерфейсом IBlob : контейнер двоичных данных без лишнего копирования между плагинами. У blob есть имя, тип содержимого, размер и сами данные; плагин может реализовать IBlob эффективно под свою модель памяти (по PluginAPI_desc.odt). В скрипте Data отдаётся как Variant-массив байт (копирование в VarArray в fsCoreScript.pas )."},{"id":"tiblob-tojson","sectionId":"13-interfeys-servera","title":"TIBlob.ToJson","text":"TIBlob.ToJson *** TIBlob.ToJson — JSON-представление blob **** function ToJson: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вызывает IBlob.ToJSON (по fsCoreScript.pas / TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"tiblob-name","sectionId":"13-interfeys-servera","title":"TIBlob.Name","text":"TIBlob.Name *** TIBlob.Name — Имя blob **** property Name: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство IBlob.Name — имя двоичного вложения (по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIBlob.Name` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Name));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiblob-contenttype","sectionId":"13-interfeys-servera","title":"TIBlob.ContentType","text":"TIBlob.ContentType *** TIBlob.ContentType — Тип содержимого **** property ContentType: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство IBlob.ContentType — тип содержимого двоичных данных (по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIBlob.ContentType` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ContentType));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiblob-data","sectionId":"13-interfeys-servera","title":"TIBlob.Data","text":"TIBlob.Data *** TIBlob.Data — Данные blob **** property Data: Variant Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: В COM — указатель на данные ( IBlob.Data ). В скриптовой обёртке копируется в Variant-массив байт ( VarArray of varByte ) в TIBlob.GetData ( fsCoreScript.pas ). Пример вызова: begin\n  // Чтение свойства `TIBlob.Data` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Data));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"tiblob-size","sectionId":"13-interfeys-servera","title":"TIBlob.Size","text":"TIBlob.Size *** TIBlob.Size — Размер данных **** property Size: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Число байт в blob ( IBlob.Size , по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIBlob.Size` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Size));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig","sectionId":"13-interfeys-servera","title":"TICoreConfig","text":"TICoreConfig Обёртка над ICoreConfig : пути сервера и плагина, адрес/порт, словарь параметров Values / Defaults , метаданные сообщений и ролей. Чтение Values[Name] — тот же слой конфигурации, что обслуживают устаревшие глобальные GetConfig* . MyPath — каталог текущего плагина; от него резолвятся относительные пути StringFromFile / FileFromString (по TLB / fsCoreScript.pas )."},{"id":"ticoreconfig-binpath","sectionId":"13-interfeys-servera","title":"TICoreConfig.BinPath","text":"TICoreConfig.BinPath *** TICoreConfig.BinPath — Каталог двоичных файлов сервера **** property BinPath: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ICoreConfig.BinPath (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.BinPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.BinPath));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-datapath","sectionId":"13-interfeys-servera","title":"TICoreConfig.DataPath","text":"TICoreConfig.DataPath *** TICoreConfig.DataPath — Каталог данных сервера **** property DataPath: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ICoreConfig.DataPath (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.DataPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.DataPath));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-defaults","sectionId":"13-interfeys-servera","title":"TICoreConfig.Defaults","text":"TICoreConfig.Defaults *** TICoreConfig.Defaults — Значение конфигурации по умолчанию **** property Defaults[Name: String]: String Входные параметры: - Name: String — имя параметра конфигурации (по TLB Defaults[Index] ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Индексное свойство Defaults[Name] — значение по умолчанию для параметра (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.Defaults` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Defaults));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-logpath","sectionId":"13-interfeys-servera","title":"TICoreConfig.LogPath","text":"TICoreConfig.LogPath *** TICoreConfig.LogPath — Каталог журналов **** property LogPath: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ICoreConfig.LogPath (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.LogPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LogPath));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-logverbose","sectionId":"13-interfeys-servera","title":"TICoreConfig.LogVerbose","text":"TICoreConfig.LogVerbose *** TICoreConfig.LogVerbose — Подробный режим логирования **** property LogVerbose: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ICoreConfig.LogVerbose (по TLB); в скриптовой обёртке читается как Boolean ( fsCoreScript.pas ). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.LogVerbose` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LogVerbose));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"ticoreconfig-msgfortype","sectionId":"13-interfeys-servera","title":"TICoreConfig.MsgForType","text":"TICoreConfig.MsgForType *** TICoreConfig.MsgForType — Имя сообщения по типу содержимого **** property MsgForType[Name: String; Value: Variant]: String Входные параметры: - Name: String — тип содержимого / ключ сопоставления (в TLB параметр ContentType ) - Value: Variant — признак команды ( IsCommand в TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Индексное свойство MsgForType[ContentType, IsCommand] (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.MsgForType` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgForType));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-msginfo","sectionId":"13-interfeys-servera","title":"TICoreConfig.MsgInfo","text":"TICoreConfig.MsgInfo *** TICoreConfig.MsgInfo — Описание сообщения **** property MsgInfo[Name: String]: String Входные параметры: - Name: String — имя сообщения (по TLB MsgInfo[MsgName] ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Индексное свойство MsgInfo[MsgName] — информация о сообщении; изменение связано с системным Core.MsgInfoChanged (по TLB / PluginAPI_desc.odt). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.MsgInfo` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgInfo));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-msginfos","sectionId":"13-interfeys-servera","title":"TICoreConfig.MsgInfos","text":"TICoreConfig.MsgInfos *** TICoreConfig.MsgInfos — Сводная информация о сообщениях **** property MsgInfos: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство MsgInfos (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.MsgInfos` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgInfos));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-msgnames","sectionId":"13-interfeys-servera","title":"TICoreConfig.MsgNames","text":"TICoreConfig.MsgNames *** TICoreConfig.MsgNames — Имена сообщений **** property MsgNames: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство MsgNames — перечень имён сообщений (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.MsgNames` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MsgNames));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-mypath","sectionId":"13-interfeys-servera","title":"TICoreConfig.MyPath","text":"TICoreConfig.MyPath *** TICoreConfig.MyPath — Каталог текущего плагина **** property MyPath: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство MyPath ; от него разрешаются относительные пути файловых функций скрипта (по TLB / fsCoreScript.pas ). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.MyPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.MyPath));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"ticoreconfig-pluginoptsdocjson","sectionId":"13-interfeys-servera","title":"TICoreConfig.PluginOptsDocJson","text":"TICoreConfig.PluginOptsDocJson *** TICoreConfig.PluginOptsDocJson — JSON-описание опций плагина **** property PluginOptsDocJson[Name: String]: String Входные параметры: - Name: String — имя плагина (по TLB PluginOptsDocJson[PluginName] ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Индексное свойство PluginOptsDocJson[PluginName] (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.PluginOptsDocJson` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.PluginOptsDocJson));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-pluginpath","sectionId":"13-interfeys-servera","title":"TICoreConfig.PluginPath","text":"TICoreConfig.PluginPath *** TICoreConfig.PluginPath — Каталог плагинов **** property PluginPath: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ICoreConfig.PluginPath (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.PluginPath` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.PluginPath));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-roles","sectionId":"13-interfeys-servera","title":"TICoreConfig.Roles","text":"TICoreConfig.Roles *** TICoreConfig.Roles — Роли сервера **** property Roles: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство Roles (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.Roles` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Roles));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-serveraddr","sectionId":"13-interfeys-servera","title":"TICoreConfig.ServerAddr","text":"TICoreConfig.ServerAddr *** TICoreConfig.ServerAddr — Адрес сервера **** property ServerAddr: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ServerAddr (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.ServerAddr` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ServerAddr));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-serverport","sectionId":"13-interfeys-servera","title":"TICoreConfig.ServerPort","text":"TICoreConfig.ServerPort *** TICoreConfig.ServerPort — Порт сервера **** property ServerPort: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ServerPort (по TLB); в COM — Integer. Пример вызова: begin\n  // Чтение свойства `TICoreConfig.ServerPort` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ServerPort));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-url","sectionId":"13-interfeys-servera","title":"TICoreConfig.URL","text":"TICoreConfig.URL *** TICoreConfig.URL — Базовый URL сервера **** property URL: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство URL (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.URL` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.URL));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoreconfig-values","sectionId":"13-interfeys-servera","title":"TICoreConfig.Values","text":"TICoreConfig.Values *** TICoreConfig.Values — Значение параметра конфигурации **** property Values[Name: String]: String Входные параметры: - Name: String — имя параметра конфигурации (по TLB Values[Index] ) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Индексное свойство Values[Name] — чтение/запись параметра конфигурации. Глобальные GetConfig* читают тот же слой через CoreConfig.Values (по TLB / fsCoreScript.pas / functions.txt ). Пример вызова: begin\n  // Чтение свойства `TICoreConfig.Values` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Values));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"ticorelocale","sectionId":"13-interfeys-servera","title":"TICoreLocale","text":"TICoreLocale Обёртка над ICoreLocale : форматирование валюты, даты, веса, форм множественного числа и перевод строк с учётом текущего языка сервера (сигнатуры — TLB / RTTI)."},{"id":"ticorelocale-formatcurrency","sectionId":"13-interfeys-servera","title":"TICoreLocale.FormatCurrency","text":"TICoreLocale.FormatCurrency *** TICoreLocale.FormatCurrency — Форматирование денежной суммы **** function FormatCurrency(Value: Double; Currency: String; Spellout, Cents: Boolean): String Входные параметры: - Value: Double — сумма (по TLB) - Currency: String — код/обозначение валюты (по TLB) - Spellout: Boolean — прописью (по имени параметра TLB) - Cents: Boolean — включать копейки/центы (по имени параметра TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Метод ICoreLocale.FormatCurrency (по TLB / RTTI). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.FormatCurrency(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorelocale-formatdate","sectionId":"13-interfeys-servera","title":"TICoreLocale.FormatDate","text":"TICoreLocale.FormatDate *** TICoreLocale.FormatDate — Форматирование даты **** function FormatDate(Value: TDateTime; WithTime: Boolean): String Входные параметры: - Value: TDateTime — дата/время (по TLB) - WithTime: Boolean — включать время в строку (по TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Метод ICoreLocale.FormatDate ; параметр WithTime включает время (по TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.FormatDate(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorelocale-formatplural","sectionId":"13-interfeys-servera","title":"TICoreLocale.FormatPlural","text":"TICoreLocale.FormatPlural *** TICoreLocale.FormatPlural — Форма множественного числа **** function FormatPlural(Value: Integer; Plurals: String): String Входные параметры: - Value: Integer — число для выбора формы множественного числа (по TLB) - Plurals: String — набор форм множественного числа (по TLB; формат строки в текстовых материалах не раскрыт) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Метод ICoreLocale.FormatPlural (по TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.FormatPlural(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorelocale-formatweight","sectionId":"13-interfeys-servera","title":"TICoreLocale.FormatWeight","text":"TICoreLocale.FormatWeight *** TICoreLocale.FormatWeight — Форматирование массы **** function FormatWeight(Value: Double; Spellout: Boolean): String Входные параметры: - Value: Double — значение массы (по TLB) - Spellout: Boolean — прописью (по TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Метод ICoreLocale.FormatWeight (по TLB / fsCoreScript.pas ). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.FormatWeight(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"ticorelocale-translate","sectionId":"13-interfeys-servera","title":"TICoreLocale.Translate","text":"TICoreLocale.Translate *** TICoreLocale.Translate — Перевод строки **** function Translate(Value: String): String Входные параметры: - Value: String — строка для перевода (по TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Метод ICoreLocale.Translate (по TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.Translate(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorelocale-language","sectionId":"13-interfeys-servera","title":"TICoreLocale.Language","text":"TICoreLocale.Language *** TICoreLocale.Language — Язык локали **** property Language: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство Language (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreLocale.Language` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Language));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorelocale-languagecode","sectionId":"13-interfeys-servera","title":"TICoreLocale.LanguageCode","text":"TICoreLocale.LanguageCode *** TICoreLocale.LanguageCode — Код языка **** property LanguageCode: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство LanguageCode (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreLocale.LanguageCode` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LanguageCode));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorelocale-languageid","sectionId":"13-interfeys-servera","title":"TICoreLocale.LanguageID","text":"TICoreLocale.LanguageID *** TICoreLocale.LanguageID — Идентификатор языка **** property LanguageID: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство LanguageID (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreLocale.LanguageID` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.LanguageID));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoremessages","sectionId":"13-interfeys-servera","title":"TICoreMessages","text":"TICoreMessages Обёртка над ICoreMessages — шина сообщений ядра: создание IMessage , асинхронный PostMsg , синхронный SendMsg , проверка подписчиков HasHandlerFor . Имя сообщения — ИмяПлагина.ИмяСообщения . Обработчики работают в потоках TMessageHandler ; после обработки нельзя удерживать ссылку на IMessage (ссылку на IBlob удерживать можно) — по PluginAPI_desc.odt."},{"id":"ticoremessages-hashandlerfor","sectionId":"13-interfeys-servera","title":"TICoreMessages.HasHandlerFor","text":"TICoreMessages.HasHandlerFor *** TICoreMessages.HasHandlerFor — Проверка подписчиков **** function HasHandlerFor(MsgName: String): Boolean Входные параметры: - MsgName: String — имя сообщения (по PluginAPI_desc.odt) Возвращает: True , если в системе есть подписчики на указанное имя сообщения (по PluginAPI_desc.odt) Сведения из исходников / ODT: Возвращает, есть ли в системе обработчики для имени сообщения (по PluginAPI_desc.odt). Пример вызова: begin\n  if CoreMessages.HasHandlerFor('Camera1.FrameJpg') then\n    DebugLog('есть подписчики');\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"ticoremessages-newmessage","sectionId":"13-interfeys-servera","title":"TICoreMessages.NewMessage","text":"TICoreMessages.NewMessage *** TICoreMessages.NewMessage — Создание сообщения **** function NewMessage(MsgName: String; Value: Variant): Variant Входные параметры: - MsgName: String — имя вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка Value (по PluginAPI_desc.odt) Возвращает: созданный объект сообщения (по PluginAPI_desc.odt / TLB) Сведения из исходников / ODT: Создаёт IMessage с именем вида ИмяПлагина.ИмяСообщения и значением Value (по PluginAPI_desc.odt). Пример вызова: var\n  LMsg: Variant;\nbegin\n  LMsg := CoreMessages.NewMessage('Camera1.FrameJpg', ''); // создать сообщение\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"ticoremessages-newmessageex","sectionId":"13-interfeys-servera","title":"TICoreMessages.NewMessageEx","text":"TICoreMessages.NewMessageEx *** TICoreMessages.NewMessageEx — Создание расширенного сообщения **** function NewMessageEx(MsgName: String; Value: Variant; Blob, CurrentUser: Variant; Timeout: Integer): Variant Входные параметры: - MsgName: String — имя вида ИмяПлагина.ИмяСообщения (по PluginAPI_desc.odt) - Value: Variant — полезная нагрузка Value (по PluginAPI_desc.odt) - Blob: Variant — двоичные данные IBlob (по PluginAPI_desc.odt) - CurrentUser: Variant — пользователь IUser (по TLB) - Timeout: Integer — таймаут сообщения (по TLB) Возвращает: созданный объект сообщения с Blob/User/Timeout (по PluginAPI_desc.odt / TLB) Сведения из исходников / ODT: Создаёт сообщение с Value , Blob , пользователем и таймаутом (по PluginAPI_desc.odt / TLB). Пример вызова: var\n  LMsg: Variant;\nbegin\n  // Blob/User/Timeout — по сигнатуре RTTI; точная семантика полей требует дополнения тимлида\n  LMsg := NewMessageEx('Camera1.FrameJpg', '', Null, Null, 0);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticoremessages-newmessagefromjson","sectionId":"13-interfeys-servera","title":"TICoreMessages.NewMessageFromJson","text":"TICoreMessages.NewMessageFromJson *** TICoreMessages.NewMessageFromJson — Создание сообщения из JSON **** function NewMessageFromJson(Json, NewMsgName: String; Remote: Boolean): Variant Входные параметры: - Json: String — JSON-текст сообщения (по TLB) - NewMsgName: String — имя сообщения (по TLB) - Remote: Boolean — признак Remote (по TLB) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Метод NewMessageFromJSON COM-интерфейса (по TLB / fsCoreScript.pas ). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.NewMessageFromJson(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"ticoremessages-postmsg","sectionId":"13-interfeys-servera","title":"TICoreMessages.PostMsg","text":"TICoreMessages.PostMsg *** TICoreMessages.PostMsg — Асинхронная отправка **** function PostMsg(Msg: Variant): Boolean Входные параметры: - Msg: Variant — объект сообщения ( IMessage / TIMessage ) для постановки в очередь (по PluginAPI_desc.odt) Возвращает: True , если сообщение поставлено в очередь хотя бы одному обработчику; False , если обработчиков нет (по PluginAPI_desc.odt) Сведения из исходников / ODT: Ставит сообщение в очередь каждого обработчика; True при успешной постановке, False если обработчиков нет (по PluginAPI_desc.odt). Пример вызова: var\n  LMsg: Variant;\n  Ok: Boolean;\nbegin\n  LMsg := CoreMessages.NewMessage('Camera1.FrameJpg', '');\n  Ok := CoreMessages.PostMsg(LMsg); // True, если есть обработчики\n  DebugLog(_ToStr(Ok));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"ticoremessages-sendmsg","sectionId":"13-interfeys-servera","title":"TICoreMessages.SendMsg","text":"TICoreMessages.SendMsg *** TICoreMessages.SendMsg — Синхронная отправка **** function SendMsg(Msg: Variant; Timeout: Integer): Variant Входные параметры: - Msg: Variant — объект сообщения для синхронной отправки (по PluginAPI_desc.odt) - Timeout: Integer — таймаут ожидания в мс; -1 — бесконечное ожидание (по PluginAPI_desc.odt) Возвращает: значение поля Result сообщения после ожидания обработки (по PluginAPI_desc.odt) Сведения из исходников / ODT: Ждёт завершения обработки всеми подписчиками до Timeout мс ( -1 — без лимита). Окончание определяется по счётчику ссылок сообщения; результат — поле Result (по PluginAPI_desc.odt). Пример вызова: var\n  LMsg, LRes: Variant;\nbegin\n  LMsg := CoreMessages.NewMessage('Camera1.FrameJpg', '');\n  LRes := CoreMessages.SendMsg(LMsg, 1000); // ждать до 1000 мс; -1 — бесконечно\n  DebugLog(LRes);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"ticoremessages-registeredmessages","sectionId":"13-interfeys-servera","title":"TICoreMessages.RegisteredMessages","text":"TICoreMessages.RegisteredMessages *** TICoreMessages.RegisteredMessages — Зарегистрированные сообщения **** property RegisteredMessages: Variant Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство RegisteredMessages интерфейса ICoreMessages (по TLB). Пример вызова: begin\n  // Чтение свойства `TICoreMessages.RegisteredMessages` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.RegisteredMessages));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorescheduler","sectionId":"13-interfeys-servera","title":"TICoreScheduler","text":"TICoreScheduler Обёртка над ICoreScheduler : отложенная или периодическая отправка сообщений. Расписание AddTask — строка из 6 полей (секунда…день недели), локальное время компьютера сервера, синтаксис как crontab; день недели и день месяца вместе обрабатываются по «ИЛИ». AddTimer шлёт сообщение каждые N секунд. В скриптовой RTTI флаг RunOnce отображается на опцию SchedulerRunOnce ядра (PluginAPI_desc.odt / fsCoreScript.pas ). Примеры в старом ODT с укороченной сигнатурой AddTask считаются устаревшими относительно текущего RTTI."},{"id":"ticorescheduler-addtask","sectionId":"13-interfeys-servera","title":"TICoreScheduler.AddTask","text":"TICoreScheduler.AddTask *** TICoreScheduler.AddTask — Задача по расписанию **** function AddTask(Tab, UpdateTaskID: String; Msg: Variant; RunOnce: Boolean): String Входные параметры: - Tab: String — строка расписания из 6 частей (Секунда Минута Час День Месяц ДеньНедели), локальное время сервера; синтаксис как crontab (по PluginAPI_desc.odt) - UpdateTaskID: String — идентификатор задачи для обновления существующей; пустая строка — создать новую (по сигнатуре TLB UpdateTaskID ) - Msg: Variant — сообщение, которое планировщик отправит при срабатывании (по PluginAPI_desc.odt) - RunOnce: Boolean — если True, в опции ядра добавляется SchedulerRunOnce (по fsCoreScript.pas ) Возвращает: идентификатор задачи планировщика (по TLB / fsCoreScript.pas ) Сведения из исходников / ODT: Планирует отправку сообщения по crontab-подобной строке Tab (6 полей, локальное время сервера). RunOnce=True добавляет флаг SchedulerRunOnce в опции ядра (по PluginAPI_desc.odt / fsCoreScript.pas ). Пример вызова: var\n  LMsg: Variant;\n  TaskID: String;\nbegin\n  LMsg := CoreMessages.NewMessage('MyPlugin.Tick', '');\n  // каждый день в 00:05:00, локальное время сервера\n  TaskID := CoreScheduler.AddTask('0 5 0 * * *', '', LMsg, False);\n  DebugLog(TaskID);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"ticorescheduler-addtimer","sectionId":"13-interfeys-servera","title":"TICoreScheduler.AddTimer","text":"TICoreScheduler.AddTimer *** TICoreScheduler.AddTimer — Периодический таймер **** function AddTimer(Seconds: Integer; Msg: Variant): String Входные параметры: - Seconds: Integer — период отправки сообщения в секундах (по PluginAPI_desc.odt) - Msg: Variant — сообщение для периодической отправки (по PluginAPI_desc.odt) Возвращает: идентификатор таймера (по TLB / fsCoreScript.pas ) Сведения из исходников / ODT: Отправляет сообщение каждые Seconds секунд (по PluginAPI_desc.odt). Пример вызова: var\n  LMsg: Variant;\n  TimerID: String;\nbegin\n  LMsg := CoreMessages.NewMessage('MyPlugin.Tick', '');\n  TimerID := CoreScheduler.AddTimer(30, LMsg); // каждые 30 секунд\n  DebugLog(TimerID);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"ticorescheduler-removetask","sectionId":"13-interfeys-servera","title":"TICoreScheduler.RemoveTask","text":"TICoreScheduler.RemoveTask *** TICoreScheduler.RemoveTask — Удаление задачи **** procedure RemoveTask(TaskID: String) Входные параметры: - TaskID: String — идентификатор задачи, возвращённый AddTask (по TLB) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Снимает задачу по идентификатору (по TLB). Пример вызова: begin\n  // Obj.RemoveTask(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"ticorescheduler-removetimer","sectionId":"13-interfeys-servera","title":"TICoreScheduler.RemoveTimer","text":"TICoreScheduler.RemoveTimer *** TICoreScheduler.RemoveTimer — Удаление таймера **** procedure RemoveTimer(TimerID: String) Входные параметры: - TimerID: String — идентификатор таймера, возвращённый AddTimer (по TLB) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Снимает таймер по идентификатору (по TLB). Пример вызова: begin\n  // Obj.RemoveTimer(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiuser","sectionId":"13-interfeys-servera","title":"TIUser","text":"TIUser Обёртка над IUser : сведения о пользователе HTTP-сессии или сообщения — идентификатор, имя, отображаемое имя, группа, роли, IP. Используется в AccessCheck страниц и в поле User сообщения (PluginAPI_desc.odt / TLB)."},{"id":"tiuser-hasrole","sectionId":"13-interfeys-servera","title":"TIUser.HasRole","text":"TIUser.HasRole *** TIUser.HasRole — Проверка роли **** function HasRole(RoleName: String): Boolean Входные параметры: - RoleName: String — имя роли для проверки (по TLB IUser.HasRole ) Возвращает: True , если у пользователя есть указанная роль (по TLB) Сведения из исходников / ODT: Возвращает, входит ли роль в набор ролей пользователя (по TLB). Используется при проверке доступа к страницам ( AccessCheck , по PluginAPI_desc.odt). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.HasRole(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiuser-tojson","sectionId":"13-interfeys-servera","title":"TIUser.ToJson","text":"TIUser.ToJson *** TIUser.ToJson — JSON пользователя **** function ToJson: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вызывает IUser.ToJSON (по TLB / fsCoreScript.pas ). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"tiuser-displayname","sectionId":"13-interfeys-servera","title":"TIUser.DisplayName","text":"TIUser.DisplayName *** TIUser.DisplayName — Отображаемое имя **** property DisplayName: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство DisplayName (по TLB). Пример вызова: begin\n  // Чтение свойства `TIUser.DisplayName` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.DisplayName));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiuser-groupid","sectionId":"13-interfeys-servera","title":"TIUser.GroupID","text":"TIUser.GroupID *** TIUser.GroupID — Идентификатор группы **** property GroupID: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство GroupID (тип UserGroupType в COM, Integer в скрипте) (по TLB / fsCoreScript.pas ). Пример вызова: begin\n  // Чтение свойства `TIUser.GroupID` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.GroupID));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"tiuser-groupname","sectionId":"13-interfeys-servera","title":"TIUser.GroupName","text":"TIUser.GroupName *** TIUser.GroupName — Имя группы **** property GroupName: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство GroupName (по TLB). Пример вызова: begin\n  // Чтение свойства `TIUser.GroupName` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.GroupName));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiuser-id","sectionId":"13-interfeys-servera","title":"TIUser.ID","text":"TIUser.ID *** TIUser.ID — Идентификатор пользователя **** property ID: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство ID (по TLB). Пример вызова: begin\n  // Чтение свойства `TIUser.ID` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.ID));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiuser-ip","sectionId":"13-interfeys-servera","title":"TIUser.IP","text":"TIUser.IP *** TIUser.IP — IP-адрес **** property IP: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство IP (по TLB). Пример вызова: begin\n  // Чтение свойства `TIUser.IP` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.IP));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiuser-name","sectionId":"13-interfeys-servera","title":"TIUser.Name","text":"TIUser.Name *** TIUser.Name — Имя пользователя **** property Name: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство Name (по TLB). Пример вызова: begin\n  // Чтение свойства `TIUser.Name` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Name));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiuser-roles","sectionId":"13-interfeys-servera","title":"TIUser.Roles","text":"TIUser.Roles *** TIUser.Roles — Роли пользователя **** property Roles: Variant Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство Roles ( OleVariant / Variant) (по TLB). Пример вызова: begin\n  // Чтение свойства `TIUser.Roles` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Roles));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"timessage","sectionId":"13-interfeys-servera","title":"TIMessage","text":"TIMessage Обёртка над IMessage : сообщение шины. Поля в основном только для чтения; исключение — Result , которое заполняют обработчики при SendMsg . Состав: полезная нагрузка Value ( Variant ), опциональный Blob , метка времени, признак устаревания Expired (устаревшие можно не обрабатывать), пользователь, таймаут, признак Remote (PluginAPI_desc.odt / TLB)."},{"id":"timessage-tojson","sectionId":"13-interfeys-servera","title":"TIMessage.ToJson","text":"TIMessage.ToJson *** TIMessage.ToJson — JSON сообщения **** function ToJson: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вызывает IMessage.ToJSON (по TLB / fsCoreScript.pas ). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"timessage-blob","sectionId":"13-interfeys-servera","title":"TIMessage.Blob","text":"TIMessage.Blob *** TIMessage.Blob — Двоичные данные сообщения **** property Blob: Variant Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Объект IBlob с двоичной полезной нагрузкой; может передаваться между плагинами без копирования данных (по PluginAPI_desc.odt). Пример вызова: begin\n  // Чтение свойства `TIMessage.Blob` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Blob));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"timessage-expired","sectionId":"13-interfeys-servera","title":"TIMessage.Expired","text":"TIMessage.Expired *** TIMessage.Expired — Признак устаревания **** property Expired: Boolean Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Если сообщение устарело, обработчик может его не обрабатывать (по PluginAPI_desc.odt). Пример вызова: begin\n  // Чтение свойства `TIMessage.Expired` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Expired));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"timessage-remote","sectionId":"13-interfeys-servera","title":"TIMessage.Remote","text":"TIMessage.Remote *** TIMessage.Remote — Признак удалённого сообщения **** property Remote: Boolean Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Boolean (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство Remote интерфейса IMessage (по TLB). Развёрнутая семантика в текстовом ODT не описана. Пример вызова: begin\n  // Чтение свойства `TIMessage.Remote` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Remote));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"timessage-result","sectionId":"13-interfeys-servera","title":"TIMessage.Result","text":"TIMessage.Result *** TIMessage.Result — Результат обработки **** property Result: Variant Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Единственное поле сообщения, доступное для записи; используется при синхронном SendMsg (по PluginAPI_desc.odt). Пример вызова: begin\n  // Чтение свойства `TIMessage.Result` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Result));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"timessage-timeout","sectionId":"13-interfeys-servera","title":"TIMessage.Timeout","text":"TIMessage.Timeout *** TIMessage.Timeout — Таймаут сообщения **** property Timeout: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Свойство Timeout (по TLB / RTTI). Пример вызова: begin\n  // Чтение свойства `TIMessage.Timeout` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Timeout));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"timessage-timestamp","sectionId":"13-interfeys-servera","title":"TIMessage.Timestamp","text":"TIMessage.Timestamp *** TIMessage.Timestamp — Время отправки **** property Timestamp: TDateTime Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TDateTime (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Дата/время отправки сообщения (по PluginAPI_desc.odt). Пример вызова: begin\n  // Чтение свойства `TIMessage.Timestamp` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Timestamp));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"timessage-user","sectionId":"13-interfeys-servera","title":"TIMessage.User","text":"TIMessage.User *** TIMessage.User — Пользователь сообщения **** property User: TIUser Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TIUser (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Объект IUser / TIUser , связанный с сообщением (по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIMessage.User` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.User));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"timessage-value","sectionId":"13-interfeys-servera","title":"TIMessage.Value","text":"TIMessage.Value *** TIMessage.Value — Полезная нагрузка **** property Value: Variant Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Простое значение типа Variant (часто JSON-структура) (по PluginAPI_desc.odt). Пример вызова: begin\n  // Чтение свойства `TIMessage.Value` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Value));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"tiwebserverrequest","sectionId":"13-interfeys-servera","title":"TIWebServerRequest","text":"TIWebServerRequest Обёртка над IWebServerRequest : входящий HTTP-запрос страницы плагина. Доступны параметры ( Input / InputStr / InputArr ), заголовки, cookie, метод, URI, имя страницы и текущий пользователь. Вызывается из пула HTTP-потоков в HandleRequest (PluginAPI_desc.odt / TLB)."},{"id":"tiwebserverrequest-cookie","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.Cookie","text":"TIWebServerRequest.Cookie *** TIWebServerRequest.Cookie — Cookie запроса **** function Cookie(Name: String): String Входные параметры: - Name: String — имя cookie запроса (по PluginAPI_desc.odt / TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Чтение cookie по имени (по PluginAPI_desc.odt / TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.Cookie(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-header","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.Header","text":"TIWebServerRequest.Header *** TIWebServerRequest.Header — Заголовок запроса **** function Header(Name: String): String Входные параметры: - Name: String — имя HTTP-заголовка запроса (по PluginAPI_desc.odt / TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Чтение HTTP-заголовка по имени (по PluginAPI_desc.odt / TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.Header(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-input","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.Input","text":"TIWebServerRequest.Input *** TIWebServerRequest.Input — Параметр запроса **** function Input(Name: String): Variant Входные параметры: - Name: String — имя параметра запроса (по PluginAPI_desc.odt / TLB) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Параметр HTTP-запроса по имени ( OleVariant ) (по PluginAPI_desc.odt / TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.Input(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-inputarr","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.InputArr","text":"TIWebServerRequest.InputArr *** TIWebServerRequest.InputArr — Параметр-массив **** function InputArr(Name: String): Variant Входные параметры: - Name: String — имя параметра-массива запроса (по TLB) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Параметр запроса как массив значений (по TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.InputArr(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-inputstr","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.InputStr","text":"TIWebServerRequest.InputStr *** TIWebServerRequest.InputStr — Строковый параметр **** function InputStr(Name: String): String Входные параметры: - Name: String — имя строкового параметра запроса (по TLB) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Параметр запроса как строка (по TLB). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.InputStr(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-tojson","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.ToJson","text":"TIWebServerRequest.ToJson *** TIWebServerRequest.ToJson — JSON запроса **** function ToJson: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Вызывает IWebServerRequest.ToJSON (по TLB / fsCoreScript.pas ). Пример вызова: var\n  r: Variant;\nbegin\n  // r := Obj.ToJson(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas ; Материалы для документации/source/fsCoreScript.pas"},{"id":"tiwebserverrequest-currentuser","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.CurrentUser","text":"TIWebServerRequest.CurrentUser *** TIWebServerRequest.CurrentUser — Текущий пользователь **** property CurrentUser: TIUser Входные параметры: Параметры отсутствуют. Возвращает: Значение типа TIUser (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Пользователь сессии / авторизации запроса; используется в проверках доступа (по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIWebServerRequest.CurrentUser` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.CurrentUser));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-method","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.Method","text":"TIWebServerRequest.Method *** TIWebServerRequest.Method — HTTP-метод **** property Method: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Метод запроса (GET, POST, …) (по TLB). Пример вызова: begin\n  // Чтение свойства `TIWebServerRequest.Method` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Method));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-page","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.Page","text":"TIWebServerRequest.Page *** TIWebServerRequest.Page — Имя страницы **** property Page: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Имя зарегистрированной страницы плагина (по TLB). Пример вызова: begin\n  // Чтение свойства `TIWebServerRequest.Page` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.Page));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverrequest-uri","sectionId":"13-interfeys-servera","title":"TIWebServerRequest.URI","text":"TIWebServerRequest.URI *** TIWebServerRequest.URI — URI запроса **** property URI: String Входные параметры: Параметры отсутствуют. Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: URI входящего запроса (по TLB). Пример вызова: begin\n  // Чтение свойства `TIWebServerRequest.URI` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.URI));\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverresponse","sectionId":"13-interfeys-servera","title":"TIWebServerResponse","text":"TIWebServerResponse Обёртка над IWebServerResponse : формирование ответа. ReturnPage — HTML по шаблону из Views внутри общего каркаса сайта (partials вроде {{>header}} ); ReturnError — страница по Error.html ; ReturnText / ReturnBlob — произвольный ответ (в COM для двоичных данных — ReturnData ). Тексты и шаблоны — UTF-8. Дополнительно: cookie, заголовки, redirect, CacheControl (флаги кэша страницы из PluginAPI_desc.odt)."},{"id":"tiwebserverresponse-redirect","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.Redirect","text":"TIWebServerResponse.Redirect *** TIWebServerResponse.Redirect — Перенаправление **** procedure Redirect(PageName: String) Входные параметры: - PageName: String — имя страницы для перенаправления (по TLB / RTTI) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Перенаправление на указанную страницу (по TLB / RTTI). Пример вызова: begin\n  // Obj.Redirect(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverresponse-returnblob","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.ReturnBlob","text":"TIWebServerResponse.ReturnBlob *** TIWebServerResponse.ReturnBlob — Двоичный ответ **** procedure ReturnBlob(Blob: Variant; Status: Integer) Входные параметры: - Blob: Variant — двоичное тело ответа (скрипковый аналог ReturnData, по PluginAPI_desc.odt / RTTI) - Status: Integer — HTTP-статус (по RTTI) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Отдаёт двоичные данные с HTTP-статусом; скриптовый аналог COM-метода ReturnData (по PluginAPI_desc.odt / RTTI). Пример вызова: begin\n  // Obj.ReturnBlob(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/functions.txt"},{"id":"tiwebserverresponse-returnerror","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.ReturnError","text":"TIWebServerResponse.ReturnError *** TIWebServerResponse.ReturnError — Страница ошибки **** procedure ReturnError(Text: String; Status: Integer) Входные параметры: - Text: String — текст ошибки; страница строится по шаблону Error.html (по PluginAPI_desc.odt) - Status: Integer — HTTP-статус (по RTTI) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Формирует страницу ошибки по шаблону Error.html внутри общего каркаса сайта (по PluginAPI_desc.odt). Пример вызова: begin\n  Response.ReturnError('Ошибка обработки запроса', 500);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"tiwebserverresponse-returnpage","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.ReturnPage","text":"TIWebServerResponse.ReturnPage *** TIWebServerResponse.ReturnPage — Ответ HTML-страницей **** procedure ReturnPage(Name: String; InputJSON: Variant) Входные параметры: - Name: String — имя HTML-шаблона в папке Views плагина (по PluginAPI_desc.odt) - InputJSON: Variant — данные для шаблона основной части страницы (по PluginAPI_desc.odt) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Строит страницу по шаблону из папки Views плагина и встраивает в основной шаблон сайта; параметры передаются в шаблон (по PluginAPI_desc.odt). В шаблоне можно подключать partials, например {{>header}} . Пример вызова: begin\n  // шаблон из Views плагина; InputJSON — данные для шаблона\n  Response.ReturnPage('Kiosk', _ObjEx(['Title', 'Готовность']));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"tiwebserverresponse-returntext","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.ReturnText","text":"TIWebServerResponse.ReturnText *** TIWebServerResponse.ReturnText — Текстовый HTTP-ответ **** procedure ReturnText(Text, ContentType: String; Status: Integer) Входные параметры: - Text: String — полный текст ответа (по PluginAPI_desc.odt) - ContentType: String — тип содержимого (по PluginAPI_desc.odt) - Status: Integer — HTTP-статус (по PluginAPI_desc.odt) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Задаёт полный текст ответа, Content-Type и HTTP-статус (по PluginAPI_desc.odt). Текстовые ответы и шаблоны ожидаются в UTF-8. Пример вызова: begin\n  Response.ReturnText('{\"ok\":true}', 'application/json', 200);\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt"},{"id":"tiwebserverresponse-setcookie","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.SetCookie","text":"TIWebServerResponse.SetCookie *** TIWebServerResponse.SetCookie — Установка cookie **** procedure SetCookie(Name, Value: String; Expires: TDateTime) Входные параметры: - Name: String — имя cookie (по TLB) - Value: String — значение cookie (по TLB) - Expires: TDateTime — срок действия cookie (по TLB) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Задаёт cookie ответа с сроком действия (по TLB). Пример вызова: begin\n  // Obj.SetCookie(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverresponse-setheader","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.SetHeader","text":"TIWebServerResponse.SetHeader *** TIWebServerResponse.SetHeader — Установка заголовка **** procedure SetHeader(Value: String) Входные параметры: - Value: String — строка HTTP-заголовка ответа (по TLB) Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Добавляет/задаёт заголовок HTTP-ответа (по TLB). Пример вызова: begin\n  // Obj.SetHeader(...); // см. параметры и сведения выше\nend Источник сведений: Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"tiwebserverresponse-cachecontrol","sectionId":"13-interfeys-servera","title":"TIWebServerResponse.CacheControl","text":"TIWebServerResponse.CacheControl *** TIWebServerResponse.CacheControl — Флаги кэширования ответа **** property CacheControl: Integer Входные параметры: Параметры отсутствуют. Возвращает: Значение типа Integer (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Соответствует WebPageOptionsType / флагам вроде WebPageCacheControlNoCache , WebPageCacheControlAgeDay и др. (по PluginAPI_desc.odt / TLB). Пример вызова: begin\n  // Чтение свойства `TIWebServerResponse.CacheControl` у объекта интерфейса (см. сведения выше).\n  // DebugLog(_ToStr(Obj.CacheControl));\nend Источник сведений: Материалы для документации/source/_odt_extract/PluginAPI_desc.txt ; Материалы для документации/source/PluginAPI_TLB.pas"},{"id":"getquery","sectionId":"14-universalnye-zhurnaly","title":"GetQuery","text":"GetQuery *** GetQuery — SQL-запрос к журналу **** function GetQuery(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос; синтаксис SQLite; имя таблицы всегда Journal (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Использует SQLite; имя таблицы журнала всегда Journal . Пример вызова: begin\n  DebugLog(_ToStr(GetQuery('WeighingJournal', 'SELECT MAX(RowID) FROM Journal'))); // SQL к журналу\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getqueryex","sectionId":"14-universalnye-zhurnaly","title":"GetQueryEx","text":"GetQueryEx *** GetQueryEx — SQL-запрос к внешней БД **** function GetQueryEx(Journal, SQL: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - SQL: String — SQL-запрос к внешней БД; синтаксис внешней БД (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Выполняет запрос к внешней базе данных. Пример вызова: begin\n  DebugLog(_ToStr(GetQueryEx('WeighingJournal', 'SELECT MAX(ID) FROM Weighing'))); // SQL к внешней БД\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getrecord","sectionId":"14-universalnye-zhurnaly","title":"GetRecord","text":"GetRecord *** GetRecord — Чтение записи журнала **** function GetRecord(Journal, Code: String): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - Code: String — идентификатор записи (по EventScript_desc.odt) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Получает запись по идентификатору Code . Пример вызова: var\n  Doc: Variant;\nbegin\n  Doc := GetRecord('WeighingJournal', 'D307EB...205027'); // запись по CODE\n  DebugLog(Doc.NUMB_TS);                                  // поле записи\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getrecords","sectionId":"14-universalnye-zhurnaly","title":"GetRecords","text":"GetRecords *** GetRecords — Поиск записей журнала **** function GetRecords(Journal: String; Args: Variant): Variant Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - Args: Variant — параметры поиска (в примерах ODT: Filter, SortField, SortDesc, MaxRows) Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: В Args используются поля Filter , SortField , SortDesc , MaxRows . Пример вызова: var\n  Args, Docs, Doc: Variant;\n  i: Integer;\nbegin\n  Args := _Obj();                        // аргументы выборки\n  Args.Filter := _Obj();                 // фильтр\n  Args.Filter.NUMB_TS := 'A999AB45';     // условие по полю\n  Args.SortField := 'DATETIME_CREATE';   // сортировка\n  Args.SortDesc := True;                 // по убыванию\n  Args.MaxRows := 10;                    // лимит строк\n  Docs := GetRecords('WeighingJournal', Args); // выполнить поиск\n  for i := 0 to Docs._Count - 1 do begin\n    Doc := Docs.Value(i);                // очередная запись\n    DebugLog(Doc.NUMB_TS);               // вывести поле\n  end;\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"setrecord","sectionId":"14-universalnye-zhurnaly","title":"SetRecord","text":"SetRecord *** SetRecord — Сохранение записи журнала **** function SetRecord(Journal: String; Doc: Variant): String Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - Doc: Variant — данные записи (по EventScript_desc.odt) Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Перед отправкой удаляет поле DBID из документа. Операция передаётся сообщением Journal.Operation через SendMsg . Пример вызова: var\n  Doc: Variant;\n  Code: String;\nbegin\n  Doc := _Obj();                         // документ записи\n  Doc.CODE := 'D307EB...205027';         // код записи\n  Doc.NUMB_TS := 'A999AB45';              // поле данных\n  Code := SetRecord('WeighingJournal', Doc); // сохранить запись\n  DebugLog(Code);                        // результат операции\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"setrecordnt","sectionId":"14-universalnye-zhurnaly","title":"SetRecordNT","text":"SetRecordNT *** SetRecordNT — Сохранение без триггеров **** function SetRecordNT(Journal: String; Doc: Variant): String Входные параметры: - Journal: String — имя журнала (по EventScript_desc.odt) - Doc: Variant — данные записи (по EventScript_desc.odt) Возвращает: результат операции записи; изменение выполняется без запуска триггеров (по EventScript_desc.odt) Сведения из исходников / ODT: Выполняет изменение записи без запуска триггеров. Операция передаётся сообщением Journal.Operation через SendMsg . Пример вызова: var\n  Doc: Variant;\nbegin\n  Doc := _Obj();\n  Doc.CODE := 'D307EB...205027';\n  Doc.NUMB_TS := 'A999AB45';\n  DebugLog(SetRecordNT('WeighingJournal', Doc)); // сохранить без запуска триггеров\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getview","sectionId":"14-universalnye-zhurnaly","title":"GetView","text":"GetView *** GetView — Данные представления **** function GetView(Journal, Name: String; Params, Filter: Variant; SortField: String; SortDesc: Boolean; FirstRow, MaxRows: Integer): Variant Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Params: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Filter: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - SortField: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - SortDesc: Boolean — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - FirstRow: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - MaxRows: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Получает данные именованного представления журнала. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetView(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"execproc","sectionId":"14-universalnye-zhurnaly","title":"ExecProc","text":"ExecProc *** ExecProc — Хранимая процедура журнала **** function ExecProc(Journal, Name: String; Args: Variant): Variant Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Выполняет хранимую процедуру журнала. Пример вызова: var\n  Code: String;\nbegin\n  Code := '';\n  DebugLog(_ToStr(ExecProc('WeighingJournal', 'SetSlaveDoc', _ArrEx([Code])))); // хранимая процедура\nend Источник сведений: Материалы для документации/source/_odt_extract/EventScript_desc.txt ; Материалы для документации/source/fsCoreScript.pas"},{"id":"getblobs","sectionId":"14-universalnye-zhurnaly","title":"GetBlobs","text":"GetBlobs *** GetBlobs — Список вложений записи **** function GetBlobs(Journal, Code: String): Variant Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает вложения (blob) записи журнала. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetBlobs(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getblob","sectionId":"14-universalnye-zhurnaly","title":"GetBlob","text":"GetBlob *** GetBlob — Чтение вложения **** function GetBlob(Journal, Code, Name: String): Variant Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Читает именованное вложение записи. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetBlob(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"setblob","sectionId":"14-universalnye-zhurnaly","title":"SetBlob","text":"SetBlob *** SetBlob — Запись вложения **** procedure SetBlob(Journal, Code, Name: String; Blob: Variant) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Blob: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Сохраняет именованное вложение записи. Пример вызова: begin\n  // SetBlob(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"setlink","sectionId":"14-universalnye-zhurnaly","title":"SetLink","text":"SetLink *** SetLink — Создание связи **** function SetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Caption: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkJournal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkType: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Создаёт связь записи с другой записью/журналом. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := SetLink(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"setlinknt","sectionId":"14-universalnye-zhurnaly","title":"SetLinkNT","text":"SetLinkNT *** SetLinkNT — Создание связи без триггеров **** function SetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String): String Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Caption: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkJournal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkType: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Создаёт связь без запуска триггеров. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := SetLinkNT(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getlink","sectionId":"14-universalnye-zhurnaly","title":"GetLink","text":"GetLink *** GetLink — Чтение связи **** function GetLink(Journal, Code, Name: String): Variant Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает данные связи по имени. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetLink(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getlink-link","sectionId":"14-universalnye-zhurnaly","title":"GetLink_Link","text":"GetLink_Link *** GetLink_Link — Идентификатор связанной записи **** function GetLink_Link(Journal, Code, Name: String): String Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа String (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает строковый идентификатор связи. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetLink_Link(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"getlinks","sectionId":"14-universalnye-zhurnaly","title":"GetLinks","text":"GetLinks *** GetLinks — Список связей **** function GetLinks(Journal, Code: String; MaxRows: Integer): Variant Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - MaxRows: Integer — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Значение типа Variant (тип подтверждён сигнатурой RTTI). Сведения из исходников / ODT: Возвращает связи записи. Пример вызова: var\n  r: Variant; // результат (тип уточняется сигнатурой)\nbegin\n  // r := GetLinks(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой функции.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"deletelinks","sectionId":"14-universalnye-zhurnaly","title":"DeleteLinks","text":"DeleteLinks *** DeleteLinks — Удаление связей **** procedure DeleteLinks(Journal, Code, Link: String) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Удаляет связи записи. Пример вызова: begin\n  // DeleteLinks(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"undeletelinks","sectionId":"14-universalnye-zhurnaly","title":"UnDeleteLinks","text":"UnDeleteLinks *** UnDeleteLinks — Восстановление связей **** procedure UnDeleteLinks(Journal, Code, Link: String) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Восстанавливает удалённые связи. Пример вызова: begin\n  // UnDeleteLinks(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"deletelinksnt","sectionId":"14-universalnye-zhurnaly","title":"DeleteLinksNT","text":"DeleteLinksNT *** DeleteLinksNT — Удаление связей без триггеров **** procedure DeleteLinksNT(Journal, Code, Link: String) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Удаляет связи без запуска триггеров. Пример вызова: begin\n  // DeleteLinksNT(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"undeletelinksnt","sectionId":"14-universalnye-zhurnaly","title":"UnDeleteLinksNT","text":"UnDeleteLinksNT *** UnDeleteLinksNT — Восстановление связей без триггеров **** procedure UnDeleteLinksNT(Journal, Code, Link: String) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Восстанавливает связи без запуска триггеров. Пример вызова: begin\n  // UnDeleteLinksNT(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"asyncsetrecord","sectionId":"14-universalnye-zhurnaly","title":"AsyncSetRecord","text":"AsyncSetRecord *** AsyncSetRecord — Асинхронное сохранение записи **** procedure AsyncSetRecord(Journal: String; Doc: Variant) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Doc: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Ставит сохранение записи в очередь сообщений. Пример вызова: begin\n  // AsyncSetRecord(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"asyncsetrecordnt","sectionId":"14-universalnye-zhurnaly","title":"AsyncSetRecordNT","text":"AsyncSetRecordNT *** AsyncSetRecordNT — Асинхронное сохранение без триггеров **** procedure AsyncSetRecordNT(Journal: String; Doc: Variant) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Doc: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Асинхронное сохранение без запуска триггеров. Пример вызова: begin\n  // AsyncSetRecordNT(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"asyncexecproc","sectionId":"14-universalnye-zhurnaly","title":"AsyncExecProc","text":"AsyncExecProc *** AsyncExecProc — Асинхронный вызов процедуры **** procedure AsyncExecProc(Journal, Name: String; Args: Variant) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Args: Variant — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Ставит выполнение хранимой процедуры в очередь. Пример вызова: begin\n  // AsyncExecProc(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"asyncsetlink","sectionId":"14-universalnye-zhurnaly","title":"AsyncSetLink","text":"AsyncSetLink *** AsyncSetLink — Асинхронное создание связи **** procedure AsyncSetLink(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Caption: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkJournal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkType: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Ставит создание связи в очередь сообщений. Пример вызова: begin\n  // AsyncSetLink(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"},{"id":"asyncsetlinknt","sectionId":"14-universalnye-zhurnaly","title":"AsyncSetLinkNT","text":"AsyncSetLinkNT *** AsyncSetLinkNT — Асинхронная связь без триггеров **** procedure AsyncSetLinkNT(Journal, Code, Name, Caption, LinkJournal, LinkType, Link: String) Входные параметры: - Journal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Code: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Name: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Caption: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkJournal: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - LinkType: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). - Link: String — > ⚠ ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет текстового описания назначения этого параметра (есть только тип из RTTI). Возвращает: Процедура ничего не возвращает. Сведения из исходников / ODT: Асинхронное создание связи без триггеров. Пример вызова: begin\n  // AsyncSetLinkNT(...);\n  // > ТРЕБУЕТСЯ ДОПОЛНЕНИЕ: в материалах нет готового примера вызова для этой процедуры.\nend Источник сведений: Материалы для документации/source/fsCoreScript.pas"}]};
